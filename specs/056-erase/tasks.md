# 任務：第二次的死——抹除，只留一塊疤（階段 51）

**Spec**: [spec.md](spec.md)

## 後端

- [x] T001 `erased_at` 加到 domains ＋ 四種知識表；`_LIVE` 一併排除抹除過的
- [x] T002 [測試先行] `tests/unit/test_erase.py`（13 條）
- [x] T003 `erase_knowledge`：⚠️ **只接受已封存的**——路徑上真的沒有捷徑
- [x] T004 `_ERASE_FIELDS` 內容全空；⚠️ **位址欄不動**
      （`digest_entries.url` 是位址不是內容，抹掉它疤就找不到了）
- [x] T005 `scar()`／`pointers_to()`；`restore_knowledge` 擋抹除過的
- [x] T006 `erase_domain`：⚠️ **不連帶抹除**遺骸內容——不做第二次死的串聯；
      那些內容改掛根領域，仍可單獨復原
- [x] T007 遺骸清單排除抹除過的（那是「可復原的東西」，別列出來誘人去按）
- [x] T008 ⚠️ **修掉階段 50 的缺陷**：封存不再清 `entry_embeddings`
      （檢索已在語料層濾掉，清它是多餘的且**弄壞復原**）；**抹除**時才清
- [x] T009 路由 `/api/knowledge/{erase,pointers}`、`/api/domains/{did}/erase`
- [x] T010 契約測試（4 條）

## 對抗測試（先看它變紅）

- [x] T011 活的東西也能一步抹除 → 紅 ✅
- [x] T012 抹除時真的 DELETE（不留疤）→ 4 條紅 ✅
- [x] T013 抹除領域時串聯抹除底下的遺骸 → 2 條紅 ✅
- [x] T014 抹除過的還能復原 → 紅 ✅
- [x] T015 封存又去清 embedding → 紅 ✅

## 前端

- [x] T016 遺骸區每列加「抹除」；⚠️ **只在遺骸區出現**——活的東西沒有捷徑通往它
- [x] T017 抹除前列出**指著它的東西**（`pointersTo`），不是只給一句「確定嗎」

## 驗收

- [x] T018 701 後端測試綠、36 前端測試綠、`npm run build` 綠
- [x] T019 實跑四步：活的抹除被擋（400 ＋ 說明）→ 封存（進遺骸清單）→
      抹除（`claim=[]`、`erased_at` 有值、**列還在**、離開遺骸清單）→ 復原被擋
- [x] T020 ⚠️ 實跑抓到一個缺陷並修掉：復原抹除過的東西**回 500**
      ——把**正確的拒絕**偽裝成故障；500 在使用者眼裡是「壞掉了」不是「這件事不能做」
