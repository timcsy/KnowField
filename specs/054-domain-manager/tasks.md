# 任務：領域管理頁與導航列歸位（階段 49）

**Spec**: [spec.md](spec.md)

## 後端：刪領域

- [x] T001 [測試先行] `tests/unit/test_domain_delete.py`（9 條）
- [x] T002 `delete_domain_preview`（只算**直屬**——報整棵子樹只會嚇人）
- [x] T003 `delete_domain`：內容與直屬子領域**上移到父領域**；孫輩不拉平
- [x] T004 路由 `GET .../delete-preview`（零副作用）／`POST .../delete`
- [x] T005 [測試先行] `tests/contract/test_domain_delete_api.py`（2 條）

## 對抗測試（先看它變紅）

- [x] T006 照抄檔案系統、連帶刪掉知識 → 3 條紅 ✅
- [x] T007 連帶刪掉子領域 → 2 條紅 ✅
- [x] T008 預覽報整棵子樹的數字 → 紅 ✅

## 前端

- [x] T009 取回 `DomainNav`（⚠️ 上一刀被我刪掉了），擺在 Logo 之下、＋新對話之上
      理由：**你按下「＋新對話」之前，要先看得到它會生在哪**
- [x] T010 「領域」入口改成連到 `/domains`；刪掉 `DomainEntry`（就地展開那版）
- [x] T011 底部重複的「⚙ 整理台」拿掉（同一件事不留兩個入口）
- [x] T012 管理頁補齊：站到這 📍／改名 ✏️／移到最上層 ⤴／刪除 🗑
- [x] T013 管理頁**接上當前領域**（從側欄點進來不用再找一次自己在哪）
- [x] T014 刪除前用 `confirm` 說出影響範圍；⚠️ 刪掉當前所在領域後**站到父領域**
- [x] T015 ⚠️ 動作用**圖示不用文字**——`opacity-0` 只是視覺隱藏、**仍佔寬度**，
      四個文字按鈕把 280px 面板裡深層節點的名字擠到零寬，而 `truncate` 讓它靜靜消失

## 驗收

- [x] T016 679 後端測試綠、36 前端測試綠、`npm run build` 綠
- [x] T017 實跑：導航列在正確位置；領域→管理頁且接上當前領域；樹的名字都看得見
- [x] T018 實跑刪除：知識總數 **53 → 53**（一件都沒少）、理解與子領域上移到 AI、
      被刪的領域消失、預覽零副作用 ✅
