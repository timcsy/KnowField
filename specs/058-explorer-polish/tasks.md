# 任務：領域頁打磨——桌機順、行動可用（階段 53）

**Spec**: [spec.md](spec.md)

## ⚠️ 修掉 spec 057 留下的行動版失能（三件在手機上到不了）

- [x] T001 `lib/longpress.ts` ＋ 測試（5 條）：長按＝觸控的右鍵
      ⚠️ 與拖曳**互斥**——移動超過門檻就取消，否則想拖時跳選單、想開選單時手一抖變成拖
- [x] T002 勾選框在**選取模式或已有選取**時常駐（原本只有 `group-hover` ⇒ 手機選不到任何東西）
- [x] T003 手機的樹抽屜（原本 `hidden md:block` ⇒ 整棵樹在手機上不存在）
- [x] T004 長按 → 同一個選單（原本只有 `onContextMenu` ⇒ 觸控到不了改名／封存）

## 貼近習慣（兩邊慣例不同，不硬套同一套）

- [x] T005 手機：標題列「選取／完成」（iOS Files／Drive 慣例）
- [x] T006 桌機：**雙擊開啟**（Finder 慣例）；單擊仍是開啟以免破壞既有行為
- [x] T007 桌機鍵盤：`↑`/`↓` 移動、`Enter` 開啟、`Esc` 清除
      ⚠️ 輸入框聚焦時不吃鍵，否則打字會被吃掉

## 窄螢幕版面

- [x] T008 欄位在窄螢幕只留「名稱」（種類／更新隱藏）；MUST NOT 橫向捲動
- [x] T009 工具列窄螢幕收成一行（📁樹 · ⬆上一層 · 目前位置 · 選取）；搜尋與新資料夾另起一行
- [x] T010 空狀態說得出下一步（清除搜尋／回根領域／在這裡開新對話）

## 驗收

- [x] T011 703 後端測試綠、41 前端測試綠、`npm run build` 綠
- [x] T012 實跑（390px iframe 模擬手機視埠）：
      工具列收成一行 ✅ · 只剩名稱欄、無橫向捲動 ✅ ·
      「選取」→ **92 個勾選框全部可見**（原本一個都選不到）✅ ·
      樹抽屜開得起來 ✅ · 長按 → `開啟 / 選取 / 📦 封存` ✅
- [x] T013 ⚠️ 過程中 `resize_window` **回報成功但視窗沒變**（`innerWidth` 仍 1470）
      ⇒ 改用 iframe 量真的 CSS 斷點行為
