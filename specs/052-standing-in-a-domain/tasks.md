# 任務：站在一個領域裡（階段 47）

**Spec**: [spec.md](spec.md)

## 後端

- [x] T001 [測試先行] `tests/unit/test_domain_view.py`（12 條）
- [x] T002 `domain_scope`（子樹；`None`＝根＝整個知識庫）／`domain_view`
- [x] T003 ⚠️ `outward` **相對於立足點**——兩端都在我的子樹裡就不算
- [x] T004 子領域的數字**含子孫**，且與「點進去看到的」一致
      ⚠️ 說謊的數字比沒有數字更糟（我第一版寫了個永遠回空字串的 stub）
- [x] T005 `GET /api/domains/{did}/view`；整理台清冊改用 `_inventory_rows()` 共用定義

## 對抗測試（先看它變紅）

- [x] T006 把 outward 當成邊的固有屬性 → 紅 ✅
- [x] T007 視野只含自己不含子孫 → 紅 ✅
- [x] T008 對方在根領域也算「在外面」 → 紅 ✅
- [x] T009 outward 不去重 → 紅 ✅

## 前端

- [x] T010 `lib/domain.ts`：當前領域住在 **URL**（`?d=`）＋ history push ＋ 測試
- [x] T011 `DomainNav`：麵包屑每段可點（往上）、`▾` 展開兄弟（往旁）
- [x] T012 側欄改寫：麵包屑 → 子領域 → 四種葉節點 → ⛓ 通往外面 → 🕘 最近 → ⚙ 整理台
- [x] T013 ⚠️ 🕘 最近**刻意不依領域過濾**——位置和時間是兩個軸，側欄不能只給一個
- [x] T014 `lib/scope.tsx`：清單頁依領域過濾 ＋ ⚠️ **FR-007 的宣告一定要畫出來**
      （拿不到視野時**不過濾**，不是顯示空的——把查詢失敗畫成「這裡沒東西」正是沉默失敗）
- [x] T015 四個葉節點頁都接上 scope ＋ 宣告
- [x] T016 ＋新對話／冊封／收來源帶上當前領域（接上 spec 051 那條等著的線）

## 驗收

- [x] T017 664 後端測試綠、30 前端測試綠、`npm run build` 綠
- [x] T018 實跑三個立足點：根＝105 件、AI＝12、Flow Matching＝6
- [x] T019 實跑 ⚠️ **同一條邊**：站在 Flow Matching＝「通往外面 1」，站在 AI＝**0**（被祖先吸收）✅
- [x] T020 實跑瀏覽器上一頁 → 回到上一個領域 ✅；點通往外面 → 站到它所在的領域 ✅
