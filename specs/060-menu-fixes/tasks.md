# 任務：批次退得出、選單收得起（階段 55）

- [x] T001 ⚠️ `inBatch = selecting || picked.size > 0` ＋ `exitBatch()`；
      按鈕、勾選框顯示、點一下加選、`Esc` 全部改讀它
      （**同一個狀態兩份來源**，spec 057 的 `sel`/`did` 之後**第二次**）
- [x] T002 底部列的「清除」改成「完成」——它原本只清 `picked`，
      留下 `selecting=true` ⇒ 底部列消失但仍在批次模式
- [x] T003 `ChatPage` 的「⋯ 更多」從 `<details>` 改成受控選單
      ⚠️ 關閉同時接 `mousedown` ＋ `touchstart`（只接前者的話手機關不掉——同一族第四次）
- [x] T004 移除「💾 存下這段」；⚠️ 它會把 `tempId` 清成 null ⇒ 下次 autosave **再開一段**
- [x] T005 刪掉沒有消費者的 `saveConversation`；`lib/api.ts` 的 `save` 標註原因
      ⚠️ **後端端點保留**（人閘門、`test_temp_save_web.py:91` 在守）
- [x] T006 選單補「📦 封存這段」與「🗂 搬到領域…」（含糾纏詢問）

## 驗收

- [x] T007 703 後端測試綠、47 前端測試綠、`npm run build` 綠
- [x] T008 實跑：勾選進批次 → 按鈕顯示「完成」→ 按下去**真的退出** ✅
- [x] T009 實跑聊天選單：內容 = 整理成重點／生應用／重生標題／重新分章／複製 ×2／
      **搬到領域…**／**📦 封存這段**；**沒有**存下這段；**點空白處收得起來** ✅
