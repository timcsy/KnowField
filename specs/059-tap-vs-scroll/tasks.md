# 任務：滑動不是點選（階段 54）

- [x] T001 `lib/tap.ts` ＋ 測試（6 條）：`isTap(start, end, slop=10)`
      ⚠️ 沒有起點時回 `true`（fail-open）——吃掉合法點擊比放行誤觸更糟
- [x] T002 所有列在 `pointerdown` 記下位置，`onClick` 先過 `isTap`
- [x] T003 ⚠️ `beginDrag` 觸控直接 return——捲動會誤觸拖放，而代價是**悄悄搬走知識**
- [x] T004 長按選單補「搬到…」，否則手機上搬不動東西
- [x] T008 **長按＝進批次模式**（使用者要求）：項目長按 → `setSelecting(true)` ＋ 選取它
      ⓘ 不出選單——選單那四項在批次底部列全都有，而長按→選單→選取是兩下
- [x] T009 ⚠️ `longPressed` 一次性旗標擋掉長按後補送的 `click`
      （沿用 `ConversationSidebar` 既有 idiom，不另創一套）
- [x] T010 資料夾長按仍出選單——**批次的對象是知識，不是領域**
- [x] T011 桌機右鍵的項目選單保留（仍有消費者，不是死程式碼）

## 驗收（390px iframe 模擬手機視埠）

- [x] T005 703 後端測試綠、47 前端測試綠、`npm run build` 綠
- [x] T006 實跑三條：
      滑動 60px → **不開啟** ✅ ／ 原地按放 → **開啟**（`/domains` → `/`）✅ ／
      從項目捲到資料夾列放開 → **不搬、不跳糾纏詢問** ✅
- [x] T012 實跑批次：長按 → 已選 1（補送的 click **沒有**把它取消）→ 點第二件 → 已選 2
      → 底部列有封存／搬到… → 「完成」退出 ✅
- [x] T007 ⚠️ 過程中我的**檢查**錯過一次：量 `location.search` 找 `resume=`，
      但 ChatPage 開啟後就把它清掉了 ⇒ 誤判成「點選也壞了」。改量 `pathname` 才對。

- [x] T013 ⚠️ **第二次量錯**：拿了 iframe reload 前的舊 DOM 參照去點，量到「沒有加選」
      ——重新取參照後是 1 → 2。**兩次都是檢查錯，不是程式錯。**

## ⚠️ 追加：長按仍跳出選單（使用者回報）

- [x] T014 **原因是瀏覽器自己的 `contextmenu`**——Android Chrome / iOS Safari 在觸控長按時
      會送 `contextmenu`。我只想到自己那條 450ms 的路徑，**沒想到原生還有一條**
      ⇒ 批次模式進去了、選單同時被叫出來
- [x] T015 `contextmenu` 只給滑鼠：`contextmenu` 事件**不帶 `pointerType`**
      ⇒ 用 `lastPointer` ref 記住上一次 `pointerdown` 的裝置
      ＋ 觸控也 `preventDefault()`（否則跳出系統的複製／選取泡泡）
- [x] T016 資料夾長按在手機不再出選單 ⇒ 列尾補一顆**常駐的 ⋯**
      ⚠️ 不補的話改名／搬動／封存在手機上到不了——**同一族的第三次**
- [x] T017 實跑：原生 `contextmenu` 不再叫出選單 ✅ · 批次模式仍進得去 ✅ ·
      資料夾 `⋯` → `開啟 / ✏️改名 / 📦封存` ✅
- [x] T018 ⚠️ **第三次量錯**：探測用的 `Runtime.evaluate` 逾時把 renderer 卡死，
      畫面顯示「知識庫 0 件」——我先查了 DB（17 對話／34 理解／5 領域完好）與 API（200）
      才確定是**探測把頁面弄壞的**，不是資料出事。硬重載後一切正常。
