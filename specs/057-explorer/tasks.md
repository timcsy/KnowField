# 任務：領域頁做成檔案總管（階段 52）

- [x] T001 `Layout`：領域頁滿版（⚠️ `max-w-3xl` 是根本障礙，不是樣式細節）
- [x] T002 `_inventory_rows` 帶 `at`；⚠️ 四種的時間欄名字不同，各自取 ＋ 守門測試
- [x] T003 工具列：⬆上一層 · 可點麵包屑 · ＋新資料夾 · 搜尋
- [x] T004 兩欄滿高、各自捲動；左樹含根領域列
- [x] T005 資料夾列進清單並排最前；點一下進去
- [x] T006 欄位標題可排序（名稱／種類／更新），預設更新新到舊
- [x] T007 選取模型：點＝開啟、勾選框 hover 才出現、⌘-點加選、shift-點連選
- [x] T008 右鍵選單（資料夾 4 項／項目 3 項）
- [x] T009 刪掉被 `itemRow` 取代的 `renderRow`

## ⚠️ 實跑抓到的三個缺陷

- [x] T010 **欄位錯位**：資料夾列把件數放進「種類」欄 → 種類欄空、更新欄寫著「資料夾」
- [x] T011 **`sel` 與 `did` 兩份真相**：點資料夾後側欄進去了、右邊清單沒進去
      ⇒ 砍掉 `sel` state，當前位置只認 URL
- [x] T012 **勾選框完全沒反應**：`beginDrag` 在 `pointerdown` 就 `setPicked`，
      跟勾選框的 `onChange` 互相抵消 ⇒ 選取改成**真的拖起來才變**

## 驗收

- [x] T013 703 後端測試綠、36 前端測試綠、`npm run build` 綠
- [x] T014 實跑：進資料夾（麵包屑／左樹／清單同步）、右鍵選單、hover 勾選框、
      勾選後進入選取模式、拖到左樹「數學」→「搬好 1 件」、AI 5→4、數學 1→2 ✅
