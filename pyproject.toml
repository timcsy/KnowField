[project]
name = "learnnews"
version = "0.1.0"
description = "LearnNews — AI 新聞/論文的每日分診工具（推模式 MVP）"
requires-python = ">=3.12"
# 核心與真實後端皆以標準函式庫實作，零必要外部相依（YAGNI，離線可測）。
# 真實 embedding／摘要走使用者的 OpenAI 格式 API（stdlib urllib），不需額外套件。
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8", "httpx>=0.27"]   # httpx：FastAPI TestClient 需要
# web 介面（階段 6）——框架相依只在此層，核心仍零相依
web = ["fastapi>=0.110", "uvicorn>=0.29", "jinja2>=3.1", "python-multipart>=0.0.12"]

[project.scripts]
learnnews = "learnnews.cli.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100
