# 008：階段 3 拉模式完成
> 日期：2026-07-23

## 轉移
- 舊：只有推（每日分診）；拉模式僅為 draft 設計。
- 新：**拉模式（主題拉取深挖）實作完成**（spec 002）。給定主題 → 跨來源擴展搜尋
  （arXiv 用主題查詢、其餘依相關性過濾）→ 去重 → 依主題排序 → 直達原文；預設附一句
  定位、`--raw` 純原礦。雙模式（推＋拉）到齊。

## 重點決策/成果
- **拉 vs 推的落地差別**：拉吃單一主題、跨源擴展（不只當日）；推吃當日新材料＋興趣清單過濾。
- **US2 匯整落庫**：`digest` 執行時保存 entries（新增 `digest_entries` 表），
  `pull --from-digest <rank>` 可從最近匯整某則的主題直接深挖，銜接推拉。
- **韌性處理**：真實後端失敗（實測 API 回 403 `allocation_quarantined`）不再炸 traceback，
  CLI 攔截給乾淨繁中訊息＋退出碼 1，並提示可切離線後端。教訓見 `experience.md`。
- 大量複用階段 2 地基（dedup／ranking／summarize／backends／store），新增程式很薄。

## 影響
- 里程碑：路線圖階段 3 達成；剩階段 4（知識沉澱）。
- 設計 draft `雙模式-推拉` 功成身退（推拉都做完）→ 場景反流至
  `episodes/2026-07-23-雙模式推拉的設計.md`；`興趣畫像` draft 因仍有未建 open problems 保留。
- 82 測試綠燈；離線真跑驗證拉出 arXiv 主題搜尋＋HF＋Import AI＋Ars 的混合。
- commit `223901a`；實作見 `specs/002-topic-pull/`。

## 狀態
✅ 已採用
