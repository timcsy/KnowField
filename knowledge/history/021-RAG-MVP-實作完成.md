# 021：RAG MVP 實作完成——「有吸引子的場」第一次落地
> 日期：2026-07-24

## 轉移
走完 spec-kit（specify→plan→tasks→implement）做出 spec 005 增量 1：`knowfield ask` 對已落庫
匯整可溯源問答。**這是「有吸引子的場」概念第一次接觸現實**——雖只鋪了「流＋檢索」地板
（吸引子/根因/optimizer 是後續增量）。

- **交付**：`ask "問題" [--today][--lang][-k]`；`entry_embeddings` 表；`rag/{types,answerer,
  service}`；`OpenAIAnswerer`；repository 語料/嵌入方法；digest 時批次嵌入落庫。
- **測試**：+11（unit 嵌入、integration 檢索/範圍、contract ask/邊界）；**128→139 綠、零回歸**。
- **離線 e2e 驗證**：累積問答命中正確來源、`--today`、查無說無皆正常。
- **唯一待辦**：T025 真跑忠實抽查（需使用者金鑰）——故 vision 增量 1 標「實作完成、真跑待驗」。

## 蒸餾出的 experience 教訓（新第 7 條）
**「溯源／grounding 要在程式結構上保證，不靠模型自律」**——來源清單由 `RagService` 從檢索
集合生成，非解析模型輸出；查無相關程式端直接不呼叫合成。同型前例：AI 圖標籤、根因萃取的
「推斷＋掛證據」。**模型負責生成，程式負責守約。** 這也把 concept 的 R4「溯源程式端保證」
從一次性設計升成可複用教訓。

## knowie 對映（原則在程式裡兌現）
- 原則 3（溯源）→ 來源程式端生成（教訓 7）。
- 原則 5（繞根因組織/可解釋）→ 本增量只鋪流+檢索，尚未有吸引子層，誠實標明。
- 教訓 1（離線 stub）、教訓 3（失敗攔截）、教訓 4（雜湊碰撞 fixture）→ 全在實作/測試兌現。

## draft 去留
RAG draft **不退場**——增量 1 雖完成，增量 2/3（種子/根因萃取/拓撲）仍是 in-flight 設計理由。
待整個階段 4 走完再反流退役。

## 後記：T025 真跑抽查（2026-07-24，使用者授權直接執行）
以使用者真實後端（ai.ccsh.tn.edu.tw、gpt-5.4-mini、text-embedding-3-small）跑：
- **忠實度通過**：SeerGuard 答案 5 個論點逐句對照原文 body 皆有據、零杜撰；查無說無正常。
- **舊資料惰性回填驗證**：RAG 前產的匯整（embeddings=0）首問時自動批次補算（FR-010 兌現）。
- **揪出並修 2 個真 bug（stub 測試抓不到，只有真跑抓得到）**：
  1. 門檻 0.10 對真實嵌入太低 → 無關問題也放行、「查無」還列 6 來源。
  2. 真命中時噪音來源全被列出。
  **一修解兩 bug**：門檻依 embedder 尺度校準（`Config.from_env`：openai≈0.30／離線≈0.05；
  實測命中≈0.62、無關≤0.22）。+3 門檻回歸測試 → **142 綠**。詳見
  `specs/005-rag-qa/research.md` R5、experience 教訓 4 延伸。

## 狀態
✅ 增量 1 **完成**（實作＋真跑抽查通過，142 測試綠）
