# 030：知識庫管理完成——場的生命週期閉環
> 日期：2026-07-25

## 轉移
走完 spec-kit 做出 spec 007 階段 7：web `/library` 瀏覽／刪除／重分類種子。這是原則 5
「權重由人冊封」的**另一半**（人冊封不只加，也退/改）＝concept 的**人工修剪場**。

- **交付**：repository `list_seeds`／`delete_seed`（連清 `entry_embeddings`）／`set_seed_class`
  （皆 `WHERE digest_id=種子容器`）；web `GET /library`＋`POST remove/reclassify`；`library.html`；導覽加「知識庫」。
- **測試**：+11（unit 種子管理、contract web、integration 重分類權重）；166→**177 綠、零回歸**。
- **結構亮點**：**每日流唯讀由 repo 層結構保證**（傳流的 entry_id→回 False），非靠 UI 不顯示。
  US3 因此**零實作碼**、只加驗證測試。

## 場的生命週期閉環（意義）
進水（digest/pull/ingest）→ 冊封（種子）→ 讀（問答）→ **修剪（/library 刪/重分類）**。
四環節全上 web。concept「有吸引子的場」的 optimizer 循環在程式裡走完一圈。

## 蒸餾
擴 experience 教訓 7：「保證做進程式」再添一面——**存取/唯讀邊界也做進資料層（repo WHERE），
不靠 UI 不顯示**。

## draft 去留
`知識庫管理` draft **不退場**——種子管理完成，但**來源管理面仍孵化**（耦合 `來源訂閱` draft）。

## 狀態
✅ 階段 7 完成（177 測試綠）
