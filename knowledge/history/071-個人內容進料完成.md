# 071：個人內容進料（貼上＋PDF，多嘴一 pipeline 第一刀）完成（階段 25）

> 日期：2026-08-04。承接 070（整理去重）。spec 030 出貨（294 測綠、零回歸）。
> 源＝進料層任務 2 的第一刀；由多輪研究（GeneralAffairs 萃取＋2026 SOTA＋Mistral 繁中實測）收斂而來。

## reframe：從「檔案格式」到「進料管道」
研究後把任務從「六種檔案格式」翻成「進料管道」——常見來源（AI Chat／論文／YouTube／社群／Blog）多半塌回文字。
第一刀＝兩張「驗證過＋通用」的嘴：**①貼上文字/markdown**（通用、繞過登入/付費牆——人的瀏覽器＝合法存取層）、
**②PDF → Mistral Document AI**（掛現有 gateway、實測吐繁體 markdown）。

## 核心洞見（讓實作極簡）
**一段內容切成多塊、每塊當一筆 `digest_entry` 存**，就白拿三件事：`retrieve_corpus`（029）自動檢索得到、
`/chat` 自動標「你收藏的」、**純度守衛天然成立**（收進存 corpus／不寫 why_nodes → `build_field_system_prompt`
只讀 anointed why_nodes → 收進絕不進地基、不自動變核心理解，原則 6）。**無新表**（教訓 8）。

## 關鍵決策
1. **切塊是真工程，寫成純函式 `chunk_markdown`**：原子塊（fenced code/`$$`/markdown 表格）**不切半**、`^#{1,6} ` 章節
   為優先切點（Mistral 標題階層不穩但「有標題就切」可靠）、中文按**字元數**（400+40，不能空白分詞）。呼應
   GeneralAffairs「查不到 root cause 多在切塊」「中文字元切」。純 stdlib、離線可測。
2. **轉檔藏 `DocConverter` 介面後**（憲章 IV／教訓 1）：核心零相依；`MistralDocConverter` 走 gateway `/v1/ocr`＝真實
   adapter、不進單元測試；離線靠注入 stub。**>30 頁**（Azure 單份上限，實測踩到）→ `pdftoppm` 逐頁 render 成圖走
   `image_url` 合併，**避開 30 頁上限與 poppler 笨切爆脹**（實測 6MB/49 頁被笨切成 45MB）。
3. **best-effort（教訓 3）**：轉檔/單頁失敗→跳過其餘照收、頁內友善、不噴 500、半殘不寫。
4. **收進即證言**：`source_class="ordinary"`（非 explainer），延續 029 膜分層。

## 產物
- `src/learnnews/ingest/`：`chunk.py`（`chunk_markdown` 純函式）、`convert.py`（`DocConverter`＋`MistralDocConverter`）、
  `service.py`（`ContentIngestService.ingest_text/ingest_pdf`＋`store_chunks`）。
- `config.py`：`ocr_model`（預設 `azure/mistral-document-ai-2512`，env `LEARNNEWS_OCR_MODEL`）。
- `web/app.py`：`app.state.doc_converter`（可注入）＋`content_ingest` helper；`POST /ingest/paste`、`POST /ingest/pdf`（上傳/URL）。
- `templates/ingest.html`：貼上 textarea 表單＋PDF 上傳/URL 表單＋「收了幾塊」結果。
- 測試：`test_chunk.py`(7)＋`test_content_ingest.py`(5)＋`test_ingest_web.py`(5)。277→**294**。
- 規格：`specs/030-content-ingest/`（13 任務全 [X]）。
- 真後端驗過：貼「養貓筆記」→ 繁體保留、檢索得到；arxiv PDF → 139 塊；49 頁繁中 PDF → 觸發逐頁 render 路（清理測試進料，未污染真庫）。

## 教訓（沿用，未新增）
- 教訓 1 離線 stub、教訓 3 邊界攔外部失敗、教訓 8 少動結構（復用 corpus/embeddings 無新表）、原則 6 收進＝證言不進地基。
- 切塊的幾何理由見 `concepts/有吸引子的場.md`〈高維向量〉段（一塊一意思＝讓向量指向乾淨方向）。

## 收束
draft `2026-08-03-進料層重定位.md` 設計任務 2 **第一刀落地**（貼上＋PDF）；draft `2026-08-04-進料轉檔選型.md` 仍留——
它還載後續增量的在飛設計（URL 抓正文、YouTube 逐字稿、瀏覽器擴充、手機分享、Office/影音），**未退場**。
「多張嘴、一條 pipeline」的骨架成形：新的嘴之後只加入口、餵同一條 `chunk→corpus` pipeline，不動核心。

## 增量：第三張嘴 URL 抓正文（2026-08-04，同 commit 線）
「繼續」時加第三張嘴，驗證骨架的可加性——**只加入口、pipeline 沒動**：`ingest/web.py`（`extract_article_markdown`
＝stdlib HTMLParser 抽 h1-6/p/li/blockquote 成 markdown、略過 nav/script/footer）＋`ContentIngestService.ingest_url`
（抓 HTML→抽→`chunk_markdown`→`store_chunks`）＋`/ingest/url` route（`app.state.web_fetch` 可注入）＋UI「🌐 收整篇網頁」。
品質＝**便利級 best-effort**（stdlib 非完美 readability、會漏點麵包屑；存取難/高品質走「貼上」）。測試 +7（web_extract 3
＋url service 2＋url web 2），294→301。真後端驗：arxiv abs→21 塊。**印證設計**：加嘴＝加入口、不碰 `chunk→corpus` 核心。
