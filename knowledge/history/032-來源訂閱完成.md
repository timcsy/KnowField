# 032：來源訂閱完成——場的「進水口」自助化
> 日期：2026-07-25

## 轉移
走完 spec-kit 做出 spec 008 階段 8：web `/sources` 貼 URL 自助追蹤站台/部落格。第三種追蹤
粒度（站台/作者＝持續水源）落地。

- **交付**：`sources/subscribe.py`（`discover_feed` stdlib 找 alternate link／`validate_feed` 復用
  `RssAdapter`／`subscribe` 加前驗證有料才建 Source）；`delete_source`；web `/sources`＋
  add/toggle/remove；`sources.html`；導覽加「來源」。
- **測試**：+13；177→**190 綠、零回歸**。
- **結構亮點**：`build_adapters(sources)` 早已吃 DB sources → 加來源進表即被 digest 抓，**抓取
  管線零改**（可插拔架構又一次付紅利，教訓 1）。刪除被尊重（digest 僅在來源全空才重種預設）。

## 「加前驗證有料」＝教訓 7 的第五個實例
死 feed／無 RSS **永不入庫**（`validate_feed` ≥1 條才落庫）——保證做進程式，不靠使用者
保證 URL 對。這是「保證做進最底層」的第五面（來源、AI 圖、模型無材料、UI 唯讀、feed 驗證）。
→ **教訓 7 已五面成熟，升格為衍生原則的提案更成立**（待人拍板，未擅動）。

## 場的「進水口」完整（意義）
固定名冊 ＋ **訂閱泉眼（本階段）** ＋ web search（未做）。三種進水口有了兩種，都由人冊封。

## draft 去留
`來源訂閱` draft **不退場**——RSS 站完成，但 email-ingestion（無 RSS 站）／作者跨平台／每來源
品質加權仍孵化。`知識庫管理` draft 的來源管理面也於此完成。

## 狀態
✅ 階段 8 完成（190 測試綠）
