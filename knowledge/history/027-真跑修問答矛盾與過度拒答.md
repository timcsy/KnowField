# 027：真跑修問答自相矛盾＋模型過度拒答
> 日期：2026-07-25

## 觸發
使用者在 web 問答問「AI原理」（已 ingest 6 篇解說文種子：colah 拓撲、YC 深度學習筆記等）。
答案顯示「沒有相關材料。」**卻同時列出 6 個來源**——自相矛盾。

## 診斷（種子/檢索/權重都正常）
- 6 篇解說文種子 body 健康（432/385…字）；檢索有命中、**解說文權重生效**（排在 ImportAI 前）。
- 真正兩個 bug：
  1. **模型過度拒答**：「AI原理」問得廣，模型拿到具體主題的解說文卻判「不足以回答這麼廣的
     問題」→ 回「沒有相關材料」（其實可以答）。
  2. **自相矛盾**：模型回投降語時，`RagService` 仍回 `no_material=False`＋列來源。

## 修正
- **prompt 放寬**（`OpenAIAnswerer._SYSTEM`）：優先根據材料作答、**部分相關就答**、只有
  **完全無關**才說沒材料、別因問題廣泛就拒答。
- **程式接住投降**（`RagService._is_no_material`）：偵測答案是「沒有相關材料」投降語 →
  轉 `no_material=True`、不列來源。＝教訓 7 的反面（模型自報訊號也要程式守約）。
- +1 回歸測試；166 綠。真實後端驗證「AI原理」現正常合成（MLE/MAP、Cross Entropy/KL…）＋溯源。

## 蒸餾
擴 `experience.md` 教訓 7：**模型自報的訊號也要程式接住**，否則「模型說一套、程式做另一套」。

## 狀態
✅ 已修（c78944a）
