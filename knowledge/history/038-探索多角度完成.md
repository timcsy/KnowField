# 038：探索（多角度 a，階段 9 增量 c）完成

> 日期：2026-07-25　｜　承接 history/037（promote）

## 轉移
vision 階段 9 增量 c **由「已 commit」→「已完成」**。spec 011 按 TDD 實作完：`/search` 加 opt-in
「深入探索」（預設關）——勾選時 `QueryExpander` 把 query 拆成 3–5 子角度、各搜一次、依 url
合併去重，再跑既有 SmartSearch 整理管線。測試 **209→219**、零回歸。commit `86bbcff`。

## 復用密度（架構再獲驗證）
- **整理管線一行不改**：fan-out 只換「結果從哪來」——`_collect(query, explore)` 回單 query 或
  多角度合併池，其後排序/抓取/整理**完全沿用** spec 010。
- **拆解複用既有 chat**：`OpenAIQueryExpander` 走 `backends/openai_api._post`（同 answerer 那條），
  零新相依（憲章 IV）。
- 不新增 schema、不改 answerer 對外行為（教訓 8）。

## 成本雙閘怎麼落（根公理「成本要極低」）
- **opt-in 預設關**：不勾＝零拆解、零多搜（`explore=False` 走單 query）。
- **子角度上限**：`angles = dedup([query]+expand())[:max_subqueries]`（預設 5，`KNOWFIELD_EXPLORE_MAXQ`）。
- **抓取沿用 top-N**：合併池再大，只 fetch top-N 內文整理——多角度變廣但抓取/LLM 成本不爆。
- 原 query 永遠納入 angles → explore 結果 ⊇ 單 query（不劣化）。

## 硬約束（落結構）
- **教訓 3 退回**：`expander.expand` 拋錯 → `_collect` 退回 `[query]`（等同單 query），不整個壞。
- **教訓 1 stub**：`StubQueryExpander` 回確定性 `[q 原理/應用/比較]`，契約測試零外部呼叫綠燈。
- 去重用 `_norm_url`（去尾斜線／`#fragment`）避免假重複。

## 設計決策（research）
- 先做 (a) 多角度、**不做 (b) agentic**（成本翻倍、難預測）——使用者 AskUserQuestion 選 (a)。
- 拆解防跑題靠 prompt「貼題」＋整理層仍 grounded（跑題結果排序低、整理不引用）。

## 產物
- 程式：`search/expand.py`（`QueryExpander`＋Stub＋OpenAI）、`search/smart.py`（`_collect`／`_norm_url`／
  `run(query, explore)`）、`backends/factory.py`（`make_query_expander`）、`config.py`
  （`explore_max_subqueries`）、`web/app.py`（`/search` 收 explore）、`templates/search.html`（開關）。
- 測試：`tests/unit/test_expand.py`（3）、`test_smart_search.py` explore（4）、`tests/contract/test_explore.py`（3）。
- 規格：`specs/011-explore-multiangle/`。

## 出口
- 增量 c 完成，redeem。draft `2026-07-25-智慧搜尋.md` 探索段：**(a) 已完成**（指向 spec 011/history/038）、
  **(b) agentic 仍 optional／未 promote** 保留孵化。draft 不整個 retire。
- 後續 optional：**(b) agentic 追線索**——多輪迴圈、需硬預算閘，待人拍板。
