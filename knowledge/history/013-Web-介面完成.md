# 013：階段 6 Web 介面完成
> 日期：2026-07-23

## 轉移
- 舊：只有 CLI；web 介面僅為 draft 方向。
- 新：**本機 FastAPI web app 完成**（spec 004）。首頁看今日匯整（散文＋原文圖**內嵌**＋
  一鍵原文）、輸入主題**即時拉**（TTL 快取）、管理興趣清單；RWD、全繁中、後端失敗攔成
  友善 503 頁。

## 重點決策/成果
- **`web/` 是唯一碰框架的一層**：FastAPI＋Jinja2＋Tailwind（Play CDN）。**核心一行不動、
  仍零相依**（只加 `store.get_last_digest()`）。
- **引入 web 框架相依**（fastapi／uvicorn／jinja2／python-multipart；dev 加 httpx）——刻意
  偏離「零相依」慣例，理由＝**RWD／UX 是使用者明講的真需求**（YAGNI 站得住），已在
  plan 004 Complexity Tracking 提出並**限縮於 web 層**。取捨緣起見
  `draft/2026-07-23-部署與介面路線.md`。
- **schema 加 `article_headline` 欄**：讓首頁能顯示整理過的新聞式標題（延續 history/012 的
  FR-012），digest 落庫時一併保存。
- 錯誤邊界：`OpenAIError` 例外處理器 → 友善繁中 503 頁（延續 experience 教訓 3）。
- 即時拉 TTL 快取：同主題二次不打後端（成本控制，FR-005）。

## 兩個框架版本陷阱（實作時踩到、已修）
- 新版 Starlette `TemplateResponse` 改用具名簽名 `TemplateResponse(request=, name=, context=)`；
  舊 `(name, {"request":...})` 會噴 `unhashable type: dict`。
- FastAPI `Form(...)` 需 `python-multipart`，否則 `create_app()` 就報錯。
- **通則**：接新框架後要真跑（TestClient）驗證其 API 形狀，別假設——同 experience 教訓 2/4。

## 影響
- 里程碑：路線圖階段 6 達成。階段 7–9（容器化/K8s/Helm）仍在 `部署與介面路線` draft 孵化。
- 127 測試綠燈；真跑 uvicorn＋真實後端繁中匯整驗證。commit `913bf7e`；實作見 `specs/004-web-ui/`。

## 狀態
✅ 已採用
