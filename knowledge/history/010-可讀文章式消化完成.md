# 010：階段 5 可讀文章式消化完成
> 日期：2026-07-23

## 轉移
- 舊：每則消化為「一句定位＋一句為何值得看」兩欄摘要（舊原則 4 的產物）。
- 新：每則消化為**可讀散文文章（Article）＋可選配圖**，完整傳達重點/數據/適用時機，
  每則一鍵直達原文；`--raw` 純原礦保留。推拉皆套用。這是原則 4 回調（history/009）的
  直接落地。

## 重點成果
- **Article 取代 Summary**：`summarize/article.py`（ArticleBuilder，忠實約束＋優雅降級）；
  `models` 新增 Article/Figure；DigestEntry/PullEntry 改帶 article；digest_entries 增欄。
- **media 層**：`figure_extract`（原文圖 best-effort，取不到回 None）＋ `ai_image`
  （可選，必標「AI 示意・非原文」，FR-007）。
- **忠實/不代勞**：散文 prompt 強約束「只依原文、不捏造、不下結論」；stub 保真；
  真實後端忠實度**待 API 隔離解除後抽查**（experience 教訓 2）。
  **（後續：已於 2026-07-23 隔離解除後抽查通過，見 `history/012`。）**
- **韌性**：後端失敗優雅降級（article.degraded、退出碼 0、不炸；experience 教訓 3）。
- 零新增相依（散文/AI 圖走既有 OpenAI 格式 urllib、抓圖用 stdlib）。

## 影響
- 里程碑：路線圖階段 5 達成。階段 4（知識沉澱）維持降低優先。
- 設計 draft `可讀文章式消化` 功成身退 → 場景反流至
  `episodes/2026-07-23-可讀文章式消化的設計.md`。
- 103 測試綠燈；離線驗證每則完整散文＋一鍵原文、論文×新聞混合、--raw 正常。
- commit `3f82e03`；實作見 `specs/003-readable-digest/`。

## 狀態
✅ 已採用
