# 012：真跑驗證（忠實度通過）與消化強化（語言、標題）
> 日期：2026-07-23

API 額度隔離解除後，補做階段 5 遺留的真實後端驗證，並依使用者回饋強化消化。

## 1. 忠實度抽查——通過 ✅
- 待辦來源：experience 教訓 2、`history/010`、`history/011`（原本 API `allocation_quarantined`
  403 卡住，見 `history/008`）。
- 做法：真實後端產出繁中散文後，抽 SeerGuard（`arXiv/HF 2607.15550`）比對——消化引用的
  `0.191`、`0.596`、`0.347`、`0.130`、SAWM、Qwen3-VL-8B **全部出現在原文摘要**，零捏造。
- 結論：**忠實度契約成立**（FR-002/003 在真實後端下驗證通過）。此待辦關閉。

## 2. 消化語言可指定（`--lang`）——擴充 FR-010
- 舊：消化語言寫死繁中。
- 新：**預設繁體中文**（英文原文自動翻譯改寫），使用者可 `--lang`／`LEARNNEWS_LANG` 指定
  其他語言。`config.article_lang`＋提示參數化。spec 003 FR-010 已更新。commit `3289e95`。
- 註：語言由真實後端產生；離線 stub 無法翻譯（保留原文語言）。

## 3. 新聞式標題（FR-012）——依使用者回饋
- 使用者要「每篇加一個整理過的標題，像新聞那樣」。
- 做法：消化多產出一行**新聞式標題**（跟隨 `--lang`、忠實不誇大）當標頭；**原標題保留為
  副標**供溯源（原則 3）。`Article.headline`；`ArticleBackend.write_article` 回 (headline, body)。
  離線 stub 無法整理 → 退回原標題。spec 003 加 FR-012。commit `aa37afa`。
- 真跑範例：「核能、基準測試與政策：AI 安全討論焦點再升溫」（原標題 Import AI 446…）。

## 狀態
✅ 已採用（三項皆已驗證/實作；忠實度待辦關閉）
