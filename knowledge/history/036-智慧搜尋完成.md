# 036：智慧搜尋（階段 9 增量 b）完成

> 日期：2026-07-25　｜　承接 history/035（promote）

## 轉移
vision 階段 9 增量 b **由「已 commit」→「已完成」**。spec 010 按 TDD 實作完，`/search` 從「倒原始
結果」升級為 **RAG over 搜尋結果**：搜尋 → 排序（Embedder＋cosine）→ 抓 top-N 內文（`fetch_url`）→
包 `CorpusEntry` → grounded answerer 合成繁中「整理」（逐點 `[n]`）→ 頁面頂端整理＋排序後結果
（每則仍可收進）。測試 **197→209**、零回歸。commit `635195d`。

## 復用密度（架構再獲驗證）
- **零新核心**：整理＝`Answerer`（spec 005）、抓內文＝`fetch_url`（spec 006）、排序＝`Embedder`＋
  `cosine`、grounded／無材料＝`_is_no_material`（spec 005）、搜尋＝`WebSearch`（spec 009）。新模組
  `search/smart.py` 只是**編排**這些件。
- **不改 answerer 介面**：把抓到的內文包成 `CorpusEntry(entry_id=序位, title, url, body)` 餵入既有
  `Answerer.answer(q, passages, "繁體中文")`——`[n]` 語義天然對齊。
- **不新增相依、不新增 schema**（教訓 8）；收進沿用種子容器。

## 硬約束怎麼守（落在結構，非提示自律）
- **教訓 7 grounding**：整理過 `_is_no_material` → 無材料時不列 `[n]` 來源（沿用 history/027 的
  「說沒材料卻列來源」矛盾修正）。
- **教訓 3 降級**：單則內文抓不到 → 退回該則 snippet；整理層整體失敗 → 保留原始結果＋友善
  `overview_error`；路由再包一層攔整體異常（分層攔截，research R6）。
- **原則 5 人挑**：整理是閱讀輔助、不落庫；只有「收進」才成種子。

## 設計要點（research 決策）
- **先排序、後抓 top-N**：排序吃便宜的 title＋snippet、只對前 N 抓網頁 → 抓的是最相關的、成本低。
- `[n]` ↔ 結果卡：結果卡 `id="res-n"`、整理 `[n]`→`#res-n`，複用問答頁維基式上標捲動 JS。
- top-N 預設 4（`LEARNNEWS_SMART_TOPN` 可調）。

## 產物
- 程式：`search/smart.py`（`SmartResult`＋`SmartSearch`）、`backends/factory.py`（`make_smart_search`）、
  `config.py`（`smart_search_topn`）、`web/app.py`（`/search` 走 `smart_search_factory`、分層攔截）、
  `templates/search.html`（整理段＋錨點）。
- 測試：`tests/unit/test_smart_search.py`（7）、`tests/contract/test_smart_search.py`（5）；更新 spec 009
  路由測試改注入 smart。
- 規格：`specs/010-smart-search/`。

## 出口
- 增量 b 完成，redeem。draft `2026-07-25-智慧搜尋.md` 的**核心段已固化**；**探索段（optional）仍
  孵化保留**（未 promote），故 draft 不整個 retire——slim 成只剩探索的 in-flight rationale。
- 後續 optional：**探索**（multi-angle／agentic）——成本翻倍，待人拍板另 promote。
