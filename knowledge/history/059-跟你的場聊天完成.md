# 059：跟你的場聊天（階段 17）完成——capture 迴圈引擎落地

> 日期：2026-07-29　｜　承接 history/058（promote）

## 轉移
vision 階段 17 **由「待實作」→「已完成」**。spec 022 按 TDD 實作完：`/chat` 多輪對話，AI 從使用者
冊封的根因往下推、維持反逢迎的膜、提冊封候選（人按才寫）、按需找佐證。測試 **322→336**、零回歸。
commit `746baa3`。**真後端質性驗過（SC-006，第一風險）**：對真實場問「Flow matching」→ 自動版複刻
手動品質——援引 #14（殘差=迭代精修）/#13（擴散=局部反演）往下推、通篇標 grounded、把「時間連續版
residual refinement」類比明確標為 **applied 不冒充 derived**（低層不借高層權威的最細防線在 code 生效）。

## 三個概念第一次同時變成使用者可操作的對話
- **concept forward pass**：讀場（注入 anointed roots）＝讓對話在你的吸引子場跑前向傳遞。
- **拆開的 optimizer**：AI 提冊封候選＝算梯度；`/chat/anoint`（人按）＝optimizer step、唯一寫 bedrock 路徑。
- **複利而不污染**（有吸引子的場新投影）：膜 system prompt 的分層（derived/empirical/applied、低層不借
  高層權威）＝守純度；對話短暫不落庫、永不自動改場＝原則 5。**這功能就是 principle 6 的體感版。**

## 關鍵設計（串既有零件、核心零新相依/零新表）
- **核心價值＝`build_field_system_prompt`**（8 條膜指令＋場脈絡注入根因 claim/ladder）——第一風險是
  「自動版能否複刻手動品質」，真後端驗過即答案。
- 多輪：`_post` 直接吃 messages list；**歷史 client 帶回**（hidden field），無 server session、無新表。
- 冊封候選：對話自由文（可建議冊封）＋`/chat/distill` **結構化蒸餾**成 `CandidateDraft`（不解析自由文，robust）。
- 佐證按需：`/chat/cite` 觸發才 `make_web_search`（省成本、深淺分明），非自動每輪搜。
- **原則 5 在 code**：`/chat`、`/chat/distill` 不寫 why_nodes（有守衛測）；**唯有 `/chat/anoint`（人按）**寫 bedrock。

## 排序更正（已在 history/058）
本階段 re-route 了「先等 /worth 訊號再蓋對話」——對話是動機引擎、先於進料口。賭注已被 2026-07-29
現場示範驗過（符 experience 教訓：先探針再蓋）。

## 產物
- `chat/field_chat.py`（ChatBackend/Stub＋`build_field_system_prompt`＋`FieldChat`＋`CandidateDraft`）、
  `backends/openai_api.py`（`OpenAIChatBackend`）、`backends/factory.py`（`make_chat_backend`）、
  `web/app.py`（`/chat`＋`/chat/distill`＋`/chat/anoint`＋`/chat/cite`＋factories）、`templates/chat.html`＋`base.html`。
- 測試：`tests/unit/test_field_chat.py`(7)、`tests/contract/test_chat_web.py`(7)。規格：`specs/022-field-chat/`。

## 出口
- 階段 17 完成，**capture 迴圈引擎落地**。真驗收「複刻到讓使用者想餵場」長期待觀察（vision 標註）。
- `反逢迎的當下副手` draft「跟你的場聊天」段標完成；draft **不退場**——續孵化：收內容口 extension（設計 B）、
  場驅動關聯、moment A `/worth` 的 tunnel 可達、軌跡量測、種子也注入對話。
- 後續（未 commit）：對話注入種子（非只根因）、多跳 forward pass、自動 tool-use（LLM 自決要不要搜）。
