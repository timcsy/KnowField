# 043：趨勢讀數（階段 11）完成——收尾「幫我發現在紅什麼」

> 日期：2026-07-25　｜　承接 history/042（promote）

## 轉移
vision 階段 11 **由「已 commit」→「已完成」**。spec 013 按 TDD 實作完：首頁頂端「🔥 今日高頻」
6–8 熱詞 chips，從最近幾份真實匯整標題用**純統計**萃取，點擊 → `/pull` 深挖。**收尾了最初的
使用者請求「幫我發現在紅什麼」**（2026-07-24，趨勢熱詞 draft 的起點）。測試 **234→243**、零回歸。
commit `8b1233c`。

## 這一塊的意義：場動力第一塊
前面 12 個 spec 都是「進水／冊封／讀單則／修剪」——**趨勢讀數是第一塊「讀場的整體趨勢」**。
趨勢＝**盆地通量**（concept：流往哪個吸引子灌），與成核同源。discovery 直接兌現根公理
「跟上趨勢的成本要極低」——使用者不必事先知道趨勢。

## 設計（research）
- **斷詞**：自寫「英文詞＋中文相鄰 bigram」——既有 `tokenize` 把中文拆單字、不成主題詞；bigram
  是零相依、夠用的中文抓法（不引入分詞相依，憲章 IV）。
- **門檻＋停用詞**：count≥2 才算趨勢；內建 STOPWORDS（中英常見＋領域泛詞如 模型/AI/model）
  濾掉無辨識度的詞（FR-007）。稀疏材料自然算不出 → chips 優雅省略（FR-005）。
- **零 schema**（教訓 8）：用已落庫 `digest_entries` 標題（`recent_digest_titles` 排除種子容器）；
  全量候選池落庫留後續。
- **純函式**（教訓 1）：`trend_keywords(titles,...)` 無 IO、離線可測、零外部呼叫；MVP 統計故無
  LLM 幻覺風險。
- **溯源＋中性**（原則 3/4）：熱詞點擊 → `/pull` 溯源真實材料；措辭「今日高頻」不預言「會爆」。

## 其他路線（否決／擱置）
- LLM 萃取熱詞：貴、要防幻覺 → 後續。
- 竄升排序（今天 vs 前幾天）：需存歷史 → 後續。
- 全量候選池落庫、成核、live web 熱詞：後續／已有 `/search`。

## 產物
- `trend/keywords.py`（`trend_keywords`＋`STOPWORDS`）、`store/repository.py`
  （`recent_digest_titles`）、`config.py`（`trend_top_n`/`trend_recent_digests`）、`web/app.py`
  （home 串接）、`templates/digest.html`（chips 區塊）。
- 測試：`test_trend_keywords`(5)、`test_recent_titles`(2)、`test_trend`(2)。
- 規格：`specs/013-trend-keywords/`。

## 出口
- 階段 11 完成，redeem。趨勢熱詞 draft：**A（web 搜尋）＋B（趨勢讀數）皆完成**，只剩**竄升排序／
  (a) LLM 萃取／成核／全量池落庫**孵化。
- 後續場動力：成核（無主通量→建議新吸引子）、竄升（需存歷史）、衰減沉降。
