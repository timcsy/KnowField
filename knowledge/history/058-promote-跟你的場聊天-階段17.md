# 058：promote 跟你的場聊天（moment B/C）→ vision 階段 17

> 日期：2026-07-29　｜　承接 history/057（moment A `/worth` 完成）＋本次現場示範

## 轉移
promote「跟你的場聊天」→ **vision 階段 17**。使用者拍板「promote 並開 speckit」。

## 關鍵：排序更正（re-route 我先前的「先等 /worth 訊號」）
先前（history/057 後）我主張「先等 /worth 一週真驗收、再決定是否蓋對話」。**使用者 2026-07-29 點破依賴
方向反了**：>「要做到 /worth，就要先把與知識『對話』功能做好，像我剛剛那樣，我才會因為想聊把東西
丟進來。」——**對話是動機引擎**，/worth 等進料口沒有「想跟場聊」這個動機就不會被餵；故「/worth 一週
會不會用」在**沒有對話下是假陰性**。**先有對話、餵養才有理由。** 我認錯、re-route。

## 為何不算搶在訊號前面（符合 experience 教訓）
教訓「先驗真實有用再蓋」——**對話賭注已被本次示範驗過**：整段（Flow matching→Transformer 內部）就是
手動探針，使用者「像我剛剛那樣，我才會想餵」＝訊號已回。故蓋它不違教訓。

## 第一風險（spec 要圍著設計）
剛剛的品質是**Claude 手動**做的——**自動版能否複刻**？價值全在 **membrane system prompt**（讀場＋膜＋
分層 derived/empirical/applied＋grounding＋提候選人按鈕），如 /worth 的兩段 prompt。真驗收是質性的（真後端驗）。

## 範圍
- 多輪對話、從冊封根因往下推、反逢迎的膜、每格報場-增量、按需找佐證、anoint-from-chat（人按）。
- 複用：`list_field_attractors`（讀場）、`make_web_search`（佐證）、chat 後端、`add_why_node`/`anoint`（人閘門）。
- **桌面深工優先**（非手機一瞥）→ 降低部署前置。
- out：自動每輪自搜、自動 capture、手機/tunnel、軌跡儀表板。

## 出口
- 行為規格已在 `反逢迎的當下副手` draft「跟你的場聊天」段（雙向連結），待階段 17 做完反流。
- 下一步：`/speckit-specify` 開 spec 022。
- 相關：experience「提案-批准≠打到需求」＋解方（先探針再蓋）、principle 6（膜/過度擬合檢查）、
  concept `有吸引子的場`（複利而不污染）、spec 021（/worth＝一個進料口）、示範產物冊封進使用者個人場
  `knowfield.db` #14/#15（非本 repo）。
