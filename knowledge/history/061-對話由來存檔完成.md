# 061：對話的「由來」存檔（階段 18）完成——tool 三層對齊 knowie

> 日期：2026-07-29　｜　承接 history/060（promote）

## 轉移
vision 階段 18 **由「待實作」→「已完成」**。spec 023 按 TDD 實作完：兩存檔點（冊封時連同存＋獨立存）、
存整段＋自動「由來」標題、`/roots` 根因「← 由來」連得回、`/conversations` 清單/單篇。測試 **348→362**、
零回歸。commit `3411a5c`。**真後端驗過**：獨立存→真標題「Transformer殘差為何用加法」；冊封連同存→
`/roots` 由來連得回。

## tool 三層就此對齊 knowie（concept 完整落地）
使用者「想存對話、是沙盒但有脈絡、忘記為何有這想法可連回查」＝tool 長出 **episodes** 層。三層齊：
**根因＝principles（地基）／對話＝draft（沙盒）／存下的對話＝episodes（場景）**。這是 `concepts/有吸引子
的場` 的完整落地——地基/沙盒/場景三態俱全。使用者獨立重新發現了 knowie 自己的結構。

## 一條硬原則在 code 生效（principle 6）
存下的對話**仍是沙盒層、唯讀、不入地基**——`build_field_system_prompt` **只吃已冊封根因、不碰
conversations 表**。**核心守衛測**：存一段含 `SECRET_FANTASY` 的對話後，新 `/chat` 的 system prompt
**不含**它。＝「把純度保證做進結構」（同教訓 7 溯源不靠自律）。

## 關鍵設計
- **新表 `conversations`**（第一個落庫的對話產物；教訓 8 記一筆：正當新實體，對話由暫時升為可回溯場景）。
- **刪根因不孤兒**：`why_node_provenance()` JOIN why_nodes → 根因被刪，對話仍在 `list_conversations`
  （自然變獨立）、`/roots` 不再連到、不崩（D3）。
- **自動標題**：`FieldChat.title` 一句摘要；失敗退回首句（教訓 3，不讓存對話崩）。
- **人閘門**（原則 5）：`/chat/save`＋`/chat/anoint` 的 save_convo，唯有人按才存；不自動存全部（不囤積）。

## 產物
- `store/schema.py`（conversations 表）、`store/repository.py`（save/list/get/provenance）、
  `models`（Conversation）、`chat/field_chat.py`（title）、`web/app.py`（/chat/save、/conversations[/{id}]、
  /chat/anoint 擴充、/roots provenance）、`templates/conversations.html`＋`conversation.html`＋chat/roots/base。
- 測試：`tests/unit/test_conversations.py`(4)、`tests/contract/test_conversation_web.py`(8)、field_chat title(2)。
  規格：`specs/023-conversation-provenance/`。

## 出口
- 階段 18 完成，**tool 三層齊、concept 完整落地**。`對話的由來存檔` draft 反流退場（設計已實現）。
- 後續（未 commit）：對話全文搜尋、跨對話關聯、編輯/版本、moment C（演算法本質深講）。
