# 065：既有重複對話清理 完成（階段 21）

> 日期：2026-07-30。承接 history/064（階段 20 修「以後不再複製」）。spec 026 出貨。

## 由來
階段 20 只堵住「以後不再複製」，`knowfield.db` 仍留 15 份同 32 句 Flow Matching 複本＋Transformer 那組
（真實使用審計照出的既存囤積）。使用者 2026-07-30 定案清掉。**刪除不可逆**，故設計圍著「安全」。

## 關鍵決策：刪除功能的三重護欄
1. **計畫與執行分離**：純函式 `plan_dedupe(convos, provenance)->DedupePlan`（分組/選留存/算重指）＝可離線
   窮舉測；repo 薄執行層才真刪。「決定做什麼」與「做」分開，測得完整、風險低。
2. **只併同指紋（非破壞的機械保證）**：分組鍵＝`conversation_fingerprint`（複用 spec 025）。內容完全相同才併，
   **不靠人判斷**；異指紋（#18/#19，65 vs 70 句）鍵不同→天然不同組→絕不動。真實 db 驗：19→4，兩個異長度版保住。
3. **兩段式人閘門**（原則 5）：GET 預覽（唯讀、守衛測證不動資料）＋POST 人確認才執行。無自動/背景/定期清理。
   ＋執行前已備份 `knowfield.db`。

## 其他決策
- **留存＝id 最大（最新）**：保最新標題/時間；其餘份的根因連結重指過來。
- **重指走既有 why_node 側連結**（spec 025 的 `why_nodes.conversation_id`，多對一）：只改指標、**根因主張/階梯完全不碰**
  （FR-006 守衛測）。清理後 `why_node_provenance` 每條仍連得到留存份（不斷、不孤兒）。
- **無新表/新欄**：只刪多餘 conversations 列＋UPDATE 連結（教訓 8）。

## 產物
- `chat/capture.py`：`plan_dedupe`＋`DedupePlan`（純函式、零相依）。
- `repository.py`：`dedupe_plan()`（唯讀）＋`apply_dedupe()`（重指＋刪、回摘要）。
- `app.py`：`GET /conversations/dedupe`（預覽）＋`POST`（確認執行→redirect flash）。
- `templates/dedupe.html`（預覽＋確認/取消）＋`conversations.html`（清理鈕＋成功 flash）。
- 測試：`test_capture_core.py` 擴 `plan_dedupe`（5）＋`test_dedupe_web.py`（4，含人閘門守衛/非破壞/根因主張不變）。414→423。commit `761eceb`。

## 教訓再釘（沿用既有 experience，未新增）
- **不可逆操作的設計＝三重護欄**（計畫/執行分離＋機械式非破壞判準＋預覽/人確認）——這是原則 5「人閘門」
  在「刪除」情境的具體落法。呼應教訓「擴充選免動既有資料結構」（無新表/欄）與「真後端/真資料才驗得準」
  （真 db dry-run 驗 2 組/15 份/19→4）。未新增教訓——既有原則/教訓已涵蓋，避免過度擬合。

## 收束：對話收料三部曲完成
階段 20（以後不再複製）＋階段 21（清既有）＝**囤積漏徹底補完**。draft `2026-07-29-對話收料的漏.md` 只剩
**#3（標題凍住/章節切分/落點重命名）待議**，其餘全落地。
