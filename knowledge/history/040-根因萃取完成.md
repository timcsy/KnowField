# 040：根因萃取（階段 10）完成——場的四步合攏

> 日期：2026-07-25　｜　承接 history/039（promote）

## 轉移
vision 階段 10 **由「已 commit」→「已完成」**。spec 012 按 TDD 實作完，**場的四步（進水→冊封
→讀→修剪）真正合攏**：`/roots` 對種子萃取候選根因、跑試金石自我反駁、人冊封、已冊封 why-node
成高權重語料餵回 ask。測試 **219→232**、零回歸。commit `9af2aab`。同時收斂 vision 舊 2b 佔位
（原掛階段 4 增量，標完成→指向階段 10）。

## 母概念本體落地——concept↔vision↔code 三方一致
concept `有吸引子的場` 的紀律逐條落成程式：
- **試金石自我反駁**（concept :65「MUST 對自己試著反駁」）→ `rootcause/extract.py` `TOUCHSTONES`
  7 條＋`OpenAIExtractor._SYSTEM` 明令逐條 pass/fail＋霧詞旗標。
- **候選≠冊封、人挑**（原則 5／concept :61）→ 候選存 `why_nodes(status='candidate')`；只有
  `/whynode/anoint` 轉 `anointed`；**無自動轉正路徑**（concept :126 否決自動冊封）。
- **grounding 落結構**（教訓 7）→ 候選必帶證據 url＋試金石；`no_material`／失敗不建候選、不杜撰。
- **重吸引子**（concept :50「一篇講透打敗五十篇」）→ `_weight("root")=rag_root_weight(2.0) >
  explainer(1.5)`。
- **拆開的 optimizer**（concept :121）→ AI 提候選（算梯度）／人冊封（optimizer step）。

## 關鍵設計（research）
- **負 entry_id 避碰撞**：已冊封 why-node 以 `entry_id=-id` 映成 `CorpusEntry(source_class="root")`
  UNION 進 `list_corpus_entries`——復用整條檢索/嵌入路徑，零改既有表（`entry_embeddings` 無 FK）；
  刪 why-node 一併清負 id 嵌入。
- **新表不動舊表**（教訓 8）：`why_nodes` 表 `CREATE IF NOT EXISTS`＋`_migrate` 冪等（既有 db 生效）。
- 萃取複用既有 chat `_post`（零 pip 相依）。

## 產物
- 程式：`store/schema.py`（`why_nodes`＋migrate）、`rootcause/extract.py`（`Candidate`/`WhyNode`/
  `RootCauseExtractor`/Stub/OpenAI）、`backends/factory.py`（`make_root_cause_extractor`）、
  `store/repository.py`（why-node CRUD＋corpus UNION）、`rag/service.py`（root 權重）、`config.py`
  （`rag_root_weight`）、`web/app.py`（`/whynode/*`＋`/roots`）、`templates/roots.html`＋`library.html`＋nav。
- 測試：`test_rootcause.py`(4)、`test_why_nodes_repo.py`(3)、`test_rag_root_weight.py`(1)、
  `test_root_cause.py`(5，含閉環)。
- 規格：`specs/012-root-cause/`。

## 出口
- 階段 10 完成，redeem。場的核心循環**全綠**：進水（名冊/訂閱/web 搜尋整理探索）→ 冊封（種子
  ＋**根因**）→ 讀（問答，根因最重）→ 修剪（知識庫管理）。
- 後續 concept 驅動增量（未 commit）：一根因掛多載體、**成核**（無主通量建議新吸引子）、跨來源
  三角測量、衰減——多屬「趨勢讀數／場動力」方向。
