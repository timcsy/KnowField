# 026：web 收進框——種子 ingest 接上網頁（增量 2a fast-follow）
> 日期：2026-07-24

## 轉移
spec 006 增量 2a 原為 CLI-only（web 列 fast-follow）。使用者要「web 先」，遂加 `/ingest` 頁——
**薄層、複用 `SeedService`、零改核心**。

- **交付**：`web/app.py` `_default_seed_ingest` 工廠＋`GET/POST /ingest`（`seed_ingest_factory`
  可測試覆寫）；`templates/ingest.html`（ref 輸入＋解說文勾選＋結果）；導覽加「收進」。
- **失敗處理**：`SourceUnavailable`／`OpenAIError` 在路由內攔、**頁內顯示友善訊息（非 500）**
  ——因 `SourceUnavailable` 不被全域 `OpenAIError` 處理器攔（教訓 3 的 web 版）。
- **測試**：+4 contract（表單/成功溯源/已在庫/失敗頁內攔）；161→**165 綠**、零回歸。

## 現況：RAG 個人知識庫的介面完整度
增量 1（問答）＋2a（收進）皆 **CLI＋web 雙介面**。使用者可全程在瀏覽器：收種子 → 問答 → 溯源。

## 留待（累積中，待單獨校正）
`docs/usage.md` 開頭簡介＋「設計原則」段仍有**舊定位語**（分診工具/深加工自己做/溯源不代勞/
不生成結論）——與原則 4（消化到底）＋RAG＋根因萃取方向已明顯矛盾（history/023 已首次標記，
此處第二次記，範圍擴大到 3 處）。**建議下次單獨跑一輪 docs 校正**。

## 狀態
✅ 完成（web 收進框上線，165 綠）
