# 002：架構「TBD」→ 定案，含兩點務實偏離
> 日期：2026-07-23

## 轉移

階段 1／2 的規格與實作，把先前保留的「架構 TBD」收斂為具體決策，並連帶固化差異化定位。

1. **架構**
   - 舊：`vision.md` 記「架構 TBD——待階段 1 決定」。
   - 新：**Python 3.12＋薄 CLI／核心函式庫、SQLite 儲存**；本地 embedding 去重與排序、
     小型 LLM（Claude Haiku）封頂摘要。技術脈絡見 `specs/001-daily-triage-digest/plan.md`。

2. **差異化定位**
   - 舊：`draft/2026-07-23-競品地貌與差異化.md` 標「刻意延到階段 1 定案」。
   - 新：**押廣度＋主權（非個人化本身）**，已由 spec 採納（FR-001／Assumptions），
     回流至 `vision.md` 核心想法。

## 兩點務實偏離（實作時決定，需留因果軌跡）

1. **測試框架：計畫寫 pytest → 實作用 stdlib `unittest`。**
   - 理由：環境未安裝 pytest；`unittest` 零安裝、universally available，且與 pytest 相容
     （TestCase 可被 pytest 收集）。不影響 TDD 精神。

2. **相依：計畫列 sentence-transformers／anthropic → MVP 預設離線可插拔後端。**
   - 理由：無法保證安裝重量級套件、也不宜在測試打真實 API。改以介面抽象
     （`Embedder`／`Summarizer`），預設 `HashingEmbedder`／`StubSummarizer`（確定性、離線），
     真實後端作為可選 extras 插拔。與 research.md R4/R5「介面抽象方便替換」一致，未違反 spec。
   - 蒸餾出的教訓見 `experience.md`（可插拔離線後端 → 零安裝 TDD）。
   - **⚠ 已被 004 取代**：本項所述真實後端（sentence-transformers／Claude Haiku）已於
     同日改為使用者的 OpenAI 格式 API，見 `history/004-真實後端改用-OpenAI-格式-API.md`。
     （可插拔介面本身不變——正是它讓後端能無痛替換。）

## 為什麼變
「TBD」是刻意保留給階段 1 的空位；規格與實作一到位，就必須把狀態同步回知識庫，
否則 `/knowie-next` 會讀到過時的「架構未定／階段未完成」而誤判。兩點偏離則是實作
現實（環境限制）與憲章原則 IV（YAGNI、簡潔）共同推導的結果，並非隨意變更。

完整實作在 `specs/001-daily-triage-digest/`（49 測試綠燈、HF Papers 真實跑通）。

## 狀態
✅ 已採用
