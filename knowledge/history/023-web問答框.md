# 023：web 問答框——RAG ask 接上網頁（增量 1 fast-follow）
> 日期：2026-07-24

## 轉移
spec 005 增量 1 原為 CLI-only（web 列範圍外、標 fast-follow）。使用者要「先反映到 web」，
遂加 `/ask` 頁——**薄薄一層介面，複用已測的 `RagService`，零改核心**。

- **交付**：`web/app.py` `/ask` 路由（`q`＋`--today`；`_default_rag_answer` 工廠可測試覆寫）；
  `templates/ask.html`（問答框＋答案卡片＋來源清單一鍵原文）；導覽加「問答」。
- **比 pull 簡單**：問答幾秒回，**不用 SSE**（pull 慢才需串流，見 history/015）；同步渲染。
- **後端失敗**：經既有全域 `OpenAIError` 處理器 → 503 友善頁（教訓 3，與首頁同機制）。
- **測試**：+5 contract（渲染/查無/**離線整合走真 RagService**/後端失敗友善）；142→**147 綠**。
- **真跑驗證**：真實後端 web 問答 SeerGuard 忠實、**來源只列命中篇**（門檻修正生效）、可回原文。
- 順手：footer 舊句「消化以省時，溯源不可省」→「消化到底，隨時可回溯」（原則 4 現況）。

## 留待
`docs/usage.md` 開頭簡介仍有舊定位語（「分診工具、深加工由你自己做、不下結論」），與原則 4
（消化到底）＋RAG 已不符——**待後續 docs 校正**（非本次範圍）。

## 狀態
✅ 完成（web 問答框上線，147 測試綠）
