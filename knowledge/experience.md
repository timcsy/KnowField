# 經驗

<!--
  這份文件記錄從開發過程中蒸餾出的教訓——不是 changelog，
  而是應該影響未來決策的模式。

  每個教訓記錄「理論」和「現實」之間的落差。
  保持簡短、可操作。一條教訓背後完整的因果軌跡（怎麼演變、考慮過什麼）放在 knowledge/history/。
-->

## 教訓

<!--
  每個重要的教訓加一個小節，使用四段式格式：
  1. 理論/預期怎麼說
  2. 實際發生了什麼
  3. 如何解決
  4. 蒸餾出的教訓

  這個格式迫使你清楚說明「為什麼」令人意外，
  而不只是「發生了什麼」。

  別人常記下的教訓（看看有沒有戳中你踩過的）：
    1. 動手前先 print 定位，別急著用推理找 bug
    2. 大重構時，讓編譯器／型別檢查當「待改清單」
    3. 效能多半來自資料模型，不是微優化
    4.「錯 → 懂 → 修」勝過「想到完美再動手」
    5. 別跳過 TDD——「之後再補測試」通常 = debug 三倍久
-->

### 把重量級相依藏在可插拔介面後，預設離線 stub

- **理論說**：分診管線需要 embedding（去重／排序）與 LLM（摘要），計畫原列
  sentence-transformers 與 anthropic 為相依——預期實作與測試都需先安裝這些重量級套件。
- **實際發生**：實作環境無法保證安裝這些套件、也不宜在測試中打真實 API；若硬綁，
  TDD 會變成「先裝好幾百 MB 才能跑第一個測試」，且測試不確定（打網路）。
- **解決方式**：把 embedding／摘要都抽象成介面（`Embedder`／`Summarizer`），MVP 預設
  用確定性、零相依的 `HashingEmbedder`／`StubSummarizer`；真實後端作為可選 extras 插拔。
  結果 49 個測試零安裝、離線、確定性通過，且 CLI 還能真實跑通 HF Papers。
- **教訓**：把外部重量級相依藏在窄介面後、預設一個離線確定性實作——TDD 才能零安裝、
  快、可重現；真實後端等要用時再插拔。這也正是「先簡單、YAGNI」（憲章原則 IV）。
- **補面（真實側才是坑，2026-07-25）**：stub 側好寫，**真實那一側若只寫通用殼**（如 Bearer
  ＋泛 payload）**很可能對不上任何真實提供者、卻看似完成**——各家認證格式不同（Bearer header／
  body `api_key`／自訂 header）。預設形狀要**對準一個具體提供者**、且留注入點（如 `poster`）供測試。
  出處：web 搜尋接 Tavily 時，原 Bearer 對不上（金鑰要放 body）——見 `history/034`。
- **來源**：[history/002-架構選型與務實偏離.md](history/002-架構選型與務實偏離.md)、`specs/001-daily-triage-digest/`

### 真實 LLM 會把提示的格式鷹架字面吐出來

- **理論說**：提示裡寫「第一行＝一句定位，第二行＝一句為何值得看」，預期模型會照著
  產出乾淨的兩行內容。
- **實際發生**：接真實 OpenAI 格式後端一跑，模型把鷹架字面吐進內容——輸出變成
  「第一行＝這篇在談…」「為何值得看：第二行：…」，且**時有時無、不一致**，離線
  stub 測試完全抓不到（stub 不會這樣）。
- **解決方式**：雙重防線——(1) 提示改為明令「直接輸出，不要加任何標籤／編號」；
  (2) 解析端 `_clean_line` 剝除殘留前綴，且**標籤字須後接分隔符才剝**（否則會誤刪
  內容裡本就以「定位」開頭的正常句——這個 bug 我第一版就踩到）。
- **教訓**：格式化 LLM 輸出時，別只靠提示；解析端要有防禦性後處理。且「用 stub 測到
  綠燈」≠「真實模型行為正確」——**接真實後端後一定要再跑一次真的驗證**。
- **來源**：[history/004-真實後端改用-OpenAI-格式-API.md](history/004-真實後端改用-OpenAI-格式-API.md)、commit `50205bc`

### 外部後端失敗不該炸掉整個指令

- **理論說**：接了真實 OpenAI 格式後端，正常情況會回 embedding／摘要，指令順利完成。
- **實際發生**：驗證拉模式時，API 回 **403 `allocation_quarantined`**（額度因前面連續呼叫
  被暫時隔離）。`OpenAIError` 一路往上冒，把整個 `pull` 指令炸成 Python traceback——
  使用者看到的是堆疊而非人話（違反原則 V）。
- **解決方式**：在 CLI 層（`pull_cmd`／`digest_cmd`）攔截 `OpenAIError`，印出乾淨的繁中
  訊息＋退出碼 1，並提示「可稍後重試，或設 `KNOWFIELD_BACKEND=offline`」。
- **教訓**：任何**外部相依**（API、網路、限流）都會失敗；在**邊界**（CLI／進入點）
  攔成可行動的訊息，別讓底層例外冒到使用者面前。可插拔介面讓「切離線」成為現成的退路。
- **來源**：[history/008-拉模式完成.md](history/008-拉模式完成.md)、commit `223901a`

### 確定性 ≠ 直覺：離線 embedder 的雜湊碰撞

- **理論說**：離線 `HashingEmbedder` 是確定性的，共享詞元的字串（如「新聞」對「新聞…」）
  應有明顯正相關 cosine，測試相關性可放心用任意字串。
- **實際發生**：階段 5 寫測試時，`cosine("新聞 …", "新聞")` 竟得 **0.0**——短字串的少數詞元
  剛好與其他 token 雜湊**碰撞抵消**（空字串更是零向量）。兩個新測試假性失敗，乍看像 bug，
  其實是測試 fixture 選錯字串。
- **解決方式**：改用**經實測會匹配**的字串（如 "agent"，cosine≈0.28–0.45），別把相關性
  斷言建在未驗證的任意短字串上；必要時先跑一段小 script 量 cosine 再定 fixture。
- **教訓**：「確定性」不等於「行為符合直覺」。對這種近似元件，測試前先**實測它對你的
  fixture 的實際輸出**，別假設。這也是「用 stub 綠燈 ≠ 真實正確」的另一面。
- **延伸（RAG 真跑 T025，2026-07-24）**：同一個坑的第二面——**離線與真實 embedder 的 cosine
  尺度不可比**。離線 stub 測試全綠，但真實 `text-embedding-3-small` 的門檻 0.10 太低（命中≈0.6、
  無關問題也有 0.22），導致「查無」還列一堆來源。**任何靠 cosine 絕對值的門檻都得對實際
  embedder 校準**（改成依 backend 分流：openai≈0.30／離線≈0.05）。凡「離線可跑」的近似元件，
  上真實後端前務必**實測尺度**再定閾值——這種 bug stub 測試永遠抓不到，只有真跑抓得到。
- **延伸（先實測再斷言，2026-08-07）**：同一坑的第三面——**別對外部系統的行為想當然，先唯讀查真實資料／實測**。
  來源進料除錯時至少三次假設錯：「抽取器太弱」（其實重抓就補全）、「OCR 不支援 pages/回圖」（其實是 gateway 沒轉、
  改完就會）、「瀏覽器看到 raw 公式＝真 bug」（其實是 **stale SW 快取**假象）。每次都靠**唯讀查 `knowfield.db`／
  離線重跑正則／真跑 gateway** 才翻案。教訓：**動手修前，先讓真實資料／實測說話**——省下修錯方向的整輪工。
- **來源**：[history/010-可讀文章式消化完成.md](history/010-可讀文章式消化完成.md)、commit `3f82e03`；`history/021`（T025 真跑）、
  `specs/005-rag-qa/research.md` R5；`history/081`（先實測再斷言）

### 對外部 API 別在迴圈裡逐一呼叫——批次＋並行

- **理論說**：功能對就好；每個候選各自呼叫一次 embedding、每則各自呼叫一次消化，
  邏輯清楚。
- **實際發生**：web 主題深挖對數十上百個候選**逐一序列**呼叫 embedding API →
  **115 秒**，還被當成異常用量**觸發額度隔離**（403）。逐則消化又是 6 次序列 LLM 呼叫。
- **解決方式**：**批次**——embedding 改 `embed_many`，一次 API 呼叫嵌入多筆（115s→37s）；
  **並行**——消化用執行緒池同時打（I/O bound，as_completed，37s→~20s）。
- **教訓**：外部 API 呼叫**能批次就批次、能並行就並行**，別在迴圈裡逐一同步呼叫——不只
  慢（N 倍延遲），還可能被服務端當成濫用擋掉。這是延遲與穩定性的雙重地雷。
- **來源**：[history/015-web效能與即時進度.md](history/015-web效能與即時進度.md)、commits `fb7d6bd`、`416e476`

### 慢操作要給即時進度，而非假 spinner

- **理論說**：慢的請求加個轉圈圈 spinner 就好。
- **實際發生**：主題深挖要 ~20s，一開始只有計時器輪播的**假階段訊息**——使用者仍「不知道
  挖到哪」，因為那不是真進度。
- **解決方式**：改 **SSE 串流**——伺服器逐步推**真實**事件（抓到 N 候選→去重 M→排序→
  **每篇文章寫好即推**），前端邊收邊顯示；頁面秒開、結果一張張冒出。
- **教訓**：慢操作的體驗關鍵是**真實、增量的進度**（已知就顯示），不是假動畫。能串流就
  串流（SSE/分段回應），讓使用者看到系統確實在推進、推進到哪。
- **來源**：[history/015-web效能與即時進度.md](history/015-web效能與即時進度.md)、commit `60c4d72`

### 溯源／grounding 要在程式結構上保證，不靠模型自律

- **理論說**：要 RAG 答案「掛來源」，就在 prompt 裡叫模型「逐點標來源」，模型會照做。
- **實際發生**：做 RAG MVP（spec 005）時意識到——**模型會漏標、標錯、甚至編造來源**。若
  「答案有哪些來源」由模型自報，溯源（原則 3「無來源不出貨」）就建在流沙上。
  **實證背書**：2026 研究顯示商用模型**引用幻覺率 11–57%**、且「**正確 ≠ 忠實**」（模型會事後
  合理化引用）——所以「叫模型標來源」本質不可靠（見 [episodes/2026-07-23-競品與來源研究快照.md](episodes/2026-07-23-競品與來源研究快照.md)）。
- **解決方式**：**來源清單由程式端從『實際檢索到的條目集合』生成**，不是解析模型輸出。
  `RagService` 只把 top-k 檢索結果編號傳給 answerer，回傳的 `sources` 直接來自那個集合——
  模型就算漏標 `[n]`，來源仍真實、可回原文。查無相關時**程式端直接不呼叫合成後端**、不產
  任何內容。
- **教訓**：凡「可信度靠某個保證」（溯源、標籤、不杜撰），就**把保證做進程式結構**，別依賴
  模型自律。同型前例：AI 圖的「AI 示意・非原文」標籤由程式加（非模型）、根因萃取的候選要
  標「推斷＋掛證據」。**模型負責生成，程式負責守約。**
- **真跑再撞一次（2026-07-25，history/027）**：問「AI原理」→ 模型回「沒有相關材料」，程式卻
  照列 6 個來源（自相矛盾）。就是同一個坑的反面——**模型自報的訊號（『無材料』）也要程式
  『接住』**（偵測到就轉 no_material、不列來源），否則「模型說一套、程式做另一套」。附帶：
  模型會**因問題廣泛而過度拒答**，prompt 要明令「部分相關就作答、只有完全無關才說沒材料」。
- **再一面（2026-07-25，history/030）**：同一原則不限模型——**存取／唯讀邊界也要做進資料層，
  不靠 UI**。知識庫管理的「每日流唯讀」由 repo 方法 `WHERE digest_id=種子容器` 結構保證
  （傳流的 id→不動作），而非「UI 不顯示刪除鈕」——後者一個手工 POST 就繞過。**保證做進
  最底層，別靠上層（模型／UI）自律。**
- **再一面（2026-08-07，history/083）**：同一原則到了「知識的**輸出**」端——**生成文章的 References 由程式從
  佐證 `evidence_urls` 結構化組**（`_references`），LLM 只寫正文、不碰引用清單。輸入端（RAG 溯源）與輸出端
  （文章 references）是**同一保證的兩端**：可信度做進程式結構，不論資料流向哪邊都不靠模型自律。
- **來源**：[history/021-RAG-MVP-實作完成.md](history/021-RAG-MVP-實作完成.md)、`specs/005-rag-qa/research.md` R4、
  [concepts/有吸引子的場.md](concepts/有吸引子的場.md)（根因萃取紀律）；[history/030-知識庫管理完成.md](history/030-知識庫管理完成.md)（唯讀邊界）

### 擴充已出貨功能時，選「免動既有資料結構」的設計，即使略醜

- **理論說**：加種子這種新語料，乾淨做法是開一張獨立的 `seeds` 表，語義清爽。
- **實際發生**：增量 1 的 `entry_embeddings` 主鍵是 `digest_entries.id`；獨立 `seeds` 表的 id
  會**跟它撞**，得改**已出貨**的嵌入表結構（加 kind 欄或改鍵）——侵入正在用的功能、要遷移。
- **解決方式**：讓種子存進**哨兵「種子容器」digest**、復用 `digest_entries` → `entry_id` 仍
  唯一 → **嵌入表免動、免遷移**。語義略醜（種子假裝成 digest），以哨兵 date＋`--today` 排除緩解。
- **教訓**：擴充已出貨功能時，**遷移風險 > 優雅**——優先選不動既有資料結構/不改已出貨表的
  設計，把「醜但隔離」的成本記在明處（research/history），勝過「漂亮但要動全表」的風險。
- **來源**：[history/025-種子ingest完成.md](history/025-種子ingest完成.md)、`specs/006-seed-ingest/research.md` R1

### 把品質準則「列成檢查表」給 AI ≠ 讓 AI 追求品質

- **理論說**：根因萃取提示裡把 7 條試金石（含「追問不撞牆」）列清楚，模型就會照著挖到底、
  給出有本質的深 why。
- **實際發生**：模型把試金石當**通過即可的檢查表**，給一個過得去的**表面 why** 就交卷——
  使用者回饋「本質的 why 太少，都是表面的，缺 aha」。準則列了，品質沒到。
- **解決方式**：把「**追求那個品質**」本身設成任務——提示改成「目標＝挖到 bedrock、逼出 aha；
  **遞迴追問**『那這個又為什麼』到撞原始層，**不到底不准停**；看到霧詞＝再挖一層」。並讓深度
  **可視化**（why 階梯，表面→bedrock）＋**可誠實放棄**（挖不到回 no_material，勝過編假深洞見）。
- **教訓**：對 AI 生成，**列品質準則 ≠ 得到品質**——準則只是判準，模型會找「剛好通過」的最省解。
  要品質，就把**追求品質的動作**寫成任務本身（遞迴/往下挖/不到底不停），並讓產物**顯示自己的
  深度**（可視化）＋**允許誠實承認不足**（no_material）。通用於整理／消化／擴展等各 AI 生成步驟。
- **來源**：[history/041-根因萃取挖到bedrock.md](history/041-根因萃取挖到bedrock.md)、[concepts/有吸引子的場.md](concepts/有吸引子的場.md)（試金石 6、aha）

### 聚合混來源資料，比較前先正規化

- **理論說**：把各來源抓到的條目丟進去重／排序，挑「發布時間最早」的當代表，`min(...)` 就好。
- **實際發生**：`published_at` 混時區——atom/arxiv 用 `fromisoformat`→**aware**、rss/None/web→**naive**；
  同一去重群組混到 aware＋naive，`min()` 拋 `can't compare offset-naive and offset-aware datetimes`，
  **整份 digest 崩**、重整整個失敗（使用者看到的還是舊匯整）。
- **解決方式**：排序鍵**正規化**——aware `astimezone(utc).replace(tzinfo=None)` 轉 naive UTC 再比。
- **教訓**：跨來源的資料**型別/格式各異**（時區有無、None、單位、大小寫）；任何 `min/max/sort/==`
  前**先統一正規化**，否則邊界一混就炸。是「餵進最底層前先歸一」的家族。
- **來源**：[history/048-web活水news模式與時區崩潰修正.md](history/048-web活水news模式與時區崩潰修正.md)、`digest/builder.py` `_canonical`

### 批次聚合：單一項的例外不該拖垮整批

- **理論說**：`build` 逐來源抓取，攔 `SourceUnavailable` 就夠——來源掛了標缺漏、繼續。
- **實際發生**：攔截**只抓預期的一種**（`SourceUnavailable`）。一個來源丟出**非預期**例外
  （上例的 `TypeError`）就漏網、往上炸掉整份 digest——一個壞來源拖垮全部。
- **解決方式**：per-來源攔截**加寬**——除了友善的 `SourceUnavailable`，再加一個 `except Exception`
  → 標缺漏、記錄、繼續。壞一個跳一個，不崩整批。
- **教訓**：批次聚合／扇出的**例外攔截面要夠寬**——別只攔「你想到的那種」。**單點失敗隔離**：
  一個壞項→標記跳過，整批照常。（教訓 3「邊界攔外部失敗」的延伸：邊界要攔**所有**失敗，不只一種。）
- **復發於串流路徑（2026-08-14，`history/092`）**：同一形狀第二次——`_post_stream` 的 `try` 只包住
  `urlopen`，**迭代 `for raw in resp` 在 try 外面**；`_stream_gen` 又只攔 `(SourceUnavailable, OpenAIError)`。
  於是中途斷線（`IncompleteRead`／`reset`／timeout）**裸奔穿出去**，SSE 連 `error` 事件都送不出 →
  使用者看到的是「回答講到一半停住、沒有任何提示」。修：迭代包進 try 收成 `OpenAIError`＋外層 except 加寬。
  **提醒：這條每次都是在「新的扇出/串流路徑」上重犯**——寫新的邊界時把「攔截面夠不夠寬」當 checklist 一項。
- **來源**：[history/048-web活水news模式與時區崩潰修正.md](history/048-web活水news模式與時區崩潰修正.md)、`digest/builder.py` build 迴圈；
  `history/092`、`backends/openai_api.py _post_stream`（串流版復發）

### 提案-批准迴圈 ≠ 打到需求（被設計優雅與流程順暢牽著走）

- **理論說**：跟著漂亮的母概念（`有吸引子的場`）＋順暢的 spec-kit 節奏走，一輪輪 specify→
  plan→tasks→implement，使用者每次都說「好／都加／做完」——看起來方向對、在穩定交付。
- **實際發生**：20 個 spec 之後，使用者說「這工具好像沒幫到我，需求都沒被打到」，且坦承**從沒
  自己打開過它**。回頭看：我一直**提案、他一直批准**，但「點頭同意」≠「需求被打到」。我被
  「和概念自洽」＋「流程有進度」的雙重舒適牽著走，一路加**下游 KB 深度**（場、關聯、根因、來源
  推薦），卻沒先證明上游最基本的價值——結果全是沒人去開的目的地。
- **解決方式**：停止提功能，**從使用者真實的一天重新推導**——問「上週你自己開過它嗎」（答：沒有）、
  「哪個時刻你會冒出需求」。一問就定位到真需求（當下、手機、反逢迎），且判明先前方向是被優雅
  帶偏。捕捉成 re-route draft（[draft/2026-07-28-反逢迎的當下副手.md](draft/2026-07-28-反逢迎的當下副手.md)）。
- **教訓**：**「使用者批准提案」是最危險的偽訊號**——它讓「自洽且有進度」冒充「有用」。真正的
  驗收訊號只有一個：**使用者會不會在沒人要求時，自己伸手用它。** 每隔幾輪就用它校準；**別讓
  設計的優雅或流程的順暢代替「打到需求」的證據**。動手前先問「這改變使用者真實動線裡的什麼」。
- **如何應用（解方，2026-07-28 實證）**：上面是**診斷**；解方是**大賭注/轉向前，先跑最便宜的手動
  探針驗「真實有用」，過了才 promote＋蓋**。做法：把功能拆成獨立的賭注（如「這份綜合有沒有幫到你」
  vs「你會不會在手機上伸手」），**用手動 dry-run 驗最核心那個賭注、零 build**——探針同時會**撞出
  真實實作風險**（光靠批准永遠不會浮現）。實證：spec 021 前對 Claude Opus 5 手動跑一次「反逢迎綜合」，
  使用者答「有用」＋撞出兩個真風險（伺服器抓被擋、query 品質），**才** promote。手動探針＝可執行規格。
  接上 principle 6（過度擬合檢查：蓋最小一塊撞現實）。出處：`history/056`/`057`、[draft/2026-07-28-反逢迎的當下副手.md](draft/2026-07-28-反逢迎的當下副手.md)。
- **來源**：[draft/2026-07-28-反逢迎的當下副手.md](draft/2026-07-28-反逢迎的當下副手.md)（2026-07-28 診斷對話）

### 內部概念詞彙別洩漏到產品對外表面

- **理論說**：把 knowie／concept 的詞彙（場、冊封、根因、grounded、derived、場-增量）直接用在使用者
  對話與介面裡——覺得精準、自洽、與整個專案模型一致。
- **實際發生**：使用者實際開 `/chat` 用了，回饋「別一直口口聲聲說『冊封』，不了解這專案的人會覺得
  莫名其妙」。而且**測試全綠、我自己審 code＋看真後端輸出時也沒察覺**——因為我泡在專案詞彙裡、
  對它免疫了。
- **解決方式**：把「**內部模型詞彙**」和「**產品對外語言**」分開——system prompt 明令輸出用自然口語、
  不用內部術語、不貼「**grounded**」這種標籤；**行為一字不改**（分把握程度／分「能證明-觀察到-只是類比」
  ／攔過度抽象／建議存但人決定），只換表達。使用者可見文案（導覽、按鈕、提示）同步 de-jargon。
- **教訓**：概念詞彙是給**模型／內部設計**用的，**不是給使用者看的**；對外表面一律用一般人看得懂的話。
  且「詞彙洩漏」這種瑕疵**我對它免疫（泡在裡面）、測試也照不出**——**只有真實使用者的眼睛照得出來**。
  這是「提案-批准 ≠ 打到需求」的解方在 UX 上的延伸：**真實使用才是驗收**，連用詞都一樣。
- **深化（2026-08-03，第二次冷讀）**：這不只是「黑話」，是**「路徑依賴的語言」——任何「只因你走過那段
  討論才覺得自然」的詞**（根因/由來/落點/佐證/暫存…）。使用者：「不是有經過我們的討論，會覺得這詞怪怪的、
  彆扭、不是平常會說的。」**免疫比想的更深**——連我（AI）都把「佐證」自評成「勉強可留」，是**使用者再冷讀
  一次才抓到**。→ 兩個更硬的點：(1) **修法＝內外分家**：`knowledge/` 概念名＋碼（`why_nodes`/`anoint`）**不動**，
  只改**使用者看得到的字**（根因→核心理解、冊封→精選、佐證→來源）——**不是把概念改笨，是內外分層**；
  (2) **建造者（含 AI）無法自審「自不自然」**，必得靠一雙**沒被討論汙染的眼睛冷讀**——所以流程上要**主動找
  使用者冷讀產品表面**，別等自己發現（自己永遠不會）。
- **來源**：commit `0583b65`、`specs/022-field-chat/`（spec 022 de-jargon，2026-07-29）；根因→核心理解/佐證→來源
  re-language（2026-08-03，使用者冷讀回饋）。

### 引擎好 ≠ 收料好（產物累積面的漏）

- **理論說**：`/chat`（引擎）能幫使用者深長推導、逐段冊封根因；spec 023 的存檔（收料）人閘門、選擇性、
  預設流走——測試全綠、單輪流程都對，收料應該也沒問題。
- **實際發生**：審使用者自己存的 19 段對話／24 根因照出兩個漏——① 同一段對話因「每冊封一條就連同存一次」
  被**複製了 15 份**（打臉「不囤積」）；② 根因收到第 32 句就停、對話卻聊到第 70 句，**最原創的尾段沒被蒸餾**。
  引擎面很好（15 條根因是真蒸餾），漏全在**下游收料**：去重／收尾／命名跟不上，金子被埋。
- **解決方式**：#1 `save_conversation` 依內容指紋冪等（同段回既有、連結改存 why_node 側，多對一）；
  #2 純函式 `distill_gap`＋只提醒不自動收的「尾段未收」提醒。（#3 命名/章節切分待議。）
- **教訓**：「單輪功能對」＋「測試綠」**不保證產物累積衛生**——**跨多次操作、人閘門之後的累積**（重複、
  半途而廢、命名凍住）是另一層，測試與單輪流程都照不出。是「真實使用才是驗收」在**產物累積面**的新投影。
  **反囤積要在每個收料層都做**：後來「整理→精選」也復發同構的重複（候選沒去重→收進同一條兩則），解＝
  正規化去重的**縱深防禦三道**（解析同次去重／整理標「已收」／精選冪等），最後一道保證庫裡不重複（`history/070`）。
- **來源**：[history/064-對話收料的漏完成.md](history/064-對話收料的漏完成.md)、`specs/025-capture-hygiene/`、commit `d0d8ec4`；復發＝`history/070`。

### 要停留在脈絡的操作，別用整頁重載（原地 AJAX）

- **理論說**：整理是 POST→回一頁；只要把對話內容一起帶回、重新渲染，使用者就在原地，沒差。
- **實際發生**：使用者說「整理完會跳回主頁」。其實對話內容有保留（messages=hist），但**整頁重載**讓卷軸彈回頂、
  串流狀態重建、候選出現在別的位置——**體感上就是被丟出正在聊的脈絡**。「內容有保留」≠「脈絡有保留」。
- **解決方式**：整條整理→精選改**原地 AJAX**（`?as=json` 回資料、client 就地渲染），全程不 navigate；無 JS 才走
  `<noscript>` 整頁 fallback。
- **教訓**：**要讓人停在當下脈絡的操作，別用整頁重載**——即使資料無損，scroll/焦點/動態狀態的重建本身就是
  「跳走」的體感。這是「真實使用才是驗收」又一次：只有真的在聊的人，才分得出「內容保留」和「脈絡保留」的差別。
- **來源**：[history/070-整理去重與原地精選.md](history/070-整理去重與原地精選.md)；使用者 2026-08-04「收完會跳回主頁，我不要這樣」。

### 主動審計既有產物 ＝ 最強的照妖鏡（別只等「用一用照出」）

- **理論說**：真實使用會照出瑕疵——所以讓使用者去用、等他回饋就好。
- **實際發生**：上面兩個漏**不是**使用者「用一用」自己撞出來的，是**使用者請 AI 去讀他存的東西**、
  我把 19 段對話／24 根因攤成數據（每段輪數、問句序列、根因落點）才照出的。被動等回饋，這種**累積型**
  的漏（囤積、收尾缺口）使用者自己很難察覺——它不在「當下這一步」，在「累了很多步之後的全貌」。
- **解決方式**：把「**主動審計既有產物**」當成一種例行——像 `knowie-judge` 之於知識庫那樣，對**使用者的場**
  也定期攤數據體檢（長度分佈、重複、收尾缺口、命名鮮度）。
- **教訓**：真實使用是照妖鏡，但**被動等回饋只照得出「當下步」的瑕疵**；**累積型**的漏要靠**主動攤開既有
  產物的數據**才照得出。審計 ≠ 等回饋，是另一種、且更早的照法。
- **來源**：[history/064-對話收料的漏完成.md](history/064-對話收料的漏完成.md)；使用者 2026-07-29「你去看看我現在聊的」審計請求。

### 「每個 X 都不一樣」→ 先找「X 藏在少數標準載體裡」（按載體不按實例）

- **理論說**：不同網站的內容/數學格式各異，要支援就得**一站一個處理器**（知乎、Medium、Wikipedia…）＝無限跑步機。
- **實際發生**：修「貼上知乎缺數學」時，讀真剪貼簿發現——**LaTeX 好端端在複製的 HTML 裡，只是躲在標準載體之一**：
  `data-tex`（知乎）、`<annotation encoding="application/x-tex">`（KaTeX/MathML 標準）、`<script type="math/tex">`
  （MathJax v2）、公式圖 `alt`/`?tex=`（Wikipedia 等）。**全網就那四五種載體，不是無限種。** 圖片（懶載 `data-*`）、
  雜訊（`nav`/`footer`/`aria-hidden`）同理——都是標準 pattern。
- **解決方式**：**按「標準載體」抽取、不按站**（一次寫、covers 大半個網路）；長尾（奇葩站）交給**兩層 fallback**：
  LLM 清理 ＋ 貼上框手動編輯。三層架構會收斂，不隨網站數膨脹。
- **教訓**：碰到「每個 X 都不一樣、要不要一個一個處理」時，先問「**X 是不是其實藏在少數標準載體裡？**」——
  按載體抽取，把「無限特例」收斂成「有限泛化」；再配 fallback 接長尾，避免 per-instance 跑步機（過度擬合檢查）。
  這也是**真實使用才驗收**（真貼知乎才照出數學躲在 `data-tex`）＋收斂紀律的合流。
- **來源**：[history/072-來源管理與rich-paste完成.md](history/072-來源管理與rich-paste完成.md)（數學缺字修復串）；`src/knowfield/ingest/web.py` 的載體抽取；
  使用者 2026-08-04「這件事可以泛化嗎？」。

### 進料層真正的 job：接住脈絡讓人敢關分頁（不是建語料）——捕捉品質＝敢放下的信任

- **理論說**：收進＝為 `/chat` 建 RAG 語料；把內容存好就對了。
- **實際發生**：使用者揭露真動機——**「看到好網站就開很多分頁、不敢關，因為那有我的脈絡；要記錄下來才敢關。」**
  分頁＝外部化的脈絡；不敢關＝怕脈絡消失。工具真正的 job 是「**一個可信的地方，讓人卸下脈絡、放心關分頁**」，
  不是多一個文章庫。這也回頭解釋：前面拼命修數學/圖片/標題/去框**不是龜毛，是在建「信任」**——捕捉品質爛→
  不敢放手→繼續囤分頁。**收進原因/脈絡欄因此是皇冠、不是選填配角**（我一度誤判成 gold-plating，修正）。
- **解決方式**：① 摩擦極小（一次關十個分頁→每個「一鍵＋一句 why」；剪貼簿一鍵、自動標題/日期都服務此）。
  ② **反囤積界線**：別把「囤分頁」變成「囤知識庫」（無底收件匣＝把囤積搬家、底層焦慮沒解）。用產品自身哲學
  （captured≠committed／預設流走，spec 028 暫存衰減同精神）：**解脫在「放下」不在「永久典藏」——安心卸載＋
  誠實衰減**（大多流走是健康的，真回頭用到的才複利）。
- **教訓**：問「工具的 job 是什麼」比「功能對不對」更根本——**進料層的 job 是接住脈絡讓人敢放手，不是建語料**。
  由此：(a) 捕捉品質是「敢放下的信任前提」，值得投資到「夠信任」為止（非無限打磨）；(b) 幫人放下囤積的工具，
  自己**絕不能變成新的囤積**——要配「預設流走」。呼應原則 6 反囤積、「有吸引子的場」預設衰減。
- **來源**：使用者 2026-08-04「不敢關分頁是因為那有我的脈絡…這工具剛好適合做這件事」；[episodes：進料轉檔選型的實測](episodes/2026-08-04-進料轉檔選型的實測.md)。

### 有結構化來源時，解析 > 辨識

- **理論說**：論文進料走 Mistral OCR（PDF→markdown）一次搞定，統一路徑最省事。
- **實際發生**：arxiv OCR 版數學是「看圖辨識 LaTeX」，密集下標/符號會猜錯；且 OCR 端點連圖都吐不出來。
  改走 **arxiv HTML 版**——它的數學是**作者原始 LaTeX（source）**、figure 是真 `<img>`，一次全對。
- **教訓**：**有結構化來源（HTML/source/API 欄位）就別讓 AI 去「辨識」**——解析是確定性、可重現；辨識再準也是猜，
  對承重內容（數學/符號/程式碼）尤其。取捨順序：**結構化 source > 解析 > 辨識**；辨識只在拿不到 source 時（掃描件、無 HTML 版）當退路。→ arxiv 定 `HTML → ar5iv → PDF-OCR` fallback（`history/081`）。
- **來源**：[history/081-來源進料品質-arxiv路線與數學圖標題.md](history/081-來源進料品質-arxiv路線與數學圖標題.md)；commits `f3e3fa9`／`0839609`。

### 確定性修承重內容 > 丟 AI「整理」

- **理論說**：來源結構有雜訊（標題分錯層次、隱形字元）→ 仿「貼上」丟給 AI 整理最省力。
- **實際發生**：貼上的 `clean_markdown` 鐵律本就是「只剝雜訊、逐字保留、不改寫」（守溯源）；但對 URL/PDF ①已結構抽取過、
  效益邊際 ②**AI 碰密集 LaTeX 極易砸壞剛用四層 bug 修好的數學**（承重內容）③成本＋損溯源。
- **教訓**：**結構性問題用確定性規則修**（標題層次重映射、清控制字元），**別把承重內容交給 AI「整理」**——
  AI「逐字」對數學是最弱保證、卻是最要命的地方。呼應反逢迎的膜（AI 不亂改冊封物）＋捕捉鐵律。AI 只在**真髒的貼上頁**選用清理（預設關）。
- **來源**：`history/081`；commit `70a5d8d`；[episodes：進料轉檔選型的實測](episodes/2026-08-04-進料轉檔選型的實測.md)（貼上 clean 鐵律）。

### CJK 輸入法組字中的 Enter 不該送出（isComposing）

- **理論說**：Enter-送出的輸入框，`onKeyDown` 判 `e.key === "Enter"` 就送，直覺又乾淨。
- **實際發生**：中文（日文/韓文同理）輸入法**組字中**按 Enter 是**確認選字**，卻被當成送出——使用者選字選到一半、
  文章主題還沒打完就被送出去生成。這對非 CJK 開發直覺是隱形的坑（英文沒有組字階段）。
- **解決方式**：`onKeyDown` 加 `!e.nativeEvent.isComposing`（＋`e.keyCode !== 229` 給舊瀏覽器 fallback）——
  **組字中的 Enter 一律放行給輸入法、不 preventDefault、不觸發送出**；組字結束後的 Enter 才送。
- **教訓**：**任何 Enter-送出的輸入框，CJK app 必須擋 `isComposing`**——這是會反覆重犯的坑（每新增一個輸入框
  就可能漏，這次一次就漏了聊天送訊＋文章主題兩處）。keep 測試：AI 會不會默默重犯而沒人察覺？會 → 立規。
  一般化：**針對母語使用者的輸入互動，別用英文鍵盤模型想當然**（同型：組字、全形標點、輸入法候選窗遮擋）。
- **第二個實例＋一般化再擴一圈（2026-08-09，commit `9c3352e`）**：這條當初寫的「同型：全形標點」**被撞中了**——
  中文粗體 `**…**` 露星號，因為 CommonMark 的 flanking 規則判斷 `**` 能否閉合時，全形句號被當 punctuation、
  而中文不用空格分詞 → 條件不成立 → 星號原樣吐出。**盲區不只在「輸入互動」，在任何以英文書寫慣例為預設的
  規範／解析器**：斷詞、`\b` 正則、emphasis flanking、截斷字數、換行斷點。修法＝**別跟規範辯論，抽佔位繞過它**
  （`@@B` 佔位 → marked → 還原 `<strong>`；數學 `@@M` 最後還原，故粗體含數學也不壞）。
  附帶：**這條教訓事先點名的坑真的發生了 ⇒ 它有預測力**（原則 6 的過度擬合檢查在教訓自身上通過）。出處：`history/091`。
- **來源**：[history/083-知識的輸出-高證實文章生成第一刀.md](history/083-知識的輸出-高證實文章生成第一刀.md)（順修）；commit `efe08f7`；
  `frontend/src/ChatPage.tsx`、`frontend/src/pages/RootsPage.tsx`；`history/091`、commit `9c3352e`（全形標點）。

### 驗證 DB 寫入要跨連線——單連線 smoke 會假綠

- **理論說**：把資料層從 SQLite 遷到 PG，寫個 smoke test 建 Repository、塞資料、讀回來，過了就代表 port 對。
- **實際發生**：port 時把 `add_why_node`/`save_article` 收斂成 `return self._insert_id(...)`，**不小心把
  `commit()` 掉了**。smoke test 在**同一個連線**塞完立刻讀——PG 未提交的資料在**同連線內可見**，所以 smoke 全綠、
  假象「port 正確」。真正的 API 場景是「請求端連線寫、另一端連線讀」，跨連線讀不到未提交資料 → 候選數 0，整套測試才露餡。
- **解決方式**：port 後**跑完整測試套件**（它天然跨連線：API 一個 Repository 寫、測試另開 Repository 讀）；
  smoke test 不足以驗 DB 層。補回 `add_why_node`/`save_article` 的 `commit()`。
- **教訓**：**PG（非 autocommit）每個寫入路徑都要顯式 commit**；而**驗證寫入必須跨連線**——單連線的「塞了又讀到」
  是最會騙人的假綠（未提交也讀得到）。呼應本專案骨幹「答案對到 ground truth、別信自報的綠」。
- **相關**：與「驗證要走與正式路徑完全相同的管線」同族——那條是管線步驟差異造成**假紅**，這條是連線邊界差異造成**假綠**。
- **來源**：[history/084-全部PG-資料層遷移定案.md](history/084-全部PG-資料層遷移定案.md)；commit `e7dcbec`；`specs/034-postgres-migration/`。

### 具體路由要註冊在 catch-all mount 之前，否則被吃掉

- **理論說**：加登入的 `/auth/login|callback` 路由，寫在 `create_app` 裡就會生效。
- **實際發生**：SPA 用 `app.mount("/", …)` 當 catch-all（非檔案 fallback index.html）。新 `/auth/*` 路由註冊在**它之後**
  → 「/」catch-all 先比對到、把 `/auth/callback` 也吃掉 → callback 沒跑、session 沒建。詭異的是**擋人有效、登入無效**
  （gate middleware 在路由層之前所以擋得住，但要跑的 /auth 路由被 mount 遮蔽）→ 測試現象「登入後仍 401、Set-Cookie 空」。
- **解決方式**：把 `/auth` 路由（`setup_auth`）移到 SPA `mount("/")` **之前**。
- **教訓**：**catch-all（`mount("/")`／萬用路由）永遠放最後**；任何具體路由都要在它之前註冊，否則靜默被吞。
  debug 訊號：「middleware 攔截正常、但具體 handler 沒被呼叫」≈ 路由被更早的 catch-all 比對走了。
- **來源**：[history/085-單人Google登入門鎖.md](history/085-單人Google登入門鎖.md)；commit `d11b5e0`；`src/knowfield/web/app.py`（setup_auth 順序）。

### 加全域攔截但不驚動既有測試——用「設定存在性」當開關、預設關

- **理論說**：加登入門鎖＝全域攔截所有請求，勢必要改一堆既有測試（讓它們登入）。
- **實際發生**：既有 344 測都走 `build_app`；若 gate 預設啟用，全部會被擋 → 得改 344 處。
- **解決方式**：**auth 只在「allowlist＋憑證都設了」時才掛 gate**（`auth_active`）。既有測試沒設這些 env → 全開 → 零回歸、
  一處不動。新功能測試才設 env 啟用。＋dev bypass 開關防設錯鎖死。
- **教訓**：**新增全域行為時，用「設定存在性」當啟用開關、預設關**——既有路徑不受驚動、遷移面積趨零。同型於「把重量級
  相依藏在窄介面後、預設離線」：預設安全/無感，opt-in 才啟用。
- **來源**：[history/085-單人Google登入門鎖.md](history/085-單人Google登入門鎖.md)；commit `d11b5e0`；`src/knowfield/web/auth.py`（auth_active）。

### 測試不能讀開發者本機 .env——會撈到真實設定、汙染測試

- **理論說**：`Config.from_env` 讀 `.env` 很方便，測試也用它建 app 就好。
- **實際發生**：使用者填好 `.env`（含真實 Google 登入 auth 設定）後，跑測試——`from_env` 的 `load_dotenv(".env")`
  把 auth 設定撈進來 → **門鎖被啟用 → 全部 web 測試被擋**（POST 401→讀空 IndexError、autosave 無 temp_id KeyError、
  export 302 登入 redirect loop）。**一度被誤判成剛做的 SQLite 遷移 parity 問題**——實為測試讀了真實 .env（同一份碼在
  .env 沒 auth 時全綠、有 auth 時全紅）。
- **解決方式**：`tests/conftest.py`（session 起點）設 `KNOWFIELD_NO_DOTENV=1`＋清 auth/DB 環境變數；`Config.from_env`
  尊重旗標、不讀 .env。測試從乾淨、確定的環境開始。
- **教訓**：**測試必須 hermetic——不依賴開發者本機 `.env`／shell 環境**。設定載入要有「測試模式跳過」的旗標。
  debug 訊號：「同一份碼昨天全綠、今天全紅，程式沒改」→ 先查**環境/設定**變了什麼（這次是 .env 被填了），別一頭栽進「昨天改的功能」。
- **來源**：[history/086-可攜資料層雙後端-reroute不騎牆.md](history/086-可攜資料層雙後端-reroute不騎牆.md)；commit `326469c`；`tests/conftest.py`、`src/knowfield/config.py`。

### 後端處理的路由前綴，都要同步加進 PWA SW 的 navigateFallbackDenylist

- **理論說**：加個後端路由（`/media/*` PDF、`/auth/login|logout`）就會生效。
- **實際發生（**踩了兩次**）**：PWA service worker 的 SPA navigateFallback 會把**導航請求**攔下、回快取的 index.html，
  於是那些路由**根本沒打到伺服器**——① `history/082` 的 `/media/*.pdf`（PDF 預覽變成聊天頁）；② `history/085` 的
  `/auth/logout`（點登出沒反應，session 沒清）。症狀很誤導：「後端路由明明對、但瀏覽器行為像沒生效」。
- **解決方式**：把該前綴加進 vite PWA 的 `navigateFallbackDenylist`（`/^\/media\//`、`/^\/auth\//`…）——讓那些導航直達後端。
- **教訓**：**凡是後端要親自處理的路由前綴（非 SPA client route），一律同步登記進 SW 的 navigateFallbackDenylist**。
  加新前綴（`/webhook`、`/oauth`、下載…）時把「更新 SW denylist」當 checklist 一項。debug 訊號：「後端路由對、但 PWA 下
  行為像被吃掉」≈ SW navigateFallback 攔了它。（也呼應：改版後**先清 SW** 再驗，否則 stale 快取會給假象。）
- **來源**：`history/082`（/media）、`history/085`（/auth）；commits `effde0b`、`87300c0`；`frontend/vite.config.ts`。

### prod 部署是另一個環境——這些差異本地/測試照不到，逐一驗

- **理論說**：本地跑得起、測試全綠，push 上去就會動。
- **實際發生（首次上 k3s 一次踩四個，`history/087`）**：全部是**上 prod 才炸、本地/測試看不到**的環境差異——
  ① **映像平台**：Mac build 出 arm64、節點 amd64 → `no match for platform`。② **執行期 vs 測試相依**：`httpx` 只在 test
  extra，但 Authlib 執行期要它 → **本地測綠（測環境剛好有）、prod crash**。③ **反向代理**：Caddy 後面沒 `--proxy-headers`
  → OAuth redirect_uri 組成內部 http → `redirect_uri_mismatch`。④ **惰性初始化**：`init_db` 首次碰 DB 才建 schema
  → 遷移/探針時撲空。
- **解決方式**：各自對症（`buildx --platform`、httpx 進 runtime deps、`--proxy-headers`、遷移前先 init_db）。
- **教訓**：**「本地綠 ≠ prod 綠」，部署是新環境，差異要當 checklist 逐項實測**——
  (a) **映像平台**對上節點架構；(b) **執行期相依**別靠測試環境剛好有（庫的 runtime import 要進 runtime deps，不是 dev extra）；
  (c) **反向代理**：scheme/host 靠 `--proxy-headers`＋Host 透傳，OAuth/絕對 URL 才對；(d) **惰性初始化**：schema/種子若
  lazy，遷移或探針要先觸發。呼應「別信自報的綠」——**測綠只證本地那個環境**。
- **來源**：[history/087-部署上線-k3s-ghcr-網域.md](history/087-部署上線-k3s-ghcr-網域.md)；commits `232bc23`、`2d2581b`；`Dockerfile`、`pyproject.toml`、`deploy/`。

### upsert 綁「實體 id」，別綁「狀態旗標」

- **理論說**：autosave 只該碰暫存對話（保護人存的永久對話不被自動改），所以 `UPDATE … WHERE id=? AND temporary=1`。
- **實際發生**：接回**已存檔（永久）**對話繼續聊 → 那句 UPDATE 因 `temporary=1` 不成立 → 落到 INSERT → **另開一筆暫存**
  （＝「繼續聊跑到暫存」「promote 後又冒暫存」的同一根因）。狀態旗標把「同一段對話」的 upsert 給拆了。
- **解決方式**：upsert 以**實體 id** 為鍵（`WHERE id=?`）、就地更新、保留其狀態；只有「查無 id」才新建。
- **教訓**：**upsert/更新要綁不變的實體識別（id），別綁會變的狀態旗標**——否則狀態一翻，同一實體就分岔成兩筆。
  想「保護永久不被亂改」要用權限/來源區分，不是把 upsert 的鍵綁在狀態上。
- **來源**：`history/088`；commit `7a97b48`；`repository.autosave_temporary`、`ChatPage.tsx`。

### 前端互動：桌面開發的直覺照不到手機/真實使用

- **理論說**：桌面瀏覽器點一點沒問題就是好了。
- **實際發生（上線後一次踩三個，`history/088`）**：① **hover-gated 控制項在手機隱形**——⋮ 用 `opacity-0 group-hover`，
  手機無 hover → 看不到/點不到。② **下拉選單被 overflow 容器裁切**——選單 `absolute` 在 `overflow-y-auto` 裡，靠底時被切掉、
  「點不下去」。③ **送出訊息沒即時回饋**——使用者那句等回答完才出現（樂觀更新缺失）。
- **解決方式**：① 觸控裝置控制項**常駐可見**＋**長按**當替代手勢；② 彈出層 **portal 到 body＋fixed 定位**（脫離 overflow 容器）、
  靠邊自動翻向；③ **樂觀更新**：使用者動作當下就反映到畫面。
- **教訓**：**桌面點得動 ≠ 手機/真實使用可用**。互動要按「無 hover、有捲動容器、要即時回饋」的真實條件驗——
  最好在**手機實機**過一遍。呼應「prod 是另一個環境」：**手機也是另一個環境**。
- **來源**：`history/088`；`components/ConvMenu.tsx`（portal）、`ConversationSidebar.tsx`（長按/常駐）、`ChatPage.tsx`（樂觀送出）。

### 上線後維運：遷移要含 DB 以外的檔案；別在使用者線上用時部署

- **理論說**：把 DB 遷過去、服務起來就完成上線了。
- **實際發生（`history/087`/`088`）**：① 只遷 DB、**漏搬 media/（圖片、PDF）** → 使用者看到「資訊不見了」（破圖/無預覽）。
  ② 為修 bug 在**使用者正線上使用時 rollout** → in-flight 請求可能掉（他按「轉永久」沒生效的疑點之一）。
- **解決方式**：① 遷移清單要含**所有持久化來源**（DB＋media/PVC＋任何上傳檔），不只資料庫；② 部署挑使用者沒在用的空檔，
  或用不中斷的滾動策略。
- **教訓**：**「資料」不只在 DB**——附件/媒體/檔案是遷移的一等公民，漏了就是使用者眼中的資料遺失。**部署有使用者成本**，
  尤其單人即時使用的服務，別在他用到一半時重啟。
- **來源**：`history/087`（遷移）、`history/088`（media 補搬、live rollout）；`deploy/README`。

### 部署要核對「pod 實際 digest」——別信「rollout 成功」（push 會靜默失敗）

- **理論說**：改碼→`buildx --push`→`rollout restart`→`healthz 200`，每步成功就是上線了。
- **實際發生（燒掉一小時，`history/089`）**：連續數次全綠，但線上**完全沒更新**——`docker login ghcr.io` 的 **token 過期**，
  `buildx --push` 於是 **build 成功、push 靜默失敗**（結尾無 `pushing manifest done`，但整體 exit 0 不明顯），
  rollout 每次拉到**同一個舊 digest**、還回報「successfully rolled out」。一路被騙，以為是各種功能 bug。
- **解決方式**：① 部署後**核對 `kubectl get pod -o jsonpath=…imageID` 的 digest ＝ 這次 build 的 digest**（或在 pod 內
  驗一個新碼才有的符號）；② 用 **git sha 當唯一不可變 tag**（`:latest` 被覆蓋看不出新舊、還可能拉到 stale）；
  ③ token 會過期 → 部署前重登 registry。
- **教訓**：**CD 每一步的「成功」都可能是假的——要驗『線上實際跑的版本』這個 ground truth，不是驗『我以為推了』**。
  尤其 `latest` tag＋mutable registry＋快取，三重不透明。呼應「別信自報的綠」的部署版：**驗 digest，不驗 log**。
- **來源**：`history/089`；`deploy/README`；症狀＝pod imageID 一小時不變、`Config.from_env().chat_max_tokens` AttributeError。

### 手機/SOTA 排版：閱讀內容全寬無框，可點項才用卡片

- **理論說**：每塊內容都裝進卡片（框＋shadow＋padding）看起來整齊。
- **實際發生**：手機上 AI 回覆/文章正文裝卡片 → **雙重內縮**（外層 padding＋卡片 padding）、文字區窄、上下擠。
  SOTA（ChatGPT/Claude）的 AI 回答**不裝任何容器**，全寬文字流。
- **解決方式**：**閱讀性內容（AI 回覆、文章正文、來源萃取）→ 全寬無框、用留白＋對齊區分**；
  **可點的列表項（來源/文章/對話列）→ 才用卡片**（卡片＝「可點單位」訊號）。手機水平 padding 單層且小。
- **教訓**：**卡片是「可互動單位」的語意，不是「內容分隔」的裝飾**。要讀的東西給它寬度和留白；要點的東西才框起來。
  分隔用空白，不用線/框。
- **來源**：`history/089`；`ChatPage.tsx`、`ArticleViewPage.tsx`、`SourcePage.tsx`。

### 「大部分正常、有時跳走」＝時機競態；修法是驗結果、不是驗「我觸發過」

- **理論說**：MathJax 渲染在內容更新後排一次 debounce typeset 就夠了——呼叫過就會渲染。
- **實際發生**：數學**偶爾**卡成 raw LaTeX，重整就好、難重現。根因是 DOM **陸續晚到**（MathJax 還在載／
  圖片 reflow／串流換節點／長對話陸續 mount），晚到的節點在那次 typeset **之後**才進 DOM；而全域 timer
  又被別處 `clearTimeout` 取消 → 沒有第二次機會。「偶發」不是難重現的小毛病，是**競態的診斷簽名**。
- **解決方式**：typeset 後**查 DOM 有沒有漏網**（`.mathcopy:not([data-typeset])`），有就 400ms 後再跑、
  最多 4 次。成立前提＝**`typesetPromise` 對已渲染是 no-op ⇒ 冪等才敢重試**。
- **教訓**：① 症狀「大部分正常、有時不對」→ 先當**競態**查（誰晚到、誰把重試取消了），別當隨機。
  ② 更通用的骨幹：**驗結果，別驗「我做了那個動作」**——這是本專案第三次撞到同一面：
  跨連線驗 DB 寫入（`history/084`，單連線讀得到未提交＝假綠）、驗 pod 實際 digest 而非 rollout log
  （`history/089`，push 靜默失敗）、驗 DOM 實際渲染而非「我呼叫過 typeset」（本條）。
  ③ 敢用「收斂式重試」的前置條件是**操作冪等**；不冪等就別重跑，要改用一次性正確。
- **來源**：`history/091`；commit `7aaa310`；`frontend/src/components/Markdown.tsx`。

### 沒人用的功能，先問「是不是搞錯了維度」再砍——膜（判準）與場（脈絡）是正交的

- **理論說**：原則 6 說「自由發散、把關只在膜」，`concepts/有吸引子的場` 說三態有「沙盒」——
  那產品上就給一顆「🧠 腦力激盪」toggle，開了更放得開、不撒網，讓使用者能進沙盒。概念自洽、實作乾淨。
- **實際發生**：**沒人用**（連使用者自己都沒按過）。追問「它想解什麼」才發現：它放寬的是**膜**，
  而膜正是使用者當初說「會一直不斷想使用」的唯一理由——**做一顆「讓工具比較不誠實」的開關等於自廢武功**。
  且膜本來就不壓制發散（自由是**預設**，不是要申請的模式），所以那顆按鈕在解一個不存在的問題。
- **解決方式**：不是砍掉了事，而是換維度重做——**保留膜、可關的是場**：`bare` 模式（🔌 不接知識庫）
  這輪不注入核心理解、不撒網、不查收藏，但**反逢迎人格照舊**（守衛測斷言 system 仍含「好聽話」）。
  真需求是「問跟我的場無關的事時，注入 24 條核心理解只是雜訊、還讓答案硬要攀親帶故」。
- **教訓**：**一個沒人用的功能，多半不是需求不存在，是把旋鈕裝在錯的軸上**——砍之前先問「它想解的需求是什麼、
  正確的維度是哪一條」，否則連同那個 reframe 一起丟掉。這次拆出來的正交軸：
  **膜＝判準／人格（永不可關，關了就沒有這個產品）** vs **場＝脈絡／內容（可按需關）**——
  原本綁在 `build_field_system_prompt` 裡沒分開過。
  ＋原則 6 的讀法變精準：**「把關只在膜」≠「一定要接場」**。
  ＋「真實使用才是驗收」的新一面：它照得出的不只是**缺**什麼，還有**多**了什麼（該退場的）。
- **來源**：`history/090`；commit `988cdd2`；`src/knowfield/chat/field_chat.py`（`_BARE_NOTE`）、
  `tests/unit/test_field_chat.py::TestBareMode`。

### 找不到「推手」時，先問「是不是根本沒人設限」——用自家概念寫成的假設最像真的

- **理論說**：使用者說「AI 回答太長、理解有負擔」→ 一定有東西在**把它推長**，找出來拿掉就好。我提了兩個
  假設，**都用專案自己的概念寫成**，所以聽起來很像洞見：① 34 條核心理解**全量注入**（實測 system prompt
  10,613 字）把回答撐長——還附了個漂亮觀察「收進資料走檢索 top-k、核心理解走全量灌，**同一個 prompt 裡
  兩套標準**」；② 膜的 **8 條編號指令**被模型**逐條表演**給使用者看（＝「品質準則列成檢查表」那條的背面）。
- **實際發生**：4 次 API 呼叫**把兩個假設全部推翻**。場注入：接場 2,941 字 vs 不接 3,157 字（**接場反而略短**，
  差距在雜訊等級）；膜：**無 system 3,000 字** > 現況膜 2,732 字（**膜不但沒推長，還壓了一點**）。
  真因是模型對技術題的預設就是長篇，而**膜從頭到尾沒有一句提到長度**——**不是有東西在推長，是從來沒有人叫它短。**
  加一句「長度紀律」：2,732→**447** 字；走真路徑（接場＋長度紀律）2,941→**553** 字（−81%），
  而且接場行為與膜的擋過度簡化**都還在**——短掉的是廢話，不是內容。
- **解決方式**：`_MEMBRANE` 加「長度紀律」，**逐字用實測過的那句、不潤飾**（改字＝那次實驗就不算數）；
  ＋一條測試釘住「膜必須含長度紀律」（含 bare 模式那條路），防日後改措辭時整條靜默消失。
- **教訓**：
  1. **歸因先問邊界條件**：症狀是「太長／太多／太慢」時，先問「**有沒有人設過上限**」，再去找「誰在推」。
     找加害者是預設反射，**這次答案是找缺席者**。
  2. **用專案自己的概念寫成的假設，最像真的、也最難自我證偽**——它自洽、有洞見感、還能引用既有原則背書。
     這是原則 6 的 folie à deux 在**除錯**上的投影：**反逢迎不只要對使用者，也要對自己的診斷**。
  3. **破法便宜到不像話**：4 次 API 呼叫就否掉兩個假設，省下我原本要動的「注入策略」大工程（那還帶著
     「地基本來就該一直在場」的概念張力）。**能實測就別推理**——接「手動探針驗賭注」的除錯版。
- **來源**：`history/092`；`src/knowfield/chat/field_chat.py`（`_MEMBRANE` 長度紀律）、
  `tests/unit/test_field_chat.py::TestSystemPrompt::test_has_brevity_discipline`。

<!--
  範例：

  ### 實作前先確認功能是否已存在

  - **理論說**：核心模組只處理求值，所以我們需要從頭建一個
    type checker。
  - **實際發生**：花了兩天在建，結果發現核心模組已經有完整的
    型別檢查功能——只是沒有文件記錄。
  - **解決方式**：刪除重複的實作，為既有功能補上文件。
  - **教訓**：動手之前先搜尋既有程式碼。答案可能已經在那裡，
    只是沒被記錄。
  - **來源**：history/010-duplicate-checker.md
-->

### 死的知識不是靜止的知識，是沒有任何消費者會發現它錯掉的知識

- **理論說**：知識庫健不健康，看它有沒有在動——有沒有持續寫入、有沒有跟著專案更新。
- **實際發生**：本次 `/knowie-judge` 實測：**93 條 `history/` 沒有任何檔指向**、`episodes/` 100% 孤兒、
  `vision.md` 勾選比 226:6。那 93 條天天隨 commit 移動、一點都不靜止——但沒有任何取用路徑會碰到它們，
  所以**就算內容錯了也不會有人發現**。同一天使用者對「暫時對話存檔」的回報（「沒什麼意義，反而麻煩」）
  是同一件事在產品面的樣子：自動存了、也在動、也有衰減，但沒有消費者。
- **解決方式**：把「有沒有消費者」變成**可機械量測**的東西——連結一律寫成 markdown（`[[ ]]` 和反引號路徑
  推導不出圖，掃出來的孤兒率是假的），再逐層算孤兒率。修完 `history/` 孤兒 100%→26%、`draft/` 56%→7%。
- **教訓**：**判一份知識死沒死，不看它動不動，看「如果它錯了，誰會發現」**。沒有那個誰，寫得再勤都是死的。
- **不適用的時候**：有 oracle 的那一半（code、schema、測試）不吃這條——編譯器和測試就是它們的消費者。
  這條專講沒有 oracle 的知識。
- **來源**：本次 judge（commit `146a29d`、`9cb44e0`）；[draft/2026-08-16-跨專案聯邦與兩種冷啟動.md](draft/2026-08-16-跨專案聯邦與兩種冷啟動.md)、
  [draft/2026-08-18-暫存的真實使用回報與連接密度.md](draft/2026-08-18-暫存的真實使用回報與連接密度.md)。

### 否決要跟接受一樣便宜，否則增生沒有相對應的凋亡

- **怎麼撞到的**：用細胞比喻盤跨專案知識庫的失敗模式時撞到癌症那一支——**癌細胞不是特別會分裂，
  是它聽不見周圍叫它停的訊號**。回頭量自己的庫，證據就在眼前。
- **實際發生**：寫一條 `history/` 是免費的（做完順手就寫，93 條裡 30 條是「完成」型），
  而退役一條要重讀、判斷、寫理由。於是**增生有管道、凋亡沒有**：`vision.md` 累到 991 行、勾選比 37:1，
  27 份 draft 只有 2 份真的被放掉。不是產出太多，是抑制那一側從來沒建。
- **解決方式**：這次用一張里程碑索引把退役那側批次補做（991→132 行），但**那是補救不是機制**——
  下次還是會長回來，除非否決的成本降到跟接受一樣。
- **教訓**：**看一個系統會不會失控，不要數它產出多快，要比「加一個」和「退一個」的成本差**。
  差得越多，垃圾累積得越穩定，而且看起來都像產能。
- **不適用的時候**：**不可逆**的否決（刪掉唯一副本、砍掉沒備份的資料）不該便宜。這條講的是可逆的那種
  ——標記退役、留墓碑、塌成一行指標。
- **來源**：[draft/2026-08-16-跨專案聯邦與兩種冷啟動.md](draft/2026-08-16-跨專案聯邦與兩種冷啟動.md)；本次 judge 的實測數字。

### 自動化只移走製造成本，沒移走呈現成本——自動又可見的東西不是零成本

- **理論說**：暫時存檔改成**自動**（每輪 upsert、閒置 7 天自動刪，spec 028 / 階段 23），
  人工負擔就消失了。閘門不存在，就不會有摩擦。
- **實際發生**：使用者真實用過之後回報「**實際用起來沒什麼意義，反而會覺得麻煩**」。
  閘門確實不存在，麻煩卻在——因為它進了 `/conversations` 主清單，跟永久存檔混在一起、
  還帶著首句截斷的便宜標題。**省下的是「決定要不要存」，付出的是每次找東西都要先撥開一堆不打算留的。**
  7 天 TTL 又卡在最壞的位置：太短，救不到階段 18 要解的「半年後回看根因」；夠長，足以把清單塞滿。
- **解決方式**：⚠️ **尚未定案**——診斷是旋鈕裝在「存活期限（暫時/永久）」這條軸上，
  該裝的是「**可見性**（要不要出現在你面前）」；同樣自動存、同樣衰減，但不進主清單。
  砍或改的判準見 [draft/2026-08-18-暫存的真實使用回報與連接密度.md](draft/2026-08-18-暫存的真實使用回報與連接密度.md) 的誠實測試。
- **教訓**：**把一件事自動化，只移走了它的製造成本；只要它還出現在你每天要看的地方，呈現成本原封不動。**
  autosave 的價值在它隱形——一旦可見，它就從保險變成雜訊。
- **不適用的時候**：需要人在當下知情的東西不能隱形（隱形＝偷偷替人做決定）。這條只適用於
  「先全存、之後再判斷」這類延後決策的東西。
- **來源**：使用者 2026-08-18 使用回報；[history/067-對話暫時存檔完成.md](history/067-對話暫時存檔完成.md)（階段 23 原設計）；
  [draft/2026-08-18-暫存的真實使用回報與連接密度.md](draft/2026-08-18-暫存的真實使用回報與連接密度.md)。
### 驗證要走與正式路徑完全相同的管線，否則照出來的是驗證腳本的 bug

- **怎麼撞到的**：簡→繁功能寫完，要對真實來源（知乎 Flow Matching，50 塊）做端到端驗證。
  我用 `"\n\n".join(chunks)` 把塊拼回來——看起來跟正式路徑等價。
- **實際發生**：驗證報告顯示「正文有簡體字沒轉到」。追下去發現殘留的 `这`／`变换` 被吞進了
  行內數學的保護區。再追：`chunk_markdown` 有 40 字元 overlap，`join` 沒去重疊，
  於是第二塊從**數學式中間**開始（`t\{ f_i \right\}$`），`$` 總數變成奇數、配對錯位，
  把一段正文當成公式保護起來。**換成正式路徑用的 `stitch_chunks` 之後，`$` 是偶數（322），
  殘留歸零、161 個行內數學全數保留。**——實作從頭到尾都是對的，錯的是我的驗證腳本。
- **解決方式**：驗證一律呼叫**與正式路徑相同的函式**（這裡是 `stitch_chunks`），
  不自己重寫一個「應該等價」的版本。差一個函式，照出來的就是另一個系統。
- **教訓**：**一個與正式路徑不同的驗證管線，量到的不是正式路徑**。它的失敗訊號指向它自己，
  而你會拿去改實作——那才是真正的代價：不是白花時間，是**照著假訊號改對的東西**。
- **同一個根，後來又撞到兩次（2026-08-18 同日）**：
  1. **殺不掉的舊 process**：啟動寫成 `uvicorn 'knowfield.web.app:create_app'`（帶引號），
     但 shell 會吃掉引號，process 命令列裡沒有——所以 `pkill -f "uvicorn 'knowfield..."`
     **永遠匹配不到**。舊 server 續佔 port、新的起不來就默默退出，而我以為在看新版，
     驗證出來的三個「修正沒生效」全是舊碼跑的。⇒ **一個沒被確認過的停止指令等於沒有停止**；
     重啟後要 `ps aux | grep -c` 確認是 0。
  2. 那條壞掉的 `pkill` **就寫在我自己剛建的 skill 裡**——照著它做才炸出來。
     ⇒ 一份沒有被**照字面執行過**的操作文件，等於還沒被驗證。
  3. **前端型別檢查用錯指令**（2026-08-21，這次一路燒到 CI）：我用 `npx tsc --noEmit` 驗，
     而 Dockerfile／CI 跑的是 `npm run build`＝**`tsc -b`**（build mode，走 project references）。
     兩者對同一份程式碼給出不同答案：本機全綠、CI 紅在我剛移除的那個 prop 上，
     部署整個停住。⇒ **驗前端不要自己拼一條指令，就跑 `npm run build`**——
     那是 Dockerfile 實際跑的那一條。
     ⚠️ 這是同一個根當天的第四次：`join` vs `stitch_chunks`、`pkill` 樣式、`ps grep -c`、
     `tsc --noEmit` vs `tsc -b`。**每一次我都以為自己挑的是「等價」的指令。**
- **不適用的時候**：刻意要隔離某一層來定位問題時（單元測試、二分法除錯），差異是**目的**不是缺陷。
  這條講的是「宣稱驗證了端到端」的那種驗證。
- **相關**：與「驗證 DB 寫入要跨連線——單連線 smoke 會假綠」同族——那條是**連線邊界**差異造成假綠，
  這條是**管線步驟**差異造成假紅。同一個根：**驗證環境與正式環境的任何差異，都會變成假訊號**。
- **來源**：`specs/037-simplified-to-traditional/tasks.md`〈執行結果〉；
  [history/094-來源語言處理從改寫取代原文反轉為顯示層轉換.md](history/094-來源語言處理從改寫取代原文反轉為顯示層轉換.md)；commit `4e9e241`。
### 命名新控制項前先 grep 這個詞在產品表面已經指什麼——同一畫面的同音異義，使用者照舊的意思讀

- **怎麼撞到的**：spec 037 給來源頁加了「繁體 ⇄ 未轉換文字」的切換，我把按鈕命名為「**看原文**」。
  自己審 code、跑測試、開瀏覽器截圖，都沒察覺；使用者一眼就說「這可能會跟看原本的網站有誤會」。
- **實際發生**：`原文` 在這個產品裡**早就有主人**——它指**原站連結／存下的 PDF，唯一真相**。
  而衝突就在同一畫面上、我那顆按鈕的**上方 4 行**：

      :125  🔍 萃取參考（自動抽取，給檢索用；可能有小誤差，準確請看上方原文）
      :129  顯示原文（未轉換）      ← 新加的
      :131  看原文                  ← 新加的

  頁面剛叫使用者「準確請看上方**原文**」（跳出去看原站），下一行就給一顆「看**原文**」按鈕做完全不同的事
  （原地切換萃取文字的字體）。`:74`、`:116` 也都用它指原站／PDF。
- **為什麼自己看不出來**：我在 code 註解裡也自然地寫「留一個看回**原文**的出口」——
  **我是用「未經轉換」這個意思在想這個詞，而產品是用「來源本尊」這個意思在用它。**
  同一個字在我腦中和在畫面上指不同東西，所以怎麼看都順眼。
- **解決方式**：改用**不可能撞的詞**——「看轉換前」／「顯示轉換前的文字」。它同時可攜到第二刀
  （英→繁），而「看簡體」不行。
- **教訓**：**取一個使用者可見的名字之前，先 grep 這個詞在產品表面已經指什麼**。同一畫面上的同音異義，
  使用者會照**既有的**那個意思讀——而你不會發現，因為你腦中的意思才是新的那個。
- **不適用的時候**：內部識別字（函式名、欄位名）撞字沒關係，那是靠命名空間區分的。這條只管
  **使用者看得到的文案**。
- **相關**：與「內部概念詞彙別洩漏到產品對外表面」同族——那條是**內部術語外洩**，
  這條是**產品既有的對外詞彙被新功能借走**。同一個根：**你對自己專案的詞彙免疫，看不見它對別人的樣子**。
- **來源**：使用者 2026-08-18 回饋「『看原文』可能會跟看原本的網站有誤會」；
  spec 037、`frontend/src/pages/SourcePage.tsx`。
### 把別處的宣稱抄進長期層之前要自己接地——尤其當它會導出「刪掉某段程式碼」這種建議

- **怎麼撞到的**：寫 `history/094` 時，我從 `draft/2026-08-10` 抄了一句「`config.article_lang`
  至今還活著，但**只餵已退役的 summarizer**……是退役掃除的漏網」。順手、通順、看起來有根據。
- **實際發生**：隔天要動手清它，先 grep 一下——**它是活的**：

      backends/factory.py:67   OpenAIArticleWriter(…, lang=config.article_lang)
      cli/ask_cmd.py:21        lang = args.lang or config.article_lang
      cli/ingest_cmd.py:23     config.article_lang = args.lang

  它餵的是**散文／消化後端**，`ingest` 與 `ask` 都在用。照那句去清會弄壞現行功能。
  ⚠️ 而那句已經進了 `history/`——**長期層裡一條會導致破壞性動作的假宣稱**，比沒有記錄危險得多。
- **解決方式**：兩處都加更正段（不刪原文，留下「曾經這樣寫過」的痕跡），並把「不要複用它」的**理由**
  從「它是死的」改成「**語義不同**」——結論碰巧一樣，但推理錯了，而下一個人會照推理行動。
- **教訓**：**一句宣稱從 `draft/` 進 `history/` 的那一刻，它從「某人當時的看法」變成「專案記得的事實」**
  ——所以要在那個邊界上接地，不是在寫 draft 時。判準：**這句話會不會讓人去刪、去改、去停用什麼東西？
  會 → 先 grep，附行號。** 本專案的骨幹「答案對到 ground truth」就是這條，只是它最容易在
  「抄自己人」的時候鬆掉——因為那看起來不像在引用外部來源。
- **不適用的時候**：draft 裡的推測不必先接地，那正是 draft 的用途（沙盒、預設衰減）。
  這條只管**跨過長期層邊界**的那一刻。
- **相關**：與「驗證要走與正式路徑完全相同的管線」同族——那條是**驗證動作**沒接地，這條是**宣稱**沒接地。
- **來源**：`history/094`〈更正（2026-08-18 當日）〉；`draft/2026-08-10-來源翻譯與對照.md`〈更正〉。
### 一個「先探針再建」的閘門，價值不在它放行的時候，在它擋下來的時候

- **理論說**：需求形狀從使用者的用詞就推得出來。他說「希望可以**對照**」，那要的就是並置——
  這個推論還很漂亮：「『對照』兩個字就是證據：要的不是替換，是並置。」整套設計（逐塊並置、
  複用切塊當對齊單位、階段名叫「翻譯**與對照**」）都從它長出來。
- **實際發生**：設計源自己對這個推論不放心，在出口寫了「**賭注過了才 promote**」——要求先做一次
  零 build 的手工對照給使用者看。今天把賭注攤開（他自己收進的來源、產品既有的切塊、真的翻譯、
  數學保護 34/34），他看完的答覆是「**全文翻譯**」。**那個漂亮的推論是錯的。**
- **解決方式**：整條轉移記進 `history/095`，把 draft 那句 reframe 標為已推翻並列出連帶失效的設計，
  roadmap 項重寫。⚠️ **推論錯了但不是全錯**——原文為真相、承重保護、標 AI 翻譯這幾條仍成立，
  逐項標「推翻／保留」比整篇作廢誠實，也讓下一個人知道哪些還能用。
- **教訓**：**從使用者的用詞反推出來的需求形狀，是假設不是證據**——他描述的是他當下能想到的解法，
  不是他要解的問題。判準：**這個設計的地基，是使用者驗過的，還是我從他一句話推出來的？
  後者 → 先花最小的成本讓他看到實物再說**。而一個擋下來的閘門**沒有可見的產出**，
  所以最容易被當成多餘的流程砍掉——它的價值恰好是那個沒有被蓋出來的錯東西。
- **不適用的時候**：可逆且便宜的東西不必先探針（做了不對就改）。這條針對的是**會長出一整套介面**
  的設計前提——蓋下去才發現方向錯，成本在介面不在邏輯。
- **相關**：與「提案-批准迴圈 ≠ 打到需求」同族——那條講**批准 ≠ 打到**，這條講**用詞 ≠ 需求**，
  同一個根：使用者說的話不等於使用者要的東西，而中間那段要用實物去接。
- **來源**：[history/095-對照被探針否決-第二刀改為一鍵全文翻譯.md](history/095-對照被探針否決-第二刀改為一鍵全文翻譯.md)；
  探針產物與逐塊實測（Lil'Log Diffusion Models，11 塊、58.5s、承重 34/34）。
### 一條沒有被錯誤實作撞過的測試，不知道自己在測什麼

- **怎麼撞到的**：spec 038 的即時進度（FR-003）。初版實作把進度累積起來、全部翻完才一次吐出
  ——使用者要盯著沒反應的畫面 93 秒。**七條契約測試全綠。**
- **實際發生**：我寫了**三版**測試，每一版都拿一個**故意做成累積式**的實作去撞：

      v1  驗「有沒有 stage 事件」        → 綠（累積式也會吐，只是最後才吐）
      v2  驗「第一個事件是不是 stage」    → 綠（順序對、時機錯）
      v3  直接測產生器 ＋ **時間上界**    → 紅 ✅（2.12s 失敗）

  前兩版**看起來**在測即時性，實際只測到存在性與順序。若沒有主動拿錯誤實作去撞，
  我會帶著兩條假測試出貨，而且更糟——**日後有人把它改壞，測試還是綠的**。
- **解決方式**：v3 加時間上界；並把串流邏輯從路由抽到 `text/translate.py::translate_stream`
  ——**不是為了分層好看，是因為時機隔著 `TestClient` 驗不了（它會緩衝）**。可測性驅動了模組邊界。
- **教訓**：**寫完一條測試，先讓它紅一次「不是因為功能還沒做」，而是「因為功能被做壞」**。
  前者只證明測試會執行，後者才證明它在測東西。判準：**我能不能寫出一個通過這條測試、
  但明顯是錯的實作？寫得出來 → 這條測試還沒完成。**
- **不適用的時候**：純資料轉換的往返不變式（`restore(*mask(t)) == t`）本身就是強斷言，
  不需要另造錯誤實作。這條針對的是**行為/時序**類的斷言，它們最容易寫成「看起來有驗」。
- ⚠️ **第三種失敗：攻擊打錯地方。** 同一份程式碼常有兩條相似路徑
  （串流 vs 非串流、批次 vs 逐筆、路由 vs repository）。攻在**沒被那條測試走到**的那一條，
  測試照樣綠，而你會以為它很穩。spec 048 實例：把領域名塞進 grounding 的攻擊打在
  非串流的 `_do_chat`，而測試走 `/api/chat/stream` ⇒ 撞不紅；攻對串流路徑後立刻紅 2 條。
  ⇒ **撞不紅時先確認三件事：測試有沒有牙齒／攻擊有沒有實作／攻擊打的是不是測試會走的那條路。**
- ⚠️ **第二種失敗：攻擊本身是 no-op。** 撞不紅有兩種原因——測試沒牙齒，
  或**你以為在實作那個 bug、其實沒有**。spec 041 的第一次攻擊把訊息 append 在
  `messages += hist` 之後（改不到東西）；spec 043 的攻擊釘住 `nodes[:2]`，
  而那剛好就是原本排最前的兩條 ⇒ 輸出完全沒變。
  ⇒ **撞不紅時，先確認攻擊真的改變了產物**，再去懷疑測試。
- **相關**：與「驗證要走與正式路徑完全相同的管線」同族——那條是驗證**管線**沒接地，
  這條是驗證**斷言**沒接地。同一個根：一個沒被挑戰過的驗證，量到的是它自己。
- **來源**：`specs/038-source-translation/tasks.md`〈三次才寫出一條有牙齒的測試〉；
  [history/096-第二刀出貨-三個只有真跑才照得出的缺陷.md](history/096-第二刀出貨-三個只有真跑才照得出的缺陷.md)；commit `ae3b8af`。

### 切塊的用途決定了合法的切點——換了用途就要重切

- **理論說**：這篇文章已經被 `chunk_markdown` 切好了（給檢索用），翻譯直接沿用那些塊就好，
  省事又一致。
- **實際發生**：畫面上出現「條件式 **Generat**」和落單的「**離子**」。追下去有兩層：
  1. `stitch_chunks` 靠**精確字串比對**去除塊間 40 字元重疊——那對原文成立，但每塊
     **獨立翻譯**後重疊段會翻成不同中文、比對失敗，兩份都留下（殘影）。
  2. 更深的一層：`chunk_markdown` 會**從單字中間切**（實測 124 個接縫有 **55 個**是）。
     `Conditioned Generation` 被切成 `Conditioned Generat` ＋ `ion`，後者獨立翻譯成「離子」。
- **解決方式**：不再重用檢索塊。先拼回全文，再用 `split_units` 切成**翻譯單位**
  ——切在段落邊界，過長段落退而切在空白處，**絕不從單字中間切**，且重組逐字還原。
- **教訓**：**同一份切塊在不同用途下有不同的「合法切點」**。檢索容得下切在字中間
  （embedding 照樣算）、容得下重疊（提高召回）；翻譯兩者都致命，因為每塊會被當成
  **完整的一段話**獨立處理。判準：**沿用既有切塊之前，先問「新用途對切點的要求，
  和舊用途一樣嗎」**——不一樣就得重切，省下的那點事會在接縫上加倍還回來。
- **不適用的時候**：新用途對切點**更寬鬆**時（例如只是計字數）當然可以沿用。
  這條防的是「更嚴格卻沿用」。
- **相關**：與「驗證要走與正式路徑完全相同的管線」是同一天的兩面——那條是我用
  `join` 代替 `stitch_chunks` 而量錯，這條是我沿用 `chunk_markdown` 而做錯。
  **同一組函式，一次用錯在量測、一次用錯在生產。**
- **來源**：[history/096](history/096-第二刀出貨-三個只有真跑才照得出的缺陷.md)；
  `src/knowfield/text/translate.py::split_units`；實測 55/124 接縫切在單字中間。
### 引用一條教訓時要連它的理由一起檢查——理由不成立，結論就不成立

- **怎麼撞到的**：把 OpenCC 列為**可選** extra 時，我引的是「把重量級相依藏在可插拔介面後，
  預設離線 stub」。聽起來完全對得上：可插拔介面 ✓、fallback ✓、可選安裝 ✓。
- **實際發生**：那條教訓的理由寫得很清楚——「TDD 會變成**先裝好幾百 MB** 才能跑第一個測試」。
  而 `opencc-python-reimplemented` 是純 Python、**5ms 裝完**。理由整個不成立，
  我卻照搬了結論。⇒ Dockerfile 只裝 `.[web]`，prod 沒有它，
  **spec 037 上線後在線上完全沒作用**，而且不報錯（identity fallback）、`/healthz` 照樣綠。
- **解決方式**：移進執行期相依。⚠️ 並拆開兩件被我綁在一起的事——**「要不要可插拔介面」
  和「要不要列為可選安裝」是不同的決定**：介面是為了「後端掛掉時別炸」，
  可選安裝是為了「別讓安裝變重」。前者該留、後者不成立。
- **教訓**：**一條教訓 = 判準 ＋ 它成立的理由。抄結論不抄理由，就會把它套到理由不成立的地方**，
  而且套起來會很像對的——因為表面特徵確實吻合。判準：**引用前先問「這條當初是為了避免什麼？
  那件事在這裡會發生嗎？」** 不會 → 這條不適用。
- **不適用的時候**：判準本身已經寫成不帶理由的硬規則（「檔名不要有空白」）時，直接套沒問題。
  這條針對的是**帶著權衡的**教訓——它們的適用範圍就是那個權衡成立的範圍。
- **相關**：這也是為什麼 0.7.3 的 experience 模板加了「不適用的時候」——一條沒有邊界的教訓
  會被過度套用。本條就是那個欄位空著時實際發生的事。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/099-可插拔介面的沉默代價-健康檢查要說得出能力.md](history/099-可插拔介面的沉默代價-健康檢查要說得出能力.md)；
  被誤引的是本檔「把重量級相依藏在可插拔介面後」那條。
### 一個元件被 N 個介面共用而沒有測試，它的 bug 就會由使用者逐個發現

- **理論說**：`Markdown.tsx` 是純渲染、邏輯簡單，看得懂就不用測；出問題重整一下就好。
- **實際發生**：它被 **6 個介面**共用，出過**三次線上 bug**——全形粗體露星號（`history/091`）、
  typeset 競態（091）、串流時退回原始碼（本輪）——**每一次都是使用者在線上撞到的**，
  而且每次修完都以為修好了。這輪補上 13 條測試，當場又照出**三個**沒人發現的缺陷：
  串流期間 typeset 從不執行、漏網偵測用「我呼叫過」當證據、重試次數與文件不符。
  ⚠️ 其中兩個是**修 091 時就寫進去的**，帶著它自己的教訓一起活了兩個月。
- **解決方式**：建最小測試基礎設施（vitest ＋ jsdom），只測**時機與不變式**，不測樣式。
  ⚠️ 每條測試都拿「還原成舊實作」的版本撞過，確認會紅。
- **教訓**：**共用度是風險的乘數，不是安全的證據**。一個被 N 處使用的元件，它的每個缺陷
  都會以 N 種面貌出現，而每一種看起來都像獨立的小問題——於是永遠在修症狀。
  判準：**這段程式碼有幾個呼叫端？沒有測試的話，我打算讓使用者發現它幾次？**
- **不適用的時候**：只有一個呼叫端、且改動成本低於測試成本的東西（一次性腳本、探針）不必測。
  這條針對的是**共用**且**難以肉眼驗證**（時機、競態、跨邊界）的那類。
- **相關**：與「一條沒有被錯誤實作撞過的測試，不知道自己在測什麼」是一組——
  那條講測試的品質，這條講測試的存在。**先有，再有牙齒。**
- **來源**：[history/100-數學渲染全面檢查-四個缺陷與前端測試從零到有.md](history/100-數學渲染全面檢查-四個缺陷與前端測試從零到有.md)；
  [history/091](history/091-前端渲染兩坑-全形標點與typeset競態.md)（同一元件的前兩次）。

### 同一個機制換一個用途，判準要跟著換——不能整套照搬

- **怎麼撞到的**：`protect.mask/restore` 這套「抽佔位→交給不可靠處理器→還原」在這個庫已經用了
  四處（SQL、OCR 圖片、簡→繁、英→繁）。要給收進時的 LLM 清理也套上時，我差點把翻譯那套
  **完整性檢查**整份搬過來：「任何佔位符不見 → 整段退回原文」。
- **實際發生**：清理的工作**本來就是刪雜訊**——它本來就會少東西。照搬翻譯的判準，
  等於任何一次成功的清理都會被判定為失敗、全部退回原文，**功能等於不存在**。
- **解決方式**：判準改成「**全部**佔位符都消失才退回」（＝模型根本沒照規則走），
  留下來的照常還原。機制相同、閾值相反。
- **教訓**：**一套機制可以複用，它的判準不行**——判準編碼的是那個用途的成功長什麼樣。
  搬機制時要問：**這個用途下，「正常」和「壞掉」的界線在哪？** 翻譯是「不該少東西」，
  清理是「本來就要少東西」，同一條規則在兩邊指向相反。
- **不適用的時候**：兩個用途的成敗定義真的相同時（例如同樣是翻譯、只換語言對）當然照搬。
- **相關**：與「切塊的用途決定了合法的切點」同源——那條是**切點**隨用途變，這條是**判準**隨用途變。
  再往上都是同一句：**複用的是機制，不是它成立的條件。**
- **來源**：[history/100](history/100-數學渲染全面檢查-四個缺陷與前端測試從零到有.md)；
  `src/knowfield/ingest/clean.py` 與 `src/knowfield/text/translate.py` 的完整性檢查對照。

### 一個閘門切錯軸，兩邊都會漏——切之前先問「風險住在哪一軸」

- **理論說**：要讓對話當文章的「情境」，就得擋住沒依據的東西跟著進來。我提的過濾是
  **只餵使用者自己的 turn、不餵 AI 的回覆**——理由是情境幾乎都在人的話裡，
  而把 AI 散文餵回 AI 生成正是 model collapse 那條線。聽起來兩全。
- **實際發生**：使用者一句話拆掉它——「如果只有我自己講的話，常常是沒有依據的」。
  我切的軸是「**誰講的**」，但風險住在「**有沒有依據**」那一軸，而**這兩軸正交**。
  於是兩種過濾各漏一邊：全對話漏未冊封的 AI 主張；只有人的 turn 漏未冊封的人的主張。
- **教訓**：**設計一個過濾器之前，先寫下「風險住在哪一軸」，再看你打算切的是不是同一軸。**
  不同軸就是白切——擋掉一半無辜的，放進一半有害的，而且兩邊都看不出來。
  ⓘ 這是「旋鈕裝在錯的軸上」的第三次（前兩次：膜 vs 場、存活期限 vs 可見性），
  但**時機不同**：前兩次是事後診斷沒人用的功能，這條是**設計當下**就該跑的檢查。
  ＋順帶一條：**未冊封 ≠ 中性**。對話走過 distill → 候選 → 人挑，未冊封裡有一部分是
  「提了、你看過、明確沒選」的——當情境餵回去等於把丟掉的東西從後門撿回來。
- **來源**：[draft/2026-08-21-從對話生文章.md](draft/2026-08-21-從對話生文章.md) 第三節；
  對照 `experience.md` 的「沒人用的功能，先問是不是搞錯了維度」。

### 保證分兩種：「不可能」與「可見」——把可見寫成結構性保證，是把人的注意力當成程式的保證

- **理論說**：讓對話收斂成一份 schema 放不下主張的簡報（只有「要回答的問題」「角度」
  「用哪幾條核心理解」），主張就進不來了——這是結構性保證。
- **實際發生**：schema 擋不住**偽裝成問句的主張**——「為什麼殘差**其實是**一種記憶機制？」
  就是一句夾帶。真正擋住它的不是 schema，是**人會看到這份簡報再按生成**。
- **教訓**：寫規格時要分清楚你手上是哪一種保證。**「不可能」＝程式路徑上做不到（可以測）；
  「可見」＝人有機會攔截（測不到，取決於人有沒有看）。** 把後者寫成前者，
  規格就開始說謊，而且是**最難發現的那種謊**——因為它在正常情況下看起來完全成立。
  正確寫法：關住可以關的（結構），**記下關不住的**（已知限制），不假裝全封。
  ＋設計「可見」型閘門時要壓低查驗成本：五個問句掃一眼就查得完，
  要人讀整段對話抓夾帶則不划算——**沒人負擔得起的可見性等於沒有可見性**。
- **來源**：[draft/2026-08-21-從對話生文章.md](draft/2026-08-21-從對話生文章.md) 第五節；
  同一個處理方式已用在 `specs/041-article-in-chat/spec.md` FR-003 的邊界說明。

### 使用者說「就只是 X，不用特殊處理」時，X 就是全部——補東西是在解自己的問題

- **理論說**：文章當對話的第一則之後，空白狀態該換成什麼？我做了「換你說」分隔線
  ＋三顆起手式（想法／不同意／再深一層），還特地讓它們只填輸入框不自動送出。
  每一顆都對應使用者描述過的用法，理據齊全。
- **實際發生**：「我純粹就只是想讓那文章當對話的第一則，不用做什麼太特殊處理。」
  全部拆掉，淨改動剩三行（`{empty && !article}`）。
  ⚠️ 這是同一功能同一天**第四次**形狀糾正，四次方向一致：**我一直在補，使用者要的是不要多。**
- **教訓**：**「只是 / 純粹 / 不用特殊處理」是範圍宣告，不是語氣詞。**
  聽到它，正確的動作是**刪到只剩那句話講的東西**，而不是「在那個基礎上把體驗做好」。
  ＋辨認自己在補東西的徵兆：你正在為一個**還沒有人抱怨過的問題**設計解法。
  ＋做新控制項前先問「這一頁已經有做同一件事的東西了嗎」——
  第五次糾正就是這個（我自製展開收合，而章節那套 `<details>`／▸▾ 一直都在，
  另立一套等於要人學兩次）。
- **來源**：[history/101](history/101-文章當第一則-從掛在對話上改為開新對話.md)；
  commit `e319c1d`（加）→ `3d228b9`（拆）。

### 一條「全有全無」的規則，顆粒度愈粗就愈不可能成立——先算 (1-p)^N 再定規則

- **理論說**：spec 039 FR-006 寫「含降級單位的翻譯結果不得被快取」——否則那次失敗會被
  固定下來，使用者永遠拿不到完整譯文。理由完全正確，我照著做成**逐文件**：全存或全不存。
- **實際發生**：真跑 45 個單位的來源，**失敗 1 個**（暫時性網路逾時）⇒ 整份不存 ⇒
  使用者要的「自動保存」**根本不會發生**。而且不是運氣：N 個單位、單位失敗率 p，
  整份可存的機率是 `(1-p)^N`，N=45、p=2% 只剩四成。
- **教訓**：**當一條規則是「全部滿足才算數」，它成立的機率隨顆粒度指數衰減。**
  定這種規則之前先估 N 和 p、算一下 `(1-p)^N`——若答案不接近 1，
  要動的不是規則本身，是**顆粒度**。把規則放到對的顆粒度上（逐單位），
  原規則的**理由**反而被更完整地滿足：失敗的永遠重試，成功的不必陪葬。
  ＋逐塊成敗要由產生它的那層**明講**（回傳 `ok` 陣列），
  **不能用「輸出 == 輸入」去猜**——純程式碼／URL 的單位翻完本來就可能一樣。
- **來源**：[history/102](history/102-譯文快取-從逐文件改為逐單位.md)；commit `6c9d367`；
  `specs/039-translation-cache/plan.md` 決策 D3。

### 日誌的 handler 掛在葉子上，就等於沒掛——沒人呼叫過設定函式的模組會整條鏈找不到 handler

- **理論說**：`logging_setup.get_logger(name)` 建好 handler 就能記錄，憲章 V 的可觀測性有著落。
- **實際發生**：handler 掛在**傳進來的名字**上，而全專案只有 `knowfield.web` 與
  `knowfield.cli` 呼叫過它 ⇒ `knowfield.text.translate` 整條鏈上沒有 handler、
  套件根 `knowfield` 又是 NOTSET（吃 root 的 WARNING）⇒ **INFO 全被丟掉**。
  後果：「第 N 塊退回原文（原因）」在正式執行時**從來沒印出來過**。
  ⚠️ 我因此差點把**暫時性網路逾時**誤判成**系統性的保護片段失敗**——那會導向完全錯的修法。
- **教訓**：**設定型的初始化要作用在「所有使用者共同的祖先」上，不是呼叫者自己身上。**
  判準：問「一個**從沒呼叫過這個設定函式**的模組，行為會是什麼？」
  若答案是「靜默失效」，就是掛錯層了。
  ＋這條的一般形：**只有被人記得呼叫才生效的機制，等於沒有這個機制**——
  跟「快取續命要併在讀裡面、不能另開一個方法」是同一句話。
- **來源**：[history/102](history/102-譯文快取-從逐文件改為逐單位.md)；
  `src/knowfield/logging_setup.py`、`tests/unit/test_logging_reach.py`。

### 一個機制的失敗如果是沉默的，就要另外給一條「我指定」的路——不是因為它不夠準

- **理論說**：來源已經會被撒網撈進對話（`_chat_corpus`），要「跟這份聊」再加一條路，
  聽起來只是準確度的邊際改善，YAGNI 該擋下來。
- **實際發生**：真正的差別不在準確度。**撒網沒撈到你這份時，畫面上沒有任何跡象**
  ——你會以為模型看過了，然後拿它的回答當「這篇沒提到」。人指定則沒有這種失敗模式。
- **教訓**：評估「要不要再加一條路」時，先問**既有那條的失敗是不是沉默的**。
  失敗會出聲 → 邊際改善，多半不值得；**失敗是沉默的 → 那不是準確度問題，是可信度問題**，值得。
  ＋這條同時決定驗收怎麼寫：**新路徑必須在舊路徑停用的條件下驗**，
  否則測到的是既有功能。（本刀的 SC-002 就是「撒網停用」。）
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/103](history/103-來源直接對話-撒網的失敗是沉默的.md)；
  `specs/042-source-in-chat/spec.md` SC-002；commit `3aa6b46`。

### 一次「全面檢查」的完備性，上限是當時存在的路徑——新加一條就可能作廢舊結論，而它不會舉手

- **理論說**：`history/100` 對數學渲染做過一次全面檢查，四個缺陷都修了，判準也改成看 DOM 證據。收斂了。
- **實際發生**：spec 042 加了一個**受控 `<details>`**（為了打字重繪時不被掰回展開），
  於是生出一條新路徑：**重繪但 `text` 沒變**。React 重套 `innerHTML` 擦掉 `mjx-container`，
  而 typeset 的 effect 綁 `[text]` ⇒ **永遠不會再排**。實測 68 個公式 → 收合 → 0，等 4 秒仍是 0。
  上一輪照不到它，是因為當時所有會重繪的元件內容都會跟著變（章節是 uncontrolled）。
- **教訓**：**「全面檢查」是對當時路徑集合的全面，不是對這個主題的全面。**
  每次新增一種「同樣內容但會重繪／重掛」的元件，就要回頭問一次舊結論還成不成立。
  ＋副教訓：**綁 `[deps]` 的 effect，等於宣告「只有 deps 變時才需要做這件事」**——
  若那件事會被**外力**（框架重繪、第三方函式庫）破壞，這個宣告就是錯的，該用無依賴 effect。
- **來源**：[history/104](history/104-重繪擦掉排版而effect不再排-公式永久卡成原始碼.md)；
  `frontend/src/components/Markdown.tsx`。

### 斷言被「你以為之外的東西」滿足——同義反覆只是其中一種

- **理論說**：寫了測試、看到綠燈，就代表那條行為被釘住了。
- **實際發生**：同一天在 spec 042 撞到兩次，機制不同但病灶相同：
  ① 「未帶來源時脈絡逐字相同」寫成 `f(None) == f()` ——**拿同一份程式碼比自己**，
     多塞幾則訊息照樣相等（這是 spec 041 `hist = list(history)` 的第二次）。
  ② 「份內檢索有把命中的塊帶進來」斷言 `關鍵字 in 整段脈絡` ——
     而**使用者的提問裡就有那個關鍵字**，於是檢索整個壞掉也照樣綠。
- **教訓**：綠燈只證明「有東西滿足了這句斷言」，不證明**是你以為的那個東西**。
  下手處：**把斷言的作用域縮到你真正在測的那一塊**（只看來源那一則，不看整段脈絡），
  並且**拿寫死的預期值比對**，不要拿被測程式的另一次呼叫比對。
  ⚠️ 判準：**反向攻擊撞不紅 ≠ 實作正確，多半是斷言被別的東西滿足了**——
  這時要修的是測試，不是慶祝。
- **來源**：`tests/unit/test_field_chat_source.py`、`tests/contract/test_chat_source.py`
  （兩處都留了為什麼的註解）；commit `3aa6b46`。

### 使用者說「前提是 X」時，要量的不是「X 成不成立」，是「X 成立時的量夠不夠用」

- **理論說**：使用者說「能不能從對話生文章？**前提是對話已經有相對應的核心理解了**」。
  前提是可查詢的（`why_nodes.conversation_id`），一查就知道成不成立——看起來已經接地了。
- **實際發生**：一查分布才發現**成立 ≠ 夠用**：13 段對話只有 5 段冊封出核心理解，
  而且**中位數 2 條**，`top_k` 是 8。原設計「只用那幾條生成」會產出兩條撐起來的薄文章。
  ⇒ 改成「那幾條釘住必進、其餘由場補滿」——對話決定角度，場提供厚度。
  ＋同一次查詢還照出第二件事：那些節點裡**真的有 `猜想` 與 `類比`**，
  照原設計直接塞進正文會把「高證實文章」的賣點悄悄破掉。
- **教訓**：前提型需求要量**兩件事**——「有沒有」與「**有多少、是什麼成分**」。
  只驗前提為真，等於只證明功能不會炸，沒證明它有用。
  判準：**把前提查詢的結果按分布列出來（中位數、最小值、成分），不要只看「非空的有幾筆」。**
  ＋這是「先探針再建」的變體，但探的對象不同：那條探的是**使用者**，這條探的是**你自己的資料**。
- **來源**：[history/105](history/105-從對話生文章-兩個數字改掉兩版設計.md)；
  `specs/043-article-from-conversation/spec.md` 開頭那兩個數字；commit `dba4628`。

### 寫驗收指標之前，先確認那個訊號在產物裡真的存在

- **理論說**：spec 043 的 SC-001 寫「那 N 條在 **References** 中出現次數 ＝ N」——
  References 是文章的溯源區塊，把用到的節點列出來，聽起來理所當然。
- **實際發生**：`_references()`（`output/article.py:65`）渲染的是**網址**或「（你收藏的來源）」，
  **一個字的主張都沒有**。正文節點的主張只存在於**送給模型的 prompt** 裡。
  ⇒ 那條驗收**永遠不可能通過**，而失敗看起來像實作壞了。我差點去改實作。
- **教訓**：**一條驗收指標同時宣稱兩件事——「行為要正確」和「這個訊號量得到」。**
  第二件常常沒被檢查，於是失敗時你會去修第一件。
  判準：**寫下 SC 之後，先問「這個數字我現在就能從產物裡數出來嗎」**——
  數不出來就先改指標（或改產物），不要等紅燈才發現。
- **來源**：`specs/043-article-from-conversation/spec.md` SC-001（含更正說明）；
  `tests/unit/test_article_pinned.py`（改成驗 prompt 的理由寫在 `_Backend` 的 docstring）。

### 你拿來當理由的那個東西，要順手記下來——否則它用完就沒了

- **理論說**：spec 041 決定「帶著這篇聊要開新對話」，我寫的理由是
  「混進既有對話會讓那段對話的**由來**變得不純」（`history/101`）。理由成立、隔離做對了、出貨。
- **實際發生**：一天後想回答「這功能有沒有人用」，才發現**那個由來我一次也沒存過**。
  我拿溯源當論據，做完隔離就收工——論據用完就丟。
  更糟的是：因為 041 FR-003 刻意讓帶入物不進 `messages`（那是對的），它在儲存層**零痕跡**
  ⇒ 使用量**永遠**查不到，而報告上的空白**分不出「沒人用」和「量不到」**。
- **教訓**：**做完一個決定，回頭看你寫的理由裡有沒有名詞是「應該被記錄的東西」**
  ——由來、出處、原因、觸發條件。有的話，它多半該落庫，而不是只活在 commit message 裡。
  判準：**如果這個理由是真的，那它指涉的事實就值得被查詢**；查不到，就是理由沒被兌現。
  ＋副作用要一起看：一個為了正確性做的**隔離**，常常同時隔掉了**可觀測性**。兩者要分開處理。
- **來源**：[history/106](history/106-三刀被做成量不到的-加欄地基補上.md)；
  `specs/044-carried-provenance/`；commit `060cb9e`。

### 「量不到」和「沒人用」在報告上長得一樣——先確認你有那個訊號，再解讀那個數字

- **理論說**：`audit-field-usage` 的判準是「哪個功能的消費者數是 0」。跑一次就知道誰是死的。
- **實際發生**：階段 37/38 在報告上是 0，但那不是使用資料——是**根本沒有欄位記錄**。
  如果照判準讀，我會得出「沒人用」然後去改錯的東西。
  ＋補完之後還有第二層：044 之前建立的 28 段對話**一律為空**，那也是「沒資料」不是「沒人用」。
  ⇒ 報告尾端必須註明訊號從哪天開始有，否則這個新數字自己會變成下一個誤導。
- **教訓**：**任何「數量 ＝ 0」的結論，先問「這個訊號存在多久了、涵蓋哪些資料」。**
  訊號比資料年輕的那一段，只能讀成未知，不能讀成零。
  ⇒ 量測工具的輸出要**自帶起算時間**；沒有它的 0 是不可解讀的。
- **來源**：[history/106](history/106-三刀被做成量不到的-加欄地基補上.md)；
  `knowledge/skills/audit-field-usage/audit.py` 尾端那行註記。

### 壞掉的 skill 比死程式碼糟——它會被照著做，而它沒有 oracle

- **理論說**：把重複的操作固化成 skill 是純賺——`history/011` 就是這麼固化
  `evaluate-and-add-source` 的（實測 → 挑 → 加進名冊 → 跑 digest 驗證 → 反流）。
- **實際發生**：拉模式隨 `history/068` 退役後，那個 skill **停在退役前的世界**：
  第 4 步要跑的 `knowfield … digest` 子指令**已不存在**（`cli/__main__.py` 只剩 `ask` 與 `ingest`），
  `last_fetch_at` 全專案零處寫入，名冊裡那 15 筆是空庫時程式自動塞的。
  它壞了三週沒人發現——而且**已經投影到兩個 skillDirs，Codex／Gemini 也看得到**。
- **教訓**：死程式碼靜靜躺著，**壞掉的 skill 會被照著執行**；而 skill **沒有 oracle**
  ——不進 CI、沒有測試、沒人跑。⇒ **每次退役一個子系統，要順手掃一遍 skill 有沒有指向它。**
  可機械化的最小檢查（本次實際跑的）：把 skill 裡出現的 CLI 子指令與檔案路徑，
  對照專案實際註冊的子指令與檔案系統。掃完發現**只有那一個爛掉**，其餘引用的路徑都在。
  ＋退役 skill 要**連投影一起移除**——留在 `skillDirs` 裡就還是可被呼叫的。
- **不適用的時候**：judgment 型（meta）skill 講的是判準不是指令，不會因為程式碼變動而失效；
  這條專講**引用了具體指令／路徑**的 domain skill。
- **來源**：[history/107](history/107-退役會教錯路的來源skill-小腦沒有oracle.md)；
  被取代的 [history/011](history/011-固化-evaluate-and-add-source-skill.md)。

### 一個數字看起來像「沒人用」之前，還有四個更可能的解釋——「沒人用」是最後才輪到的

- **實際發生**：同一週撞到三次，形態各不相同：
  ① **訊號還沒開始記**——帶著文章／來源聊在儲存層零痕跡（spec 044 才補上）。
  ② **機制已經退役**——來源訂閱報 15/0，但根本沒有抓取路徑（[history/107](history/107-退役會教錯路的來源skill-小腦沒有oracle.md)）。
  ③ ⚠️ **量錯欄位**——對話清單讀 `conversations.why_node_id`，而事實來源是
     `why_nodes.conversation_id`；正式庫真的是由來的 12 段，畫面只認得 4 段。
     **程式自己的註解就寫著哪邊是事實來源**，而 UI 讀了另一邊。
  ④ ⚠️ **那個活動使用者還沒開始做**——`/api/source/distill`（來源頁「整理成核心理解」）
     上線至今 **0 條**。我正要把它當訊號解讀，使用者說：「**我只是還沒開始收集來源，
     到時候會用到**」。⇒ 那是**還沒到**，不是**不要**，而這兩者在數字上長得一模一樣。
     ＋前三種我自己查得出來，**第四種只有使用者知道**。
- **教訓**：**看到低得可疑的數字，依序排除：訊號多久前開始記？產生它的機制還在嗎？
  我讀的是不是事實來源？使用者進入那個階段了嗎？** 四個都排除掉，才輪到「沒人用」。
  跳過就會去解一個不存在的問題——而且很可能順手砍掉一個其實會動的功能。
  ＋**「還沒到」與「不要」的處置完全相反**：前者該**停止量它**（不然每次審計都跳出來
  浪費一輪對話），後者才該重新想設計。分不出來就**問人**——那一句話比再量三天都準。
  ＋前兩次錯在**報告**，第三次錯在**使用者的畫面**：同一個病灶，代價差一個量級。
  ＋當一份資料有兩個地方可以放（雙向連結、快取欄、反正規化），**寫下哪邊是事實來源，
  並讓讀取端只讀那一邊**——不然讀錯的那邊不會報錯，只會少顯示東西，而少顯示沒有人會抱怨。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/108](history/108-對話由來徽章讀錯欄位-儀器說謊的第三種形態.md)；
  spec 044／045、[history/106](history/106-三刀被做成量不到的-加欄地基補上.md)、[107](history/107-退役會教錯路的來源skill-小腦沒有oracle.md)。

### 量測工具報出來的 0，可能是工具在說謊——先確認那條路還在，再解讀那個數字

- **理論說**：`audit-field-usage` 報「來源訂閱：15 個訂閱、**0 個真的供過料（0%）**」，
  照判準讀就是「這功能沒人用」。
- **實際發生**：查下去發現**根本沒有抓取路徑**——沒有 `digest` 指令、`last_fetch_at` 零處寫入、
  那 15 筆是自動種的。⇒ 那個 🔴 不是「沒人用」，是**工具把殘骸列成現役功能**。
  照著它讀會去解一個不存在的問題。
- **教訓**：**看到 0 之前，先確認「產生這個數字的那條路現在還通不通」。**
  一個量測項的前提（機制還在）跟它的數值（有沒有人用）是兩件事，而報告只顯示後者。
  ⇒ 退役任何子系統時，**量測工具裡對應的那一列要一起處理**——改標成殘骸存量，
  不要留一個永遠紅的假訊號。假訊號會訓練人忽略紅色。
  ＋這是「量不到 ≠ 沒人用」的第二種形態：那次是**訊號還沒開始記**，這次是**機制已經沒了**。
- **來源**：[history/107](history/107-退役會教錯路的來源skill-小腦沒有oracle.md)；
  `knowledge/skills/audit-field-usage/audit.py` 那一列的註解。

### 用行號引用一份會一直長大的檔，等於寫下一個有到期日的指標

- **理論說**：引用一條教訓時附上 `experience.md:890`，讀的人一跳就到，比翻標題快。
- **實際發生**：`experience.md` 是 append-heavy（本週 47 → 62 條），**每加一條就把後面所有行號往下推**。
  `/knowie-judge` 機械掃過 16 處行號引用，**4 處已經指錯**——其中一處在**正式程式碼裡**
  （`src/knowfield/output/article.py`）。它們壞掉時沒有任何東西會報錯。
- ⚠️ **而我修的時候差點讓它更糟**：第一版用「行號現在屬於哪條教訓」機械替換，
  於是把「指向一堆看不懂的內文」（明顯壞掉）換成了「**指向一條真實但不相干的教訓**」（自信地錯）。
  **可見的壞比隱形的錯便宜**——機械修正之前要先確認你在修的是指標，不是語意。
- **教訓**：**引用要錨在不會漂的東西上。** 判準：**這個錨點會不會因為別人在別處新增內容而失效？**
  會 → 換一個。教訓的**標題**就是好錨點——knowie 自己要求標題是「可以拿文字去判斷的主張」，
  而那種句子既穩定又可 grep。同理適用於：任何附行號的檔案引用、任何指向可變清單第 N 項的說法。
- **來源**：`/knowie-judge`（2026-08-23）；本檔全部 16 處引用已改為標題錨點。

### 「還沒做的」通常不是尾巴，是洞——用水位線表示進度，等於把答案藏起來

- **理論說**：「上次收到哪裡」用一個水位線（`max(已處理)`）就夠了，前面已處理、後面沒處理。
  程式裡原本的 `distill_gap` 就是這個模型。
- **實際發生**：量了正式庫，覆蓋**不連續**——對話 44 收到第 46 則卻缺第 3–8 則、
  對話 31 缺 [1,2,9–12]。⚠️ 水位線會回報「收到第 46 則」，**而使用者要找的正是那個洞**。
- **教訓**：**進度不是一維的，除非你的流程強制它是。**
  凡是「可以跳著做」的工作（挑段落收、抽樣校對、逐條審核），
  進度就是**集合**不是**位置**——用位置表示會把已知的缺漏顯示成「已完成」。
  判準：**問「這件事能不能跳著做」。能 → 別用單一水位線。**
- ⚠️ **測起來有陷阱**：**沒有洞的 fixture，水位線實作也會綠。**
  一份「1–20 已收、21–46 沒收」兩種實作都通過。測試必須特地放一個**中間的洞**。
  ⇒ 這是「沒被錯誤實作撞過的測試」的變體：**撞得倒與否取決於 fixture 的形狀，不只取決於斷言的措辭。**
- **來源**：[history/111](history/111-水位線被實測推翻-洞才是使用者要找的.md)；
  `frontend/src/lib/coverage.ts` ＋ 其測試（反向攻擊撞紅 4 條）；commit `e72c2f0`。

### 發現「同一件事有兩套介面」時，我的第一反應是補齊——使用者兩次都選拿掉

- **實際發生**：spec 046 之後我自己發現覆蓋顯示只加在聊天頁、檢視頁沒有，
  於是問使用者「要我補上嗎」。他的回答是「**我現在對話不想分檢視和接著聊了**」。
  ⚠️ **同一個模式的第二次**：spec 041 那次我加了「換你說」分隔線＋三顆起手式，
  他說「我純粹就只是想讓那文章當對話的第一則，**不用做什麼太特殊處理**」。
- **教訓**：**「兩套介面」的預設修法是砍到一套，不是補到兩套齊全。**
  補齊會讓維護成本 ×2、而且下一個功能又要再問一次「這個要不要也加到那邊」。
  判準：**兩套裡有一套是另一套的子集嗎？** 是 → 砍子集。
  這次盤點下來聊天頁是超集，只差一個「重生標題」——搬過去就結束了。
- ⚠️ **但砍之前必須逐項盤點能力**：刪頁面最容易的錯是順手弄丟一個**只有那頁有的動作**。
  用「可用動作集合的差 ＝ 0」當驗收，而不是憑印象說「反正那頁沒什麼」。
  ＋順帶會撈到既有的壞掉：這次發現側欄的「目前這段」高亮**只在要被刪的那頁亮得起來**
  ——砍之前不盤點，就會把一個一直壞的東西連同它的最後一個生效場景一起刪掉，
  然後永遠不知道它壞過。
- **來源**：[history/112](history/112-對話的檢視頁退場-兩套介面是我自己造的.md)；
  [history/101](history/101-文章當第一則-從掛在對話上改為開新對話.md)；commit `822d168`。

### 一個門檻要**兩邊**都量：已知答案給上界，結構崩壞給下界

- **實際發生**：跨 base 分群的門檻，我照「先拿已知答案校驗儀器」做了——
  噪音 99.9 分位 0.561、一對我**知道**是同一條的句子拿 0.617 ⇒ 算出「建議門檻 0.59」。
  數字有校驗背書，看起來很專業。實跑 0.59：**一群 178 條**，好幾個判準被串成一坨。
- **教訓**：校驗配對只回答「**不能再高**」（高過它，你知道是對的那對就漏了），
  它**完全沒有**回答「不能再低」。低的那側由**結構崩壞**守著：
  連通分量會傳遞性串連（A≈B、B≈C ⇒ A 和 C 同群，即使它們不像）。
  ⇒ **兩個界一起量，取「結構還沒垮」的最高值。**（實測 0.62：49 群、最大群 20。）
- ⚠️ 只量一邊比不量更難懷疑——**它已經通過了一道檢查**。
  一個有校驗背書的錯數字，會讓下一個人省掉查證。
  ⇒ 校驗過的儀器要標明**校驗到的是哪一側**，而不是標明「已校驗」。
- **來源**：[history/137](history/137-兩條規則被同一天的實跑推翻-而測試都是綠的.md)；
  `knowledge/skills/knowie-crosscheck/`（`最大一群` 那行就是下界的量法）。

### 去重／冪等的身分，不能是**使用者被鼓勵去改**的那個欄位

- **實際發生**：跨 base 收件匣的匯入去重比對 `claim` 文字。單元測試綠。
  實跑時收下一條並**把它改寫成自己的話**——那正是這個功能鼓勵的動作，
  介面特地放了輸入框——重匯，它復活了。
- **教訓**：**身分要挑「這個功能不會去動」的那個欄位。**
  這裡挑成員原文（`ladder`）：冊封只動狀態與 claim，動不到它。
  判準：**問「這個欄位，功能被正常使用時會不會變？」會 → 它不能當身分。**
- ⚠️ 那條單元測試冊封時**沒有改寫**——所以它綠得毫無意義：它測的是一條沒有人會走的路。
  這是「一條沒有被錯誤實作撞過的測試」的下一步：
  **撞得倒與否，取決於你餵的是不是「這個功能被真正使用的樣子」。**
  對抗性攻擊測的是**實作**寫錯；只有實跑測得到**測試的假設**寫錯。
- **來源**：[history/137](history/137-兩條規則被同一天的實跑推翻-而測試都是綠的.md)；
  `src/knowfield/store/repository.py` `import_borrowed`；
  `tests/unit/test_borrowed_inbox.py::test_reimport_after_rewriting_the_claim_does_not_duplicate`。

### 開一個**寫入口**時，去問「哪一支**讀**這份資料的程式，假設現在假了？」

- **實際發生**：跨 base 量測（今名 `knowie-crosscheck`）讀 `experience.md`
  每個 `###` 當一條獨立教訓——
  那個假設在**只能讀**的時候是對的。而 `knowie-pull` 一旦能把借來的判準**寫進去**，
  下一次量測就會讀成「這個 base 也獨立撞到了」⇒ 計數餵回推薦、推薦餵回計數。
- **教訓**：禁令早就寫過（「只算『撞到』，不算『借走』」），但那時**沒有寫入口**
  ⇒ 讀取器不需要知道這件事。**是新的寫入口讓一條舊禁令變得可違反的。**
  判準：**加一個寫入口，就去盤點每一個讀這份資料的消費者，問它依賴了什麼「因為只有人手寫」才成立的假設。**
- ⚠️ 這種洞不會由**新功能的測試**抓到——它壞的是**舊功能**，而舊測試餵的還是舊形狀的資料。
  ⇒ 有效的測法是**來回**：用新的產生器寫出來的東西，餵給舊的讀取器。
- **來源**：[history/138](history/138-pull有push沒有-而對稱的名字會蓋出不對稱的東西.md)；
  `knowledge/skills/knowie-crosscheck/crosscheck.py`（`BORROWED_MARK` 產生器與讀取器共用）；
  `tests/unit/test_knowie_pull.py::test_round_trip_written_then_read_is_skipped`。

### 一個標記的「拿掉」，必須跟「加上」一樣機械

- **實際發生**：借來的判準要在真的撞到之後**升格**。而那個標記在區塊裡出現了**兩次**
  （標記行 ＋ 那句「怎麼升格」的說明本身引用了它）。照說明刪掉標記行的人，
  另一處還在 ⇒ 升格失敗、量測照樣跳過它，**而沒有人會發現**。
- **教訓**：**加是程式做的（一次寫對），拿掉是人做的** ⇒ 拿掉必須「**刪一行就完成**」。
  判準：**問「這個狀態要退回去，人要正確地做幾件事？」超過一件 → 會有人做一半，而系統看起來正常。**
  現在寫成產生器裡的一句 `assert block.count(MARK) == 1`。
- ⚠️ 這是**對抗性驗證漏掉的那一種**：四種攻擊裡有一種撞不紅，
  而撞不紅的原因就是這個 bug 本身（第二處標記讓測試不管怎樣都綠）。
  ⇒ **一種攻擊沒撞紅，先當成「這裡有東西」，不是「這條測試多餘」。**
- **來源**：[history/138](history/138-pull有push沒有-而對稱的名字會蓋出不對稱的東西.md)；
  `tests/unit/test_knowie_pull.py::test_marker_appears_exactly_once`。

### 一個「他本來就會知道」的命中率，量的是**冗餘**，不是價值

- **實際發生**：測「跨 base 拉來的判準能不能解冷啟動」，用留一法——
  假裝某個 base 是空的，從其餘 12 個算出「第一天會發給它的 12 條」，
  再看**它後來自己撞出來的教訓**對得上幾條。天花板不綁的兩個大 base：**4/24 嚴格命中**。
- **教訓**：那個數字有兩種讀法，而**同一個數字兩邊都支持**——
  「給的多半不是你會撞到的 ⇒ 沒用」，或「給的多半是你**不會**撞到的 ⇒ 那正是價值」。
  ⚠️ **一條你本來就會自己想到的判準，給你等於沒給** ⇒ 這個測法量的是重疊，
  而重疊高才是壞消息。判準：**問「這個指標變高，是不是我要的？」答不出來 ⇒ 它量錯了。**
- ⚠️ 這是「先拿已知答案校驗儀器」的**下一關**：儀器準確、門檻校驗過、數字算對了，
  **而它回答的是另一個問題**。校驗保證不了這件事——校驗只管量得準不準。
- ⓘ 這個測法**不是白做**：它照出兩個機械缺陷（別的 base 的 `🔴`／`⭐`／`<u>` 跟著判準跑、
  以及事件紀錄型的條目不可攜）。⇒ **一個回答不了主問題的測，仍可能是最好的缺陷偵測器。**
- **來源**：[history/139](history/139-留一法回答不了冷啟動-而它照出了別的.md)；
  `knowledge/skills/knowie-crosscheck/crosscheck.py` `_clean()`；
  `tests/unit/test_knowie_pull.py::TestPortableClaim`。

### 一份 90% 是雜訊的報告等於沒有報告——**擋要寬容，報要精確**

- **實際發生**：跨 base 死指標檢查第一版報出 **467 條**，其中真的只有個位數。
  已知答案（`digest/builder.py`）**有找到**，但埋在雜訊裡。
  雜訊全來自我在猜什麼算路徑：`3/64`（分數）· `why_nodes.kind`（欄位）·
  `store/schema.py`（真的存在，只是寫的是**部分路徑**）· `history/040`（相對於 `knowledge/`）。
- **教訓**：**「這條算不算」如果需要猜，就收窄到不用猜的那個子集**，
  而不是全部報出來讓人自己篩。收窄成三條機械規則後 **467 → 83**，已知答案仍在最上面。
- ⚠️ 這跟「[命名不該由程式否決](history/134-命名不該由程式否決-而我量錯了泛不泛.md)」
  **不衝突，是它的另一半**：
  **擋**（阻止一個動作）要寬容，因為擋錯會弄丟好答案；
  **報**（產生一份清單）要精確，因為報錯的失敗方式是**整份被關掉**，不是被發現錯了。
  判準：**問「這個輸出錯了會怎樣？」——被忽略 ⇒ 要精確；擋住人 ⇒ 要寬容。**
- ⚠️ 而語意判斷偽裝成機械判斷**比純語意判斷危險**：輸出看起來像一份客觀清單。
- **來源**：[history/140](history/140-死指標報告的雜訊-而我在猜什麼算路徑.md)；
  `src/knowfield/store/repository.py` `dead_refs`；
  `tests/unit/test_fetch_bases.py::TestDeadRefsIsMechanical`。

### 測試餵的是「那一步的**產物**」時，那一步本身沒有被測到

- **實際發生**：spec 072 的 16 條測試全餵**預先抓好的**假資料。
  把 `default_branch` 改成寫死 `"main"`——**全部照樣綠**。
  而實測 VizGPT 的預設分支是 `knowledge-python`，寫死就是 404 或抓到錯的分支。
- **教訓**：判準是**問「這條測試餵的東西，是不是我要測的那一步算出來的？」**
  是產物 ⇒ 那一步在測試裡**不存在**。修法是把它的**外部邊界**樁掉
  （這裡是 HTTP 那一層），然後斷言**它實際發出的請求**，而不是斷言結果長得對。
- ⚠️ 這是「一條沒有被錯誤實作撞過的測試」的第三個變體：
  前兩次是**斷言太鬆**與**餵的不是功能被真正使用的樣子**，這次是**管線的上游被跳過**。
- **來源**：[history/140](history/140-死指標報告的雜訊-而我在猜什麼算路徑.md)；
  `tests/unit/test_fetch_bases.py::TestFetchUsesTheRightBranch`。

### 一個「給你判斷用」的數字，沒有對應的旋鈕就不是判斷用的，是裝飾

- **實際發生**：跨 base 分群照規矩回了**兩個界**（校驗配對 0.617／噪音上緣 0.557／最大一群 2），
  然後我把門檻**寫死在 0.62**。使用者看得到那三個數字，**卻不能拿它做任何事**。
- **教訓**：判準是**問「他看到這個數字之後，能做什麼？」**——答不出來就是裝飾，
  而裝飾會讓介面看起來很嚴謹，實際上把判斷權留在寫程式的人手上。
  ⇒ 要嘛給旋鈕，要嘛別顯示。
- ⚠️ 這跟「[不可反駁的理由等於沒有理由](history/127-建議整理-而不可反駁的理由等於沒有理由.md)」
  是同一族的下一步：那條講**理由**要可查證，這條講**數字**要可作用。
- **來源**：[history/141](history/141-下界不是門檻的性質-是語料規模的性質.md)；
  `frontend/src/pages/BasesPage.tsx` 的門檻選擇。

### 一個門檻的下界不是門檻的性質，是**語料規模**的性質

- **實際發生**：13 個 base（1,396 條）時，門檻 0.59 產生**一群 178 條**（黏成一坨）；
  而正式庫只有 3 個 base（588 條）時，同一個 0.59 的**最大一群只有 3**，群數還多了六倍。
- **教訓**：傳遞性串連需要**密度**——base 少、句子少，A≈B≈C 的鏈接不起來。
  ⇒ **「0.62」不是答案，是當時那份語料的答案。**
  判準：**每次都要重量兩個界，不要抄上次那個數字。**
- ⚠️ 這是「[一個門檻要兩邊都量](history/137-兩條規則被同一天的實跑推翻-而測試都是綠的.md)」
  的下一層：不只要量兩邊，**兩邊都會隨語料移動**。
- **來源**：[history/141](history/141-下界不是門檻的性質-是語料規模的性質.md)；
  `src/knowfield/organize/crossbase.py` `calibrate`／`group` 的 `largest`。

### 延後一個決定的正確理由是「**還沒有資訊**」，不是「還沒想好」

- **實際發生**：spec 073 把「外部知識該不該進搜尋／檢索語料」標成「**另一個決定**，不順手做掉」。
  一刀之後的雙模式 UI 直接把它逼出來（「⌘K 必須跨模式」），而那時答案**自明**了
  ——因為外部知識終於有了讀者。在那之前問這題，是問一個還沒有人要用的東西該怎麼被用。
- **教訓**：判準是**問「什麼事情發生之後，這題會自己有答案？」**
  答得出來 ⇒ 等那件事，而且**把那件事寫進 out of scope**（不是只寫「延後」）。
  答不出來 ⇒ 那不是延後，是拖。
- ⓘ 而答案通常比想像簡單：這次是「**進搜尋、不進檢索語料**」——
  因為「找得到」和「當成你的地基」**本來就是兩件事**，只是沒有讀者時看不出來。
- **來源**：[history/142](history/142-雙模式-而它逼出上一刀延後的那個決定.md)；
  `specs/073-crossbase-in-field/spec.md` 的 Out of scope。

### 還原一次對抗性攻擊，要還原「我剛剛改的那一處」，不是「那個檔案」

- **實際發生**：對抗性驗證時用 `git checkout src/knowfield/web/app.py` 還原一個攻擊，
  **把那個檔案裡這一刀所有未提交的改動一起洗掉**（兩條新路由＋一個排序）。
  是測試一條一條紅回來，才知道少了什麼。
- **教訓**：**`git checkout` 的粒度是檔案，而攻擊的粒度是一行。**
  ⇒ 攻擊前先 `cp` 一份備份、用備份還原；`git checkout` 只在**那個檔案這一刀沒動過**時才安全。
- ⚠️ 這種錯的可怕之處在於**它看起來像成功**：測試會綠（因為攻擊被還原了），
  少掉的功能要等下一次全跑才現形。
- **來源**：[history/142](history/142-雙模式-而它逼出上一刀延後的那個決定.md)。

### 「它算哪個模式／哪一區」是**路由**的事，不是樣式的事

- **實際發生**：「管理專案」那一頁看起來不像開發模式的一部分。我第一反應是調樣式，
  ⚠️ 而真正的原因是它在 `/bases`，而 `dev` 判斷是 `pathname.startsWith("/dev")`
  ⇒ 點進去**側欄整個切回另一套**，版面也吃不到 `isFull`。搬成 `/dev/bases` 之後，
  側欄高亮、全寬版面、模式歸屬**三件一起對了**——因為它們讀的是同一個判斷。
- **教訓**：判準是**問「這個『我在哪』的訊號，是從路徑算的嗎？」**
  是 ⇒ 那就用路徑修，別用樣式補；改樣式只會讓三個地方各自為政，而它們遲早不同步。
- ⓘ 同一族的第二個：判斷「我在哪」要用 `useLocation`，不是 `window.location`
  ——後者**不隨路由更新**，那個訊號會停在你第一次進來的那一頁，而且不會報錯。
- **來源**：[history/142](history/142-雙模式-而它逼出上一刀延後的那個決定.md)。

### 刪除的提示要說「**留下什麼**」，不只說「要刪什麼」

- **實際發生**：移除一個外部知識庫，會清掉它的知識與目錄樹，但**已經收進場的借來判準不動**
  （那是 `why_nodes`，你冊封過）。而它本身是**別人 repo 的快照**，重加一次就回來。
  兩件事不講，使用者只能猜——猜的結果是**不敢按**，或**按了才後悔**。
- **教訓**：判準是**問「按下去之後，他會不會需要知道某樣東西還在？」**
  會 ⇒ 寫進提示。⚠️ 而這也決定了**要不要留遺骸**：
  可重建的（外部快照）**真的刪**；不可重建的（你自己的知識）走封存→抹除。
  ⇒ **「刪除」有幾種，取決於那個東西還能不能長回來。**
- **來源**：[history/142](history/142-雙模式-而它逼出上一刀延後的那個決定.md)；
  `repository.delete_ext_base`（四張表一起清，`why_nodes` 不動）。

### 「導覽到某處」關抽屜；「**換一組導覽**」不關

- **實際發生**：手機上切到開發模式，側欄自己收起來。原因是模式切換沿用了每個導覽連結
  都在用的 `onNavigate`（＝關抽屜）。那個 handler 的用意是「你選了目的地，抽屜可以收了」
  ——對所有連結都對，**唯獨對模式切換是反的**：你切過去正是要看**新的那份側欄**。
- **教訓**：判準是**問「按完之後，他要看的東西在抽屜裡還是抽屜外？」**
  在外面 ⇒ 關；**在裡面 ⇒ 不能關**。一個共用的 handler 套到所有元素上時，
  這個問題就是它的邊界。
- ⓘ 同一族的第二條：**看到小字先問「它是給人讀的、還是給人按的」**——
  按的那些不能小（這次是手機上的「← 檔案」，`text-xs`）。
- **來源**：[history/142](history/142-雙模式-而它逼出上一刀延後的那個決定.md)；
  `frontend/src/components/ModeSwitch.tsx`（刻意不收 `onNavigate`）。

### 一個從結構算出來的分類，不該取代那個結構當導覽——它只該是**篩子**

- **實際發生**：knowie 的「層」（experience／concepts／history…）是**從路徑導出**的（`layer_of`）。
  我拿它當開發模式的導覽：一排層的籤 ＋ 一份平面檔案清單。
  ⚠️ 而那個形狀**說不出巢狀**——`skills/knowie-pull/SKILL.md` 壓成一行檔名之後，
  「它在哪個 skill 底下」就不見了。使用者一句「用檔案樹呈現會比較好」點破。
- **教訓**：判準是**問「這個分類是我算出來的，還是資料本來就長這樣？」**
  算出來的 ⇒ 它是**篩子**（可以拿來過濾、統計、排序），**不是導覽**；
  導覽要照資料原本的結構走，否則使用者心裡的地圖跟畫面對不上。
- ⓘ 而**位置進網址、視線不進**：選了哪一份是位置（`?doc=`），展開了哪些資料夾是視線
  ——後者塞進網址，分享出去的連結會帶著你的展開狀態。
- **來源**：[history/142](history/142-雙模式-而它逼出上一刀延後的那個決定.md)；
  `frontend/src/components/FileTree.tsx` ＋ 其 vitest（巢狀、排序、同名不互吃）。

### 使用者描述版面時，**先畫成一張欄位表再動手**

- **實際發生**：「主要區域的左邊是檔案樹」——我**連錯兩次**，兩次都把「左邊」讀成「側欄」。
  第一版做成側欄＋清單＋預覽擠在同一個容器（字行只剩約 30rem）；
  第二版把檔案樹搬進側欄、主區只剩預覽。正確的是 IDE 的**三段**。
- **教訓**：**「左邊」是相對於某個容器說的，而容器是誰，只有畫出來才會發現雙方講的不是同一個。**
  判準：**動手前先寫出「哪一欄、放什麼、多寬」三列**，讓對方一眼看得出對不對
  ——那比做完再問便宜一個數量級。
- ⚠️ 而「做出來給他看」在這裡**不夠**：兩版都能跑、都不報錯，錯的只是**它不是他要的形狀**。
- **來源**：[history/142](history/142-雙模式-而它逼出上一刀延後的那個決定.md)。

### 兩塊介面要看同一個選取時，先問「**它能不能是網址的一部分**」

- **實際發生**：開發模式第一版把「選專案／選層／選檔」放在主區，變成
  側欄＋清單＋內容**三欄**，內容擠到剩一條。使用者要 IDE 的形狀（左檔案樹、右那一份檔案）
  ⇒ 要把選取整個搬到側欄。而**那次重排幾乎沒有成本**——因為選取一開始就在 URL（`?base=&layer=&doc=`），
  兩塊各自讀，**不需要共享狀態、不需要溝通**，搬的只是 JSX。
- **教訓**：判準是**問「這個選取，重整之後該不該還在？」**——該 ⇒ 它是網址的一部分。
  而那個決定的**紅利在重排時才兌現**：狀態在 URL，換版面是搬 JSX；狀態在元件裡，
  換版面要一路傳 props 或開 context，而那個成本會讓人乾脆不換。
- ⓘ 順帶的第二個紅利：可分享、上一頁有用、⌘K 可以直接把人**送到另一個模式的某一份檔案**。
- **來源**：[history/142](history/142-雙模式-而它逼出上一刀延後的那個決定.md)；
  `frontend/src/components/DevSidebar.tsx` 與 `pages/DevPage.tsx` 各自讀同一組 query。

### 蓋橋之前先量**兩岸有沒有東西要過去**——而那不在任何一份 spec 的「前提」欄裡

- **實際發生**：雙模式的 ⛓「讀別人的文件時顯示你自己相關的理解」——
  設計合理、實作便宜（`retrieve_corpus` 現成）、技術前提**全部成立**，spec 開了、端點寫好了。
  ⚠️ 而量測否決了它：40 份外部文件對場裡 80 條理解，最高相似度
  **最高 0.560／中位 0.463／最低 0.367**——範圍只有 0.19 寬、**沒有斷點**。
  原因不是門檻：場裡是 **AI／ML**，那三個 base 是**軟體工程**，兩邊講的不是同一件事。
  （最直接的證據：三份毫不相干的文件，最相關的**都是同一條**。那是雜訊底線。）
- **教訓**：**一座橋的價值等於兩岸真的有東西要過去。**
  判準：**動工前先量「這兩邊真的有關係嗎」**——用實際資料算一次分布，
  ⚠️ **有沒有斷點**比平均值重要：連續而沒有斷點 ＝ 那個相似度沒有在區分任何東西。
- ⚠️ 而技術前提清單**檢查不到這件事**：可行性全綠，不成立的是**假設本身**。
  ⇒ 前提要多一列：「這兩邊真的有關係嗎」，而它的驗證方式是**量**，不是想。
- ⓘ 失敗方式是沉默的：永遠顯示三條無關的、或永遠顯示「沒有」——
  兩種都會**被學會忽略**，而被忽略的介面不會報錯。
- **來源**：[history/143](history/143-先量重疊再蓋橋-而那座橋沒有兩岸.md)；
  `specs/075-cross-mode-links/spec.md`（保留為當時的量測）。

### 一個門檻是為**某個用途**調的，換用途就要重調

- **實際發生**：`retrieve_corpus` 的 `min_score=0.10` 是給「聊天時撒網」用的
  ——那裡寧可多給，因為漏掉一條相關的比多給一條貴。
  ⚠️ 而拿同一個門檻當「有沒有真的相關」用，結果是**永遠有三條**。
- **教訓**：判準是**問「這個門檻當初是為了避免哪一種錯？」**——
  避免漏報的門檻**不能**拿去做需要精確的事。
  （同一族：「**擋要寬容，報要精確**」。）
- **來源**：[history/143](history/143-先量重疊再蓋橋-而那座橋沒有兩岸.md)。

### 「要不要讓 X 進來」答不漂亮時，先問「**進到哪裡**」

- **實際發生**：spec 074 我把問題寫成「外部知識該不該進檢索語料」，答「進搜尋、不進語料」。
  答案不錯，但**問題太窄**——使用者要「對專案也可以聊」時就卡住了：要聊就得有語料。
  ⚠️ 正確的問法是「**哪一個場**」：思考模式的語料是你的理解，開發模式**站在某個專案裡**時
  是那個專案的 `knowledge/`。那道膜沒有變薄，是**多了第二個場**。
- **教訓**：判準是**問「有沒有第二個容器，讓這兩件東西各自完整？」**
  有 ⇒ 兩個場比「一個場加一條例外」乾淨得多，而且例外會長出更多例外。
- ⓘ 檢驗方式：新的那個場**不能讓舊的測試變鬆**。這次 spec 074 的兩層測試照樣綠，
  而且新場複製了同一組形狀的測試。
- **來源**：[history/144](history/144-對專案也能聊-而問題不是進不進語料是哪一個場.md)；
  `retrieve_corpus(..., entries=, vectors=)`。

### **檢索寧可寬，回答必須嚴**——同一個門檻換不了用

- **實際發生**：站在專案裡問它的知識庫，沿用 `rag_min_score`（0.10）
  ⇒ 問一個它根本不談的題目也回 6 段。量過之後：明顯離題的（義大利麵 0.198／房價 0.263）
  在 0.30 以下切得乾淨，⚠️ **而「技術性但不在這個 base」（0.441）切不掉**
  ——它跟真正相關的最低分（0.423）**重疊**。
- **教訓**：沒有斷點時，**決定門檻的是「錯了會怎樣」**：
  **檢索**給你段落、帶出處 ⇒ 錯了你一眼看得出來 ⇒ **寧可寬**；
  **回答**替你決定 ⇒ 錯了你不知道 ⇒ **必須嚴**。
  ⚠️ 這也是為什麼 [143](history/143-先量重疊再蓋橋-而那座橋沒有兩岸.md) 那一刀被否決
  而這一刀沒有——**同樣沒有斷點，但那一刀是回答，這一刀是檢索。**
- **來源**：[history/144](history/144-對專案也能聊-而問題不是進不進語料是哪一個場.md)；
  `app.py` 的 `_ASK_MIN = 0.35`（註解裡有那六個量測數字）。

### 出貨的那句話要描述**它做得到的**，不是那份 spec 的**標題**

- **實際發生**：階段 70 第一刀出貨，我說「**對專案也能聊**」。
  而 `/api/bases/{bid}/ask` **只回段落**——沒有合成、沒有多輪。那是**搜尋**，不是聊。
  ⚠️ 標題（vision 與 spec 都寫「聊」）替實作宣稱了它沒做到的事，而我照著念。
- **教訓**：**標題是打算，出貨說明是現況。** 兩者不同時，讓標題替它宣稱
  就是在放大自己的成果——而且**下一個讀 vision 的人會以為那件事已經有了**。
  判準：**寫出貨說明前，先把那個功能自己跑一次，照它實際回什麼寫。**
- **來源**：[history/145](history/145-問完要留下東西-而合成的門檻另外量.md)。

### 兩件東西分不開時，**選失敗方向對的那一邊**——而退路不算失敗

- **實際發生**：合成的門檻。實測「知識庫要怎麼收斂」（真的相關）**0.442**
  vs「Transformer 的殘差匯流排」（不相關）**0.441**——⚠️ **分不開**，
  而「第 1 段與第 6 段的落差」也救不了（0.057 vs 0.039）。
- **教訓**：分不開時不要硬調參數，改問**「錯哪一邊比較便宜」**。
  這裡：亂合成 ⇒ 你讀了假答案還不知道；不合成 ⇒ **退回已經在跑的檢索模式**。
  ⇒ 規則變成「**最高那一段夠強才合成**」，而它拒答一些真相關的問題——那是可接受的，
  因為**退路不是失敗，退路是另一個功能**。
- ⚠️ 而退下去時**要說出為什麼**（「材料不夠強 0.44，要 0.55」），
  否則使用者讀到的是「它答不出來」。
- **來源**：[history/145](history/145-問完要留下東西-而合成的門檻另外量.md)；
  `app.py` 的 `_ANSWER_MIN_TOP = 0.55`（註解裡有那三組量測）。

### 為了隔離而清空某個輸入時，**要同時告訴模型「為什麼是空的」**

- **實際發生**：站在某個專案裡聊時，我刻意讓 `roots = []`（你的理解不該當那個專案的地基）。
  而第一次實跑，回答的第一句是「**你目前還沒有存下自己的理解**」——⚠️ 使用者有 **80 條**。
  那個空缺是**我為了隔離造出來的**，而模型把它讀成一個事實。
- **教訓**：判準是**問「這個輸入是空的，是因為它真的空，還是因為我把它擋掉了？」**
  是後者 ⇒ 一定要把**理由**一起送進去，否則模型會把**你的實作決定**敘述成
  **關於使用者的事實**。那是憑空生資訊，只是來源不是幻覺，是你。
- ⚠️ 而它**不會報錯**：那句話讀起來完全通順，只有知道實情的人看得出不對。
- ⓘ 同一族：標籤也要跟著換——那個專案的判準被叫成「你收藏的」，
  聽起來就像使用者自己的東西。
- **來源**：[history/146](history/146-兩個場一份形狀-而空缺不是事實.md)；
  `chat/field_chat.py` `build_field_system_prompt(roots, project)`。

### 一支多重斷言的批次改寫，**中途失敗＝整支沒寫入**

- **實際發生**：一支改四處的腳本在第 4 個 `assert` 失敗，而我以為前三個已經生效
  ——`p.write_text()` 在迴圈之後，所以**一個字都沒寫進去**。
  接著我在那個假設上繼續改，直到測試報 `_messages() takes 10 but 11 were given` 才發現。
- **教訓**：**「前面幾個應該有進去」是猜的。**
  判準：批次改寫要嘛**逐項寫入**（每項成功就落地），要嘛失敗後**重新確認每一項的現況**
  ——而確認的方式是 grep，不是回想。
- ⚠️ 這種錯的形狀跟「[git checkout 的粒度是檔案，而攻擊的粒度是一行]
  (history/142-雙模式-而它逼出上一刀延後的那個決定.md)」一樣：
  **工具的成功／失敗單位，跟我心裡的單位不同。**
- **來源**：[history/146](history/146-兩個場一份形狀-而空缺不是事實.md)。

### 一個「範圍」的概念立起來之後，**去盤點每一個消費者有沒有吃它**

- **實際發生**：spec 048 讓知識歸屬到領域、052 定了「你站的地方」、046 讓新東西生在那裡、
  053／054 讓側欄只顯示那底下的東西——⚠️ **而聊天一路沒跟上，十九個階段**。
  你站在「音樂與數學結構」裡問問題，它照樣拿「Transformer 表示」的東西回答你。
  （實測：不縮 661 筆語料，站在「數學」裡只剩 28 筆。）
- **教訓**：判準是**問「誰會讀這個範圍？」並逐一去看**——
  ⚠️ 而漏掉的往往是**最大的那個消費者**，因為它不在你當時正在改的那個檔案裡。
  它不會報錯：回答只是比你以為的**散**，而散是看不出來的。
- ⓘ 而機制常常早就有了（`domain_descendants` 是領域頁在用的），缺的只是接上去。
- **來源**：[history/147](history/147-聊天一直沒吃領域-而那個洞開了十九階段.md)；
  `list_corpus_entries(domain=)`。

### 不要在物件上 `getattr` 一個**你沒確認過存在**的欄位——猜錯是靜默的

- **實際發生**：要把理解也依領域過濾，我寫了
  `getattr(r, "domain_id", None) in ids`。而 `WhyNode` **根本沒有那個欄位**
  ⇒ 永遠是 `None`、**永遠濾掉全部**，聊天會忽然沒有任何理解可用——而且不報錯。
- **教訓**：`getattr(x, "f", default)` 的預設值**把「欄位不存在」偽裝成「值是空的」**。
  判準：**寫下 `getattr` 之前先確認那個欄位在不在**（grep 那個 dataclass），
  不在就回資料來源問，不要在物件上猜。
- ⚠️ 而它跟「[空缺不是事實](history/146-兩個場一份形狀-而空缺不是事實.md)」同一族：
  **兩者都把「我沒拿到」講成「它沒有」。**
- **來源**：[history/147](history/147-聊天一直沒吃領域-而那個洞開了十九階段.md)；
  `repository.anointed_ids_in_domain`。

### 斷言是「原始碼裡有這個字串」時，先問**改壞它的最短方式會不會留下那個字串**

- **實際發生**：spec 080 的測試是掃原始碼（有沒有寫 `ContentIngestService`、
  `set_knowledge_domain` 在不在 `if` 外面）。而三種攻擊——**只在新收時歸屬** ·
  **前綴放寬** · **不走共同出口**——⚠️ **一種都沒撞紅**。
  改成把外部那層樁掉、**真的跑一次**之後，四種全紅。
- **教訓**：**掃描器看得到「有沒有寫那個字」，看不到「它做了什麼」。**
  判準：**問「我要破壞這個行為，最短的改法是什麼？那個字串還在嗎？」**——
  還在 ⇒ 這條測試守不住任何東西。
- ⓘ 掃描器仍有它的位置：守**結構性禁令**（「這個字**不准**出現」）時它是對的工具；
  守「**有做到**」時它是空的。
- **來源**：[history/148](history/148-專案就是來源-而三種攻擊沒撞紅因為測試在掃字串.md)；
  `tests/unit/test_project_as_source.py::TestFetchCreatesSources`（樁掉 GitHub、真的跑）。

### 一種攻擊沒撞紅時，**先確認那個攻擊真的改到了東西**

- **實際發生**：「前綴放寬」我寫成 `prefix[:22]`，而 `github://timcsy/Demo/`
  ⚠️ **只有 21 個字元**——那個攻擊**什麼都沒改**，測試當然綠。
  我差點據此判定「這條規則不需要」。
- **教訓**：對抗性驗證有**兩種**綠：測試沒守住、或**攻擊沒生效**。
  判準：**攻擊之後先看那個檔案真的變了嗎**（`diff` 一下），再解讀結果。
  ⚠️ 否則你會得到一個**反向的結論**——把「我沒打中」讀成「這裡不用守」。
- **來源**：[history/148](history/148-專案就是來源-而三種攻擊沒撞紅因為測試在掃字串.md)。

### 攻擊生效、測試仍綠 ⇒ 先問**兩條路徑的產物分得出來嗎**

- **實際發生**：攻擊「預覽回頭讀快照」確實改了程式，測試照樣綠。
  ⚠️ 因為測資裡**快照和來源是同一份文字**——兩條路徑的輸出一模一樣，
  行為測試也分不出來。
- **教訓**：對抗性驗證有三種綠：測試沒守住 · 攻擊沒生效 · **兩邊產物相同**。
  判準：**讓正確路徑的產物帶一個錯誤路徑拿不到的印記**
  （一句只存在於來源的話；一份只存在於來源的檔）。
  沒有那個印記，你測的是「它有回東西」，不是「它讀對了地方」。
- **來源**：[history/148](history/148-專案就是來源-而三種攻擊沒撞紅因為測試在掃字串.md)；
  `tests/unit/test_two_modes.py` 的 `ONLY_SOURCE`。

### 兩份實作做同一件事時，**它們一定會漂，而漂掉的那一份不會報錯**

- **實際發生**：「站在專案裡」曾是**第二個場**——自己的語料表（`ext_chunks`）、
  自己的門檻（0.35）、自己的問答路由。專案落成來源之後它整個多餘，
  ⇒ **刪掉，不是停用**（表留著只為清舊列，並在 schema 寫明沒有人再寫）。
- **教訓**：它不會壞給你看，只會**慢慢答得跟另一邊不一樣**，
  而你以為你在看同一個系統。判準：**同一件事只留一條路徑**；
  真的要留第二條，就讓它**不可能被執行**（刪掉程式碼），不是靠紀律不走。
- ⓘ 收掉之後順帶治好一個更廣的洞：「你還沒存下自己的理解」不是專案專屬的
  ——`roots` 被**任何**窄領域濾空都會發生 ⇒ 「你站在哪」該掛在 `domain` 上。
- **來源**：[history/148](history/148-專案就是來源-而三種攻擊沒撞紅因為測試在掃字串.md)；
  `tests/unit/test_project_as_source.py::TestThereIsOnlyOneField`。

### 換一份資料當讀取來源時，**舊資料不會自己搬過去**——而「空的」跟「還沒搬」長得一樣

- **實際發生**：spec 080 把開發模式的樹從「快照」改讀「來源」。正式庫三個專案是
  更早抓的 ⇒ `快照 187／266／61 份、來源 0`，樹全空。
  ⚠️ 而畫面說的是「**這個專案還沒有知識庫**」——檔明明在資料庫裡。
  使用者的回報是「我怎麼看不到專案的檔案了？」
- **教訓**：換讀取來源是**一次資料遷移**，不只是改一行查詢。判準：
  **問「舊資料在新的讀法下長什麼樣？」**——如果答案跟「本來就沒有」
  **無法區分**，那就一定要多回一個數字把兩者分開（這裡是 `n_snapshot`），
  並把補救的動作放在**看到問題的那一頁**。
- ⚠️ 但**不要自動補**：一個專案就是幾百筆新來源湧進來源頁，
  而**第一次淹掉就是你唯一會發現它的時刻** ⇒ 那一下由人按。
- **來源**：[history/148](history/148-專案就是來源-而三種攻擊沒撞紅因為測試在掃字串.md)；
  `tests/unit/test_project_as_source.py::TestOldBasesSayWhy`。

### 「它是同一種東西」不等於「**它該排在同一個清單裡**」

- **實際發生**：spec 080 把專案的知識檔落成**來源**（語意上對的）。
  於是它們跟你自己收的排在同一頁、同一個清單裡
  ——⚠️ **226 份 vs 9 份**，你自己收的被壓到最底下。
  使用者：「為何開發的來源都跑到互動模式那邊了？」
- **教訓**：**同一層（語意）與同一頁（呈現）是兩個決定**，而我把它們併成一個。
  判準：把新的一批東西併進既有的清單前，**先問「數量級一樣嗎？」**
  ——差一個數量級就不是「多了幾筆」，是**把原本那些擠掉**。
- ⚠️ 濾掉呈現時，那一頁**要說出少了幾份、去哪看**：它們仍在語料裡、仍會被引用，
  不說就變成「看不到卻會影響回答」。
- **來源**：[history/149](history/149-專案是來源-不代表它該排在你的來源頁上.md)；
  `tests/unit/test_projects_not_in_library.py`。

### 一組分類在兩個地方都說得通 ⇒ 它是**鏡頭**，不是那個地方的屬性

- **實際發生**：使用者說開發模式的側欄「要幾乎跟互動模式一樣」，我讀成**長得像**，
  做了一組開發專用的導覽。他要的是**同一組五格**
  （領域／來源／互動／理解／應用），只是換成那個專案的。
  ⚠️ 而專案本來就是一個領域 ⇒ 直接問**同一支** `domainView`，五格自然成立。
- **教訓**：判準：**為第二個地方另做一組分類，等於宣告你的第一組其實不通用。**
  先問「同一組鏡頭照過去會怎樣？」——照得出來就別另做一套。
- **來源**：[history/149](history/149-專案是來源-不代表它該排在你的來源頁上.md)；
  `tests/unit/test_two_modes.py::test_dev_sidebar_mirrors_the_interaction_one`。

### 切開兩個視野時，**每一種都要照同一條線切**

- **實際發生**：專案的東西要退出思考模式。只做「來源」的話，
  ⚠️ 對話／理解／應用／子領域還在那裡——而**不一致比全有或全無更難察覺**：
  你在某一格看到專案的東西、在別格看不到，然後以為是資料掉了。
- **教訓**：判準：**那條線只能有一個定義、一個地方**
  （這裡是 `project_domain_ids()`），每一格都問它。兩套判準一定會漂，
  而漂掉的那一套不會報錯。
  ⚠️ 而**反面也要驗**：「A 看不到」做完要接著驗「B 看得到」
  ——只做一半的話那些東西就**兩邊都不見**了。
- ⓘ 這條線走在每一次領域視野上 ⇒ **沒有專案時要一個查詢結束**（否則整套測試慢 50%）。
- **來源**：[history/149](history/149-專案是來源-不代表它該排在你的來源頁上.md)；
  `tests/unit/test_one_line_two_modes.py`。

### 容器的名字不能是它底下某一格的名字

- **實際發生**：兩個模式叫 `[ 互動 | 開發 ]`，⚠️ 而**「互動」已經是那五格裡的一格**
  （💬 互動 ＝ 對話）——同一個側欄上下兩處、同一個詞指兩件事。
  使用者：「另外一個模式好像不應該叫互動模式」。改成「思考」。
- **教訓**：判準：**模式／分頁／區塊（容器）的名字，不能是它底下任何一項（內容）的名字。**
  代價不是難看，是**每次讀到都要先判斷這個是哪一個**——要花腦力的介面就這樣累積出來。
  ⓘ 它機械可判 ⇒ 落成掃描（容器的標籤 ∩ 內容的名字 ＝ ∅），不必靠人記得。
- **來源**：[history/150](history/150-模式的名字不能是它底下某一格的名字.md)；
  `tests/unit/test_two_modes.py::test_mode_names_do_not_collide_with_the_five_entries`。

### 批次改名前，先把 `「…」` 圈出來排除——**引號裡的字是記錄，不是術語**

- **實際發生**：把「互動模式」改名成「思考模式」時，替換掃過整個 repo，
  ⚠️ 而其中 9 個檔的那四個字是**引號裡的使用者原話**。改掉它等於竄改當時發生的事。
- **教訓**：判準：**改名動的是術語，不是記錄。**
  `「…」` 內、`history/` 整層、commit 訊息——這些用的是**當時的詞**，不跟著改。
  改完再掃一次 `「[^」]*<新詞>[^」]*」` 確認引號內容沒被動到。
- **來源**：[history/150](history/150-模式的名字不能是它底下某一格的名字.md)。

## 關鍵延伸（主題觸發必讀）

### 偵測器缺了輸入不會回報「不知道」，它會回報「乾淨」——而乾淨看起來像好消息

- **理論說**：糾纏偵測寫好、測試綠了，就可以拿去搬任何一種知識。
- **實際發生**：`articles` 的來源當時是生成時寫進 markdown 的**字串**，不是連結。
  搬一篇文章時偵測器查不到任何相鄰知識，於是回報**「沒有糾纏」**——
  ⚠️ 那不是「沒關係」，是「沒問到」，而介面上這兩者長得一模一樣。
- **教訓**：**「查無結果」和「查不到」必須在儀器上分得開，否則缺陷會偽裝成通過。**
  凡是回傳空集合的偵測／檢查／掃描，動手前先問：
  **它的輸入來源全部接上了嗎？沒接上的那條，會讓它回報什麼？**
  若答案是「一樣回報空的」，那這個偵測器對那一類輸入是**半瞎的**，而它不會說。
  ⇒ 判準：**空結果要能區分成「找過了，沒有」與「沒得找」。** 做不到就先補輸入，別先出貨偵測。
- ⓘ 這是「儀器說謊」的第四種形態（前三種：量錯欄位、恆真條件、訊號起算時間），
  差別在**這次儀器的話是對的**——它說的是「我沒找到」，錯的是我把它讀成「不存在」。
- **來源**：[history/114](history/114-糾纏從裁決降級為問句-而護欄被實測的數字定住.md)；
  spec 049 把「補文章的來源連結」排成 P1、排在搬動之前；commit `7e97047`。

### 當我把一件事拆成「觸發器」和「結果」兩個名字，先問它們是不是同一個東西的兩個時刻

- **理論說**：搬動時偵測到的**衝突**叫「糾纏」，人選完之後留下的**邊**叫「捷徑」——兩個概念。
- **實際發生**：使用者一句「**或許糾纏 tangle 就是捷徑的別名**」把兩者合成一個。
  那條跨樹的邊在偵測之前就已經存在，偵測只是**讓它現形**，人只是決定要不要留著。
- **教訓**：**兩個名字若指向同一個東西在不同時刻的樣子，那是一個概念，不是兩個。**
  拆成兩個的代價不是多打幾個字，是**多出一個生命週期要維護**——
  合併之後，`resolve_tangle`、warning、「未解決的糾纏」清單**全部消失**，
  因為糾纏不再是要被消滅的錯誤狀態，它就是 DAG 的邊。
  ⇒ 判準：**問「這兩個名字有沒有可能同時為真」。** 會的話，它們是同一個東西。
- **來源**：[history/114](history/114-糾纏從裁決降級為問句-而護欄被實測的數字定住.md)；
  使用者裁決，2026-08-25。

### 用「最近一筆」指認你剛做的那件事，在有延遲時會指到上一筆——而它會回你「成功」

- **理論說**：`git push` 完馬上 `gh run list --limit 1`，抓到的就是這次的 CI run。
- **實際發生**：GitHub 註冊 run 有幾秒延遲。抓到的是**上一次**的 run，
  ⚠️ 而它回報「已經完成，success」——**假的綠燈長得跟真的綠燈一模一樣**，
  因為它沒有失敗，它只是在講另一件事。（用 `headSha` 比對才發現當下的 run 還 `in_progress`。）
- **教訓**：**指認要用內容比對，不要用時間或順序比對。**
  凡是「拿最新的一筆當作我剛產生的那一筆」（最後一個 run／最新的 pod／最近的檔案／最後一行 log），
  都在賭「寫入已經發生」，而延遲會讓這個賭安靜地輸。
  ⇒ 判準：**這個查詢有沒有把「我要的東西」寫進條件裡？** 沒有的話它找到的是「別的東西」，不是「沒東西」。
  ⓘ 同一族的第五種形態，也是最像好消息的一種——前四種回報怪數字，這種回報成功。
- **來源**：[skills/ship-knowfield/SKILL.md](skills/ship-knowfield/SKILL.md) 步驟 3（2026-08-25 現場撞到後補的）。

### 驗收的起始狀態要是使用者的起始狀態——而那幾乎永遠是「什麼都還沒做」

- **理論說**：搬動功能測試全綠、實跑也真的搬了，就是能用。
- **實際發生**：出貨隔天使用者問「要怎麼整理？」——答案是**幾乎不行**。
  正式庫 31/31 對話、75/75 理解**全部未歸屬**，而搬動入口只掛在**已歸屬**的項目上。
  ⚠️ 我實跑時**先用 SQL 把資料塞進領域再測搬動**，等於自己餵了一個現實中不存在的初始狀態。
- **教訓**：**功能在「使用者現在的狀態」上跑得起來嗎？**
  不是「在我準備好的狀態上」。做 setup 的那幾行 SQL／fixture，
  每一行都在把系統推離使用者所在的地方——**setup 越長，驗收離現實越遠**。
  ⇒ 判準：**問「第一次用的人，第一個動作是什麼？那個動作現在做得到嗎？」**
  起點動作做不到的話，後面全部的功能都站在空集合上。
- ⓘ 這跟「量錯欄位」不同：儀器沒壞、數字沒錯，**是我把系統擺到了一個沒人會在的位置**。
- **來源**：[history/115](history/115-在空集合上出貨-而測試是我自己餵的初始狀態.md)；
  spec 049 → 050 的落差；commit `7f35010`。

### 互動次數超過某個量，功能就等於不存在——那是可行性，不是體驗

- **理論說**：能搬就是能整理，一次搬一件慢一點而已，之後再優化。
- **實際發生**：108 件 × （開下拉 ＋ 選）＝ **216 次點擊**。沒有人會做完。
  ⓘ 這跟前一天寫下的「搬一段跳 15 條詢問 → 第二次就沒人用」**是同一條判準**，
  ⚠️ 而我當時只把它套在**詢問**上，沒想到它同樣管**操作**。
- **教訓**：**把互動次數乘上真實的資料量再看一次。**
  N 件 × K 次點擊，K 是常數不代表可行——決定可不可行的是 **N×K**，而 N 來自正式庫不是想像。
  ⇒ 判準：**這個操作在最大的那份資料上要點幾次？** 三位數就是「不存在」，不是「慢」。
- **來源**：[history/115](history/115-在空集合上出貨-而測試是我自己餵的初始狀態.md)；
  spec 050 的批次搬動。

### HTML5 拖放在觸控裝置完全不觸發——而它不報錯，只是不動

- **理論說**：`draggable` ＋ `onDrop` 就是拖放。
- **實際發生**：那版在合成事件下完全沒反應。查下去才想到 KnowField 是 **PWA**
  ——⚠️ HTML5 drag-and-drop **在觸控裝置上根本不產生事件**，手機使用者會遇到「拖不動也不報錯」。
  改用 Pointer Events（滑鼠與觸控同一條路）之後，實跑一次就過。
- ⓘ 附帶收穫：**能被自動化驅動的實作，通常也是跨裝置成立的那個**
  ——合成事件驅不動它，往往是因為它依賴一條只有某類輸入裝置才有的路徑。
- ⚠️ **同時撞到的第二個坑**：`Row`／`Node` 定義在元件內部 ⇒ 每次 render 都是**新的元件型別**，
  React 整棵子樹卸載重掛。拖曳一秒幾十次 render × 105 列 → **頁面卡死到截圖逾時**。
  判準：**定義在 render 裡的 JSX 元件是 bug，不是風格**。
- **來源**：[history/115](history/115-在空集合上出貨-而測試是我自己餵的初始狀態.md)；
  `frontend/src/pages/DomainsPage.tsx`（`beginDrag` / `renderRow` / `renderNode`）。

### 一個「例外桶」放在結構外面，就沒有人能在它裡面工作——把它放進結構的頂端

- **理論說**：沒有歸屬的東西不屬於樹，所以另外列一格「未歸屬」，清楚。
- **實際發生**：整理台出貨，使用者說「**還是很難整理**」——因為東西在樹**外面**，
  就做不了檔案系統最基本的動作：**在它現在的位置底下開一個子領域，再把它拖進去**。
  ⚠️ 資料模型一個字都不用改（仍是 `null`），錯的是它的**位置與名字**。
- **教訓**：**「還沒分類的」不是結構的例外，是結構的起點。**
  凡是你想開一個「其他／未分類／待處理」的桶子，先問：
  **使用者要在這個桶子裡做事嗎？** 要的話，它就得是結構裡的一個節點（通常是根），
  而不是結構旁邊的一格——旁邊那一格沒有子節點、不能被選、不能當容器。
  ⇒ 判準：**名字有沒有預設一個「之後會清空」的承諾？**「未歸屬」有，「根」沒有。
  而永遠不會清空的東西不該叫待辦。
- **來源**：[history/115](history/115-在空集合上出貨-而測試是我自己餵的初始狀態.md) 轉移五；
  使用者回饋，2026-08-25；`DomainsPage.tsx` 的 `ROOT_NAME`。

### 一條規則的**理由**被推翻時，先問規則本身還對不對——別連著一起丟

- **理論說**：「鄰居未歸屬時不算糾纏，因為**它還沒有位置**。」
- **實際發生**：把「未歸屬」改成「根領域」之後，它**有位置了**，理由當場失效。
  順手把規則一起改掉是很自然的下一步——⚠️ 而那會讓「搬出第一件東西」對所有鄰居報糾纏，
  66/75 條理解連著對話 ⇒ 第一次整理就跳十幾條詢問，正好撞回它本來要防的失效模式。
- **教訓**：**理由和規則是兩層，會分別壞掉。**
  理由塌了只證明「這條路走不通」，不證明「終點是錯的」——
  先用**後果**重新檢查規則（拿掉它會怎樣？），再決定要換理由還是換規則。
  ⇒ 判準：**改規則前先問「舊規則擋住的那個災難，現在還會發生嗎」。** 會 → 留規則、換理由。
- ⓘ 這次換的理由是：糾纏是「兩件**刻意放過**的東西被拆散」，根領域是還沒放過的地方
  ——比原本那個更準，因為它不依賴「有沒有位置」這個已經改掉的概念。
- **來源**：[history/115](history/115-在空集合上出貨-而測試是我自己餵的初始狀態.md) 轉移五；
  spec 050 FR-007（**行為零變更**，只換論證）。

### 下游的改善每次都有效一點點——那正是它掩蓋方向錯誤的方式

- **理論說**：使用者說「很難整理」，那就把整理介面做得更好用。
- **實際發生**：我連做兩刀（批次搬動、根領域＋搜尋＋全選），**每一刀都真的比前一刀好用**，
  而使用者每次都還是說「還是很難」。第三次我去查程式碼，才看到冊封／生文章／收來源
  **都不設歸屬** ⇒ ⚠️ **那份待整理清單是持續生產線，不是歷史包袱**。
  整理完，冊封三條又長回去。
- **教訓**：**改善不等於方向對。** 下游的每一次優化都會量到進步，
  所以「有進步」**不能**用來確認方向——能確認方向的只有「**它會不會再發生**」。
  ⇒ 判準：**問「我修完之後，同一個問題還會不會自己長回來」。**
  會 → 你在下游；那就去找**它是怎麼被生產出來的**。
- ⓘ 徵兆是可辨識的：使用者用同一句話抱怨第三次，而你每次都做了不同的（且有效的）改善。
  **同一句抱怨重複出現，比抱怨的內容更有資訊。**
- **來源**：[history/116](history/116-病因在上游-而繼承不是猜.md)；spec 045 → 050 → 051 的三刀。

### 把「猜」換成「繼承」——判準不是準不準，是錯的時候誰負責

- **理論說**：自動幫新知識歸類就是自動分類，而使用者已經否決過三次。
- **實際發生**：他要「新東西自動歸位」。同一個動作，兩種依據：
  自動分類是 **AI 看內容猜**；繼承是「**它長自一段你已經放好位置的東西**」。
- **教訓**：**一個自動行為可不可接受，看的是它的依據能不能被追問，不是它的準確率。**
  ⇒ 判準：**問「它錯的時候，是誰錯了」。**
  答案是「模型猜錯了」→ 那是推測，要人閘門；
  答案是「**你把出處放錯了**」→ 那是**導出**，可以自動做，因為錯誤有主人、而且改得動。
- ⓘ 同一把尺也解掉了「多個出處分屬不同領域怎麼辦」：取**最近共同祖先**
  （`AI/生成模型` ＋ `AI/數學` → `AI`）——那仍然是導出，不是猜。
- **來源**：[history/116](history/116-病因在上游-而繼承不是猜.md)；spec 051 FR-004／FR-007。

### 一個關係算不算「跨界」，取決於你站在哪裡看——那不是那條邊的屬性

- **理論說**：`A → B` 跨不跨領域，是這條邊自己的性質，算一次存起來就好。
- **實際發生**：使用者選了「當前領域**含子領域**」，同一條 `Flow Matching → 數學` 的邊
  就變成：站在 Flow Matching 是**跨出去**、站在 `AI` 是**內部連結**（兩端都在我的子樹裡）。
  實跑印證：同一條邊，一個立足點數到 1、另一個數到 0。
- **教訓**：**凡是含「外部／其他／跨」的概念，都要先問「相對於誰」。**
  這類詞看起來是在描述**對象**，其實藏著一個沒說出口的**觀察者**；
  把它當成對象的固有屬性存起來，換一個觀察者就全錯，而且不會報錯。
  ⇒ 判準：**這個屬性換一個立足點還成立嗎？** 不成立 → 它是關係，不是屬性，**不能快取成欄位**。
- ⓘ 連帶結論：同一個名字底下可以有兩個問句。搬動時問「**未來的歸屬**」、
  瀏覽時問「**現在的視角**」——邊是同一條，但顯示的字要不一樣
  （側欄寫「通往外面」不寫「糾纏」，後者看起來像有待辦要清）。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/116](history/116-病因在上游-而繼承不是猜.md)；spec 052 FR-003。

### 用「形式」命名一個位置，會無聲地排除掉所有其他形式

- **理論說**：那一格放的就是文章，所以叫「文章」最準確。
- **實際發生**：使用者要求改名「應用」，理由是「**這樣可以有更多類型的決策目標**」。
  他要的不是換個好聽的字——是**把那個位置從一種形式解放成一種用途**。
- **教訓**：**名字是這個位置未來能長出什麼的邊界。**
  用形式命名（文章／報告／清單），任何其他形式都會顯得「不屬於這裡」，
  ⚠️ 而排除是**無聲的**：沒有人會提案「在文章頁做一份決策清單」，所以你永遠不會收到抗議。
  ⇒ 判準：**問「這個名字是在講它長什麼樣，還是在講它拿來做什麼」。**
  講長什麼樣 → 換成用途，位置就空出來了。
- ⓘ 換名字**不必**同時把多種形式做出來（YAGNI）——先空出位置，形式後補。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/117](history/117-文章改名應用-而重載的詞會沉默地弄壞prompt.md)；
  使用者裁決，2026-08-26；spec 053。

### 元件在這裡做不出來時，先量它的**容器**——限制常常不在你正在改的那一層

- **理論說**：檔案總管做不出兩欄，那就繼續調那個元件。
- **實際發生**：擋住它的是 `Layout.tsx:41` 的 `mx-auto max-w-3xl`——一個**上兩層**、
  而且是**別的需求**（閱讀行寬）留下的置中容器。在元件裡怎麼調都不可能成立。
- **教訓**：**「做不出來」先問這是元件自己的限制，還是它被放進去的那個框給的。**
  ⇒ 判準：**把這個元件單獨放進一個空白頁，它做得出來嗎？** 做得出來 → 病灶在容器，不在元件。
  ⚠️ 這種卡關**不會報錯**，只會表現成「怎麼調都差一點」，於是時間全花在錯的那一層。
- ⓘ 同一形狀：`overflow` 被祖先切掉、`z-index` 被祖先的 stacking context 關住、
  `position: sticky` 被某一層的 `overflow: hidden` 廢掉。**都是上游，都不報錯。**
- **來源**：[history/123](history/123-領域頁借檔案總管-而只有桌機到得了的路徑等於不存在.md)（spec 057）；`frontend/src/Layout.tsx:10` 的註解。

### 把異質來源併成一張清單時，「欄位同名」是假設不是事實——猜錯不會少一列，只會安靜排錯

- **理論說**：四種知識都有「時間」，`SELECT ... created_at` 一路撈完就好。
- **實際發生**：四種知識用**三個不同的欄名**，而且兩種還要額外處理
  （`repository.py`：對話 `COALESCE(NULLIF(last_activity_at,''), created_at)`、
  理解／應用 `created_at`、來源 `MAX(ingested_at)` ＋ `GROUP BY url`）。
  ⚠️ 假設同名的話，缺那個欄的種類拿到空字串 ⇒ **不會少一列、不會報錯，只會全部沉到底**，
  而「這種東西比較舊」看起來完全合理。
- **教訓**：**併表時，欄位語意要逐個來源確認，不能靠名字推。**
  ⇒ 判準：**猜錯的時候，畫面上會少東西還是只會順序不對？**
  少東西會被發現；**順序不對不會**——後者才需要你先去查每張表。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/123](history/123-領域頁借檔案總管-而只有桌機到得了的路徑等於不存在.md)（spec 057）；
  `src/knowfield/store/repository.py` 的 `_inventory_rows`。

### 改名之前先數一數這個詞有幾種意思——重載的詞會讓全域取代沉默地改壞語意

- **理論說**：把「文章」全部換成「應用」，改名完成。
- **實際發生**：`field_chat` 的三層份量 prompt 裡有**兩種**「文章」：
  「他收藏的**文章**/論文＝外部證言」是**別人寫的**、「他自己知識庫生成的」才是應用。
  ⚠️ 全換的話 prompt 會把外部來源歸進使用者的產出層，
  **而模型不會報錯——它只會開始拿別人的觀點當他的地基**，正好是反逢迎那道膜要擋的。
- **教訓**：**全域取代的風險跟出現次數無關，跟「這個詞有幾種意思」有關。**
  動手前先 grep 出全部出現處，逐一問「這裡指的是哪一個」——
  ⇒ 判準：**如果我改錯了，什麼東西會壞、誰會發現？**
  答案是「prompt 的語意會壞，而沒有人會發現」 → 那就要為它專門寫一支測試，
  因為既有測試全都不會紅。
- ⓘ 同一把尺也保住了「收進外部素材」那些入口的用詞（那裡的「文章」也是別人寫的）。
- ⓘ **第二次（對話→互動，spec 061）**：同一把尺，而且**這一次的先例就是上一次**
  ——範圍不必重估，117 已經切好了（實體名改／識別字不改／註解不改）。
  ⚠️ 我卻重估了一次，還估成五倍（見下一條）。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/117](history/117-文章改名應用-而重載的詞會沉默地弄壞prompt.md)；
  `tests/unit/test_vocabulary.py`；commit `f7a4efe`。

### 改名的成本不是出現次數，是要做幾次判斷——用前者估會把該做的事往後推

- **理論說**：`grep -c` 說 202 處，那是大工程，先做別的。
- **實際發生**：實際動到 **38 處**（前端 30、後端 8）。202 裡絕大多數根本不需要決定——
  程式識別字不改、註解不改，剩下的判斷只有**三類**，分類一次之後全是機械取代。
  ⚠️ 我拿這個高估五倍的數字寫進 draft，結論是「**改名要放最後**」——
  一個**用錯誤成本推出來的排序**，而使用者推翻它才發現數字是錯的。
- **教訓**：**估一件重構的成本，數的是「要做幾次不同的判斷」，不是「要改幾個地方」。**
  ⇒ 判準：**這些出現處可以分成幾類？** 每一類只需要決定一次，類數才是成本。
  一個詞出現一千次但只有兩種意思，比出現二十次卻每次都要想的便宜。
- ⓘ 反過來也成立：117 那條說「風險跟出現次數無關」——**成本也是**。
  同一個量（出現次數）既高估不了風險，也高估不了工作量，卻很容易同時被拿來估兩者。
- **來源**：[history/122](history/122-對話改名互動-而那個放最後的建議建立在高估五倍的數字上.md)（spec 061）。

### 用 X 定義位置、又用「涉及 X 的邊沒斷」當證據——那是在量自己的假設

- **理論說**：分區之後，真實的邊只被切斷 21%，而亂分的基準是 81%
  ⇒ 這個分割**確實在尊重真實關係**，強證據。
- **實際發生**：那 66 條真實邊的大宗是「理解→互動」，
  ⚠️ 而我把**互動的位置定義成它的理解的重心**——它們**天生就近**。
  ⇒ 那個數字有一大半是**我自己造成的**，不是資料的性質。差一點就拿它當設計成立的依據。
- **教訓**：**量測方式和被量測的假設共用同一個東西時，那個數字不是證據。**
  ⇒ 判準：**如果我的假設是錯的，這個數字會變嗎？** 不會 → 它量的是我的實作，不是這個世界。
  ⇒ 作法：找出**不共用**的那一部分當對照（這次是「來源↔理解」那幾條，可惜太少），
  或明講「這一項這次量不出來」——**比報一個漂亮但循環的數字誠實**。
- ⓘ 同一族：用同一份資料調參數又報那份資料的分數 · 用 LLM 生測資又用 LLM 評分 ·
  「我們的使用者滿意度很高」而問卷只發給還在用的人。**倖存者與循環是同一種形狀。**
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[draft/2026-08-26-領域改成行政區](draft/2026-08-26-領域改成行政區-而第三刀的前提被推翻.md) 第四節判準②
  （2026-08-26 的分區實驗，正式庫真實資料）。

### 一個介面如果給不出搜尋給不了的東西，它就不該存在——而搜尋給不了的是**副作用**

- **理論說**：做一個好的瀏覽介面（目錄、資料夾、樹），讓人找得到東西。
- **實際發生**：「組織 vs 搜尋」在產品史上對決過兩次——Yahoo 人工目錄 vs Google、
  Email 資料夾 vs Gmail 的「archive，別分類」——**兩次組織都輸了**。
  輸的原因一致：**它們賣的正是搜尋更強的那一半**。
- **教訓**：**搜尋是零副作用的**——你問什麼得到什麼，一點多的都沒有。
  ⇒ 判準：**這個介面能給我「我沒在找的東西」嗎？** 不能 → 它在跟搜尋比它會輸的項目。
  對知識工具來說，**那個「多的」（複習、意外的連結、鄰居、岔路）才是產品**。
- ⇒ 兩個設計推論：① 先把「找得到」**真的**交給搜尋（KnowField 在此之前根本沒有全域搜尋，
  卻已經在拿領域樹當找東西的介面）；② 瀏覽介面要把鄰居與岔路放到**顯眼處**，
  ⚠️ 而不是做成一個更好的列表——列表是搜尋結果的形狀。
- ⓘ 反面也要守住：**搜尋結果不要摻「你可能也想看」**。兩邊都想做對方的事，
  結果是兩個介面都說不清自己是幹嘛的。
- ⓘ 這條也降低了另一件事的標準：既然瀏覽介面不是檢索範圍，它「分得對不對」的要求就低得多
  ——**一個合理的劃法**就夠了，不需要是正確的劃法。
- **來源**：[history/128](history/128-找得到讓給搜尋-而逛的副作用才是產品.md)（spec 066）。

### 不可反駁的理由等於沒有理由——審核介面的品質，取決於它給的理由能不能被打臉

- **理論說**：AI 提議、人審核，這就滿足「人保有決策主權」了。
- **實際發生**：審核成不成立，全看**理由長什麼樣**。
  「這 7 條理解都是從〈某段互動〉冊封出來的」——你點得開、可以說「不對，那段講的是別的」。
  「它們都跟生成模型有關」——⚠️ **你沒辦法不同意**。
  而一個你無法反駁的提議，你只會按同意 ⇒ 那個介面就是自動化多按一下。
- **教訓**：**要人審核什麼，就要給他能反駁的東西。**
  ⇒ 判準：**這條理由，使用者有沒有辦法說「不對」，而且說得出哪裡不對？**
  沒有 → 那不是理由，是換句話說。
- ⇒ 因此的設計推論：**把可查證的部分和需要判斷的部分分開**。
  spec 065 讓**成員來自結構**（既有的邊，指得出來），**只有命名與合併交給 LLM**
  ——不走 embedding 相似度，因為相似度**給不出可查證的理由**（它只會回「就是像」）。
- ⓘ 連讀感都算數：第一版印出「這 **1** 條理解都是從⋯」還漏了個 markdown 星號。
  那不是文案瑕疵——**讀起來像機器湊的 ⇒ 不可信 ⇒ 不會細看**，於是「能反駁」在實務上落空。
- ⓘ 對照組：spec 046 學到「只能按同意的介面會被橡皮圖章化」。這條是它的上游——
  **能不能按不同意，取決於你有沒有給他不同意的立足點。**
- **來源**：[history/127](history/127-建議整理-而不可反駁的理由等於沒有理由.md)（spec 065）；
  `src/knowfield/organize/suggest.py`。

### 一條「不要做 X」的規則會被忘記——把它變成「**沒有那條路**」

- **理論說**：實驗證明全量重劃會大洗牌（ARI ≈ 0.18），所以寫一條規則：不要全量重劃。
- **實際發生**：規則寫在 spec 裡、寫在註解裡，然後**下一個人（包括三個月後的我）會加一個
  「重新劃一次」的按鈕**，因為那看起來很自然，而且沒有東西會擋。
- **教訓**：**一個重要的禁令，要做成「做不到」而不是「不要做」。**
  ⇒ 判準：**要違反這條規則，需要動到程式碼嗎？** 不需要 → 它遲早會被違反。
  ⇒ 作法：讓禁止的那件事**在資料流上不可能**。
  （spec 069 的劃界**只讀 `domain_id IS NULL`**，於是「動到已歸屬的東西」不是被禁止，
  是沒有輸入；spec 065 的「沒有全部套用」也一樣——那個 API 不存在，
  而且有一支測試掃路由表守著它不會出現。）
- ⓘ 這條跟「能靠記性守住的東西就是保證會漏的東西」是同一族，
  但更強：那條講的是**執行時漏掉**，這條講的是**有人刻意加回來**。
  前者靠掃描擋得住，後者只能靠**讓它做不到**。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/131](history/131-行政區-而禁令要做在結構上不是紀律上.md)（spec 069）；
  `src/knowfield/organize/district.py`。

### 下結論之前，先拿一個**你已知答案的案例**去校驗儀器

- **理論說**：把 1,393 條教訓標題 embed、算跨 base 餘弦、門檻 0.78 ⇒ 就知道有多少可轉移的判準。
  結果是「相符 8/1340、概念 0/53」——**幾乎沒有複利**。
- **實際發生**：在報這個數字之前，拿一對**我肉眼判定是同一個母概念**的去驗——
  `沒有東西會叫的失敗就是不會被修的失敗` ↔ `沉默的失敗不會自己現形`：**0.581**。
  而隨機無關的是 0.27–0.42、措辭幾乎一樣的是 0.88–1.00。
  ⚠️ 也就是說「**同一個判準、不同的詞**」離雜訊只有 ~0.17
  ⇒ 那個 0.78 報的是**儀器的答案，不是這個世界的答案**。
  改用 0.62 之後：跨 base 群 **49 個、157 條、跨 ≥3 個 base 的 16 群**——複利是真的。
- **教訓**：**任何新量測，先餵一個你已經知道答案的案例，看它答不答得出來。**
  ⇒ 判準：**這把尺在我已知的正例上給幾分？離它在負例上給的分有多遠？**
  兩者太近 → 這把尺分不出來，它報的任何數字都是它自己的性質。
- ⓘ 這條跟「量了一件錯的事」是一組：那條說**別拿代理指標測它測不到的東西**，
  這條說**怎麼在下結論前發現你正在這麼做**——**用一個你已知答案的案例**。
  ⚠️ 那也是唯一便宜的辦法：你不必先知道真答案，只要**知道一個**就夠。
- **來源**：[draft/2026-08-27-knowie接進來](draft/2026-08-27-knowie接進來-讀而不寫.md) 第十一節
  （13 個 base × 1,393 條的跨 base 實測）。

### 一段「現狀」只要描述**功能**，就等於預約它下一次出貨時過期

- **理論說**：知識庫該有一段「我們現在做到哪」，讓第一個讀的人快速上手。
- **實際發生**：那一段爛過**兩次**。第一次寫死「33 個階段兌現」（索引已經 59 列）；
  修掉之後我又寫了「這些東西住在一棵**領域樹**裡（領域頁＝檔案總管）」
  ——⚠️ **一天後領域就變成行政區、領域頁也不只是清單了**。
  而同一段裡就寫著我上一輪才放進去的那句：「抄一份會漂的真相，等於預約它過期的那天」。
- **教訓**：**寫「現狀」時，逐句問：這一句會因為我們出貨而變假嗎？**
  會 → 它不屬於現狀，屬於里程碑索引（那裡本來就會跟著出貨更新）。
  ⇒ 留在現狀的只有**不會因為出貨而改變的**：定位、架構、那件「一直在做的事」。
- ⓘ 這比「不要抄會漂的真相」更難守，因為**功能描述讀起來很像定位**
  ——「它是一個把知識放進樹裡的工具」聽起來像在講本質，其實在講這一版的實作。
  ⚠️ 判準要逐句套，不能靠語感。
- **來源**：[history/136](history/136-一次性人工操作-而它不是一個功能.md) 同一次 judge；
  `knowledge/vision.md` 的「現狀」段（改寫後自帶那條判準）。

### 介面已經讓人改的東西，後端不要替他否決——尤其當你用的是一個代理指標

- **理論說**：自動命名會退化成泛詞（「AI」「模型」），所以在程式裡擋掉。
  再進一步：泛不泛是相對於語料的 ⇒ 改成「這個詞在**過半數的區**都出現就擋」。
- **實際發生**：使用者兩次否決——「深度學習沒什麼不好呀」、「我覺得不用特別擋名字」。
  ⚠️ 我錯在兩層：
  ① **量錯了東西**：「這個詞在別區的成員文字裡出現過」**不等於**
     「這個名字套在別區上也說得通」。領域術語是整個庫的**背景詞彙**，
     到處都會出現，但只有一區**是在講它**。
  ② 更根本：名字好不好是**語意判斷**，而**介面本來就讓他改名**。
     後端替他否決＝把他的裁決搶過去，而且用的還是一個代理指標。
- **教訓**：**在 AI 提議、人裁決的流程裡，程式只該擋「機械可判的」——
  格式、重複、缺漏。凡是需要判斷的，交給人，並且把判斷所需的資訊給他。**
  ⇒ 判準：**這個檢查擋掉的東西，使用者有沒有可能是對的？** 有 → 不要擋，要**呈現**。
  （spec 069 改成：讓模型輸出「它跟哪幾區容易混、這個名字怎麼分開」，
  那句話交給人判——擋掉的變成看得見的。）
- ⓘ 這條跟「不可反駁的理由等於沒有理由」是同一組的另一半：
  那條說**要給人反駁的立足點**；這條說**別在他反駁之前就先替他決定**。
- **來源**：[history/134](history/134-命名不該由程式否決-而我量錯了泛不泛.md)（spec 069 後續）。

### 自動命名一定會退化成「共有的最泛的詞」——所以泛詞要**擋**，不能只在 prompt 裡講

- **理論說**：在 prompt 裡寫「名字要具體、要用成員文字裡出現過的詞」，就夠了。
- **實際發生**：第一次跑出來的區名是「**AI**」。它確實出現在成員文字裡，也確實是共同點
  ——⚠️ 而它**可以套在任何一群上**。
- **教訓**：**要求 LLM 為一群東西命名時，它最省力的答案永遠是最泛的那個詞。**
  ⇒ 判準（knowie 判斷真／假母概念的同一把尺）：**這個名字排除掉了什麼？**
  答不出來 → 它沒有修剪力。
  ⇒ 而這一條**在 prompt 裡講不夠**，要在程式裡擋（黑名單 ＋ 擋掉就退回一個具體的錨）。
- ⓘ 擋掉之後的實測：「AI」→「資料幾何結構」「cyber resilience」「層級式維度」
  「Agent security」——**每一個都答得出「它排除了什麼」**。
- **來源**：[history/131](history/131-行政區-而禁令要做在結構上不是紀律上.md)（spec 069）。

### 「之後再把那 N 個地方改一次」——那是一個**保證會漏**的計畫

- **理論說**：先把 owner 過濾鋪好（88 個查詢點），之後要加 persona 時再改一次就好。
- **實際發生**：那 88 處有**兩種寫法**（68 處內嵌 `owner_id={self.owner}`、12 處 `{self._OWN}`）。
  ⚠️ 「再改一次」意味著要在 88 個地方各做一次同樣的判斷，而**漏掉任何一個都是跨身分外洩，
  而且不會報錯**。
- **教訓**：**當你已經知道未來會有第二個維度，第一個維度就不該散在 N 個地方。**
  ⇒ 判準：**這個規則之後還會被改嗎？** 會 → 現在就收成**一份可呼叫的東西**，
  然後只改那一份。（spec 067 把兩種寫法統一成 `_own()`，persona 因此自動涵蓋 88 處，
  **包含前一刀才做的全域搜尋**——而搜尋正是最容易漏掉硬邊界的地方。）
- ⓘ 這是「一份寫法 vs 兩套會漂」（spec 064）的**時間版**：那條講的是同時存在的兩套，
  這條講的是**現在一套、未來要改的那一次**。兩者的解法相同，而後者更容易被說服自己延後。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/129](history/129-persona是硬隔離-而收成一份述詞省掉了會漏的那一次.md)（spec 067）。

### 同一個錯犯第二次時，該做的不是「記得」，是**把它變成機器檢查**

- **理論說**：markdown 星號漏到純文字介面上，注意一下就好。
- **實際發生**：spec 065 的區理由漏過一次，spec 067 的身分說明**又漏一次**。
  而加上掃描測試之後，它上線第一件事就抓到一個**既有的**漏網
  （`DomainsPage` 抹除領域的 `confirm()` 字串裡也有）。
- **教訓**：**第二次的價值不是「這次記得了」，是你終於知道該把它做成檢查。**
  ⇒ 判準：**這個錯誤有沒有一個機械可判的特徵？** 有 → 寫掃描，別再靠記性。
  （這裡的特徵是：非註解行 ＋ `\*\*…\*\*`。⚠️ 而第一版掃描器誤報了多行 JSX 註解的**續行**
  ——掃描器自己也要被驗一次。）
- ⓘ 反過來說：**第一次**就寫掃描通常是過度反應，因為你還不知道它會不會再來。
  兩次是一個不錯的門檻。
- **來源**：[history/129](history/129-persona是硬隔離-而收成一份述詞省掉了會漏的那一次.md)（spec 067）；
  `tests/unit/test_persona.py::TestNoMarkdownInPlainTextUi`。

### 一條寫對的警告，只保護**它被實作到的那一半**——警告不是實作

- **理論說**：spec 055 已經把「⚠️ 封存必須擋住檢索，不只擋住畫面」寫進程式碼註解了，
  而且就寫在 `_LIVE` 常數旁邊。那條路踩過一次，不會再踩。
- **實際發生**：它只被實作在**理解**那半（`_anointed_corpus_entries`）。
  來源那半（`list_corpus_entries`，而那正是 `retrieve_corpus` 真正呼叫的）**完全沒濾**。
  ⚠️ 封存過的來源仍在影響每一個回答，而清單頁**完全正確** ⇒ 看不出來。
- **教訓**：**一條判準寫下來的當下，要問「這條規則有幾個實作點？我做了幾個？」**
  ⇒ 判準：**同一個保證有幾條路可以繞過去？** 註解只證明你想到了，不證明你做完了。
  ⇒ 作法：規則有多個實作點時，把述詞收成**一份可呼叫的東西**（這裡是 `_live(alias)`），
  然後掃「有幾個地方該用它而沒用」——**兩套寫法一定會漂，而漂掉的那一套不會報錯**。
- ⓘ 更糟的變種：**另一處的決定引用了這個保證**。`delete_why_node` 寫著「不清 embedding：
  檢索一律先過 `list_corpus_entries`，那裡已經濾掉封存的」——那句話是假的，
  而「不清向量」這個決定靠著它站著。⇒ **註解裡的「那裡已經處理了」是最容易過期的依賴。**
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/126](history/126-封存沒擋住檢索-而那條警告就寫在旁邊.md)（spec 064）；
  `tests/unit/test_archive_blocks_retrieval.py`。

### 驗「有沒有被濾掉」，測資必須是**還有內容的**那種——空的東西撈不到不代表過濾成立

- **理論說**：抹除掉一份來源，然後斷言檢索撈不到它。
- **實際發生**：抹除會把內容清空 ⇒ **就算過濾根本沒實作**，檢索也撈不到東西
  ⇒ 測試綠、洞還在。⚠️ 這支測試會在「過濾成立」和「內容剛好是空的」之間**分不出來**。
- **教訓**：**驗一個過濾器，測資要在「被過濾掉」以外的每個維度都是正常的。**
  ⇒ 判準：**如果把過濾拿掉，這筆測資還撈得到嗎？** 撈不到 → 測資選錯了，不是程式對了。
  （spec 064 用「只封存、不抹除」的遺骸——內容還在，所以撈不到只能是因為真的被濾掉。）
- ⓘ 同一族：驗去重要用真的重複、驗權限要用真的有資料的另一個帳號、
  驗排序要用**順序不同**的輸入。**測資的「乾淨」常常正是它沒牙的原因。**
- **來源**：[history/126](history/126-封存沒擋住檢索-而那條警告就寫在旁邊.md)（spec 064）。

### 掃描式的守門只掃**它記得的形狀**——形狀之外的漏洞，它會連同安全感一起給你

- **理論說**：寫一支掃原始碼的測試，斷言每個碰到 owned 表的查詢都帶 owner 條件，
  這樣「明天才寫、而我不會回來補測試的那一個」也被管到了。
- **實際發生**：那支掃描器漏了**兩種形狀**，兩次都是別的東西抓到的——
  ⚠️ ① `UPDATE {t}` **動態表名**（正規表達式找的是具名的表），而 `set_knowledge_domain`
  正是這樣寫的 ⇒ B 真的把 A 的知識搬走了，是**行為測試**抓到的；
  ② `app.py` 也直接下 SQL（三句全沒過濾），而掃描器只讀 `repository.py`。
- **教訓**：**掃描式守門的涵蓋率有兩個維度：形狀與檔案，而它對兩者的缺口都是沉默的。**
  ⇒ 判準：**這個規則有幾種寫法？我掃了幾種？** 以及 **這種程式碼還可能出現在哪個檔？**
  ⇒ 而因為那兩個問題都答不完全，掃描層**必須**配一層**行為層**（真的做出違規情境、驗它被擋）。
  兩層不是冗餘，是互補：一層抓形狀、一層抓語意，**任何一層單獨存在都會給出假的安全感**。
- ⓘ 行為層自己也漏——它只驗「我想得到的那些方法」。所以是互相補洞，不是誰包含誰。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/125](history/125-多租戶的底層-而掃描器只掃它記得的形狀.md)（spec 063）；
  `tests/unit/test_tenant_isolation.py` 的兩個 class。

### 對抗驗證要確認你拉的是**那條線**——拉錯槓桿，紅得太少會被讀成「本來就安全」

- **理論說**：把守衛關掉，看測試紅幾支——紅得多就是保護得廣。
- **實際發生**：我把 `_OWN` 這個述詞常數改成 `"1=1"`，只有 **1 支**變紅。
  ⚠️ 我差一點把它讀成「其他那些本來就沒在保護」。真相是**槓桿沒連上**：
  自動轉換器產生的是內嵌的 `owner_id={self.owner}`，不是 `{self._OWN}`，
  所以那個開關只連著我手改的 9 處。改拉 `self.owner` 之後是 **7 支紅**。
- **教訓**：**對抗驗證有兩種失敗，而它們的輸出一樣：測試沒牙，或你拉的東西根本不在路徑上。**
  ⇒ 判準：**紅得比預期少的時候，先證明那個開關真的在受測路徑上**（例如故意讓它拋錯，
  看有沒有炸），再去解讀「所以那些沒被保護」。
- ⓘ 這是「攻擊打到錯的路徑」的一個變種，特徵是**它讓你對真實覆蓋率的估計偏低**
  ——於是你會去補一堆本來就不缺的測試，而真正的缺口原封不動。
- **來源**：[history/125](history/125-多租戶的底層-而掃描器只掃它記得的形狀.md)（spec 063）。

### 一個「什麼都沒有」的狀態，如果它是**判斷的結果**，就要存下來，不能推導

- **理論說**：「這條理解沒有外部依據」＝ 出處那幾欄都空著，查一下就知道，不必多一欄。
- **實際發生**：spec 062 的第四種出處是「**這是我自己的判斷，沒有外部依據**」——
  而它跟「欄位忘了填」「存進去的時候出錯了」在資料上**長得一模一樣**。
  ⚠️ 推導的話，讀的人分不出「他想過而且宣告了」與「這裡少了東西」，
  而那兩者的認識論地位相反：前者是一條合法的信任鏈終點，後者是資料缺損。
- **教訓**：**空值有兩種——「沒填」和「填了『沒有』」。後者是資訊，要有自己的表示法。**
  ⇒ 判準：**這個空狀態是使用者**做過的一個選擇**，還是預設？** 是選擇 → 存起來。
  （spec 062 用 `origin='self:judgment'` 存那個宣告，而不是靠「三個出處欄都空」推。）
- ⓘ 同一族的反面也踩過：`source_url` 在庫裡查不到 → `source_entry_id` 是 0 →
  **不能當成「有出處」放行**。「查不到」不是「有」，也不是「宣告沒有」——是第三種。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/124](history/124-人自己寫理解-而出處是被跳過那道檢查的替代品.md)（spec 062）；
  `tests/unit/test_write_understanding.py::TestOriginIsStoredNotDerived`。

### 掃整棵樹然後斷言「沒有殘留」的測試，必須先斷言它**掃到了東西**

- **理論說**：走訪 `frontend/src`、把還留著舊詞的行收集起來、斷言集合是空的。
- **實際發生**：我用了相對路徑 `pathlib.Path("frontend/src")`。從別的目錄跑 pytest，
  `rglob` 走訪一個不存在的目錄 → 收集到 0 行 → **斷言通過**。
  ⚠️ 「沒有殘留」和「一個檔都沒掃到」在這種測試裡**輸出完全一樣**，
  而它是一支專門為了防止沉默失敗而寫的測試——自己就是沉默失敗。
- **教訓**：**任何「掃描 → 斷言空集合」的測試，都要另外斷言分母不是零。**
  ⇒ 判準：**這支測試在「什麼都沒找到」時，會通過還是失敗？**
  會通過 → 它同時接受「乾淨」和「沒在測」，那它證明不了乾淨。
  作法：`assert seen > N`（掃到的檔數），或把根目錄錨在 `__file__` 上而不是 cwd。兩個都做。
- ⓘ 同一個形狀在別處也有：`grep | wc -l == 0`、「查詢回空陣列＝通過」、
  「沒有錯誤日誌＝沒有錯誤」。**空的結果永遠有兩個原因，而測試只認得一個。**
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/122](history/122-對話改名互動-而那個放最後的建議建立在高估五倍的數字上.md)；
  `tests/unit/test_vocabulary_interaction.py`（`assert seen > 20`）。

### 斷言「一個詞出現過」和斷言「一個規則還在」是兩件事

- **理論說**：測試裡寫 `assert "核心理解" in blob`，就表示那段 prompt 有在講理解優先。
- **實際發生**：⚠️ 那個條件**光靠 prompt 提到一次就滿足**。
  我把「與理解衝突時以理解為準」**整句刪掉**，測試照樣綠——
  它守的是一個詞的存在，不是那條規則的存在。
- **教訓**：**斷言要指向你真正怕失去的那句話，不是它附近的字。**
  ⇒ 判準：**把我怕的東西刪掉，這條測試會紅嗎？** 不會 → 它守的不是那個東西。
  文字型的斷言特別容易犯這個：詞會在很多地方出現，規則只在一個地方成立。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/117](history/117-文章改名應用-而重載的詞會沉默地弄壞prompt.md)；
  `tests/unit/test_field_chat_article.py::test_marked_as_ai_derivative`。

### 借一個比喻之前，先問「原版那個動作的代價，在這裡是不是同一個等級」

- **理論說**：領域就是資料夾，那「刪領域」就照著「刪資料夾」做——裡面的東西跟著走。
- **實際發生**：這裡面是**使用者的知識**。刪掉是不可逆的，而且事後
  ⚠️ **沒有任何辦法發現本來有什麼**——沒有回收桶、沒有 tombstone，連「少了幾件」都問不出來。
  檔案系統的刪除有回收桶和備份兜著，這裡什麼都沒有。
- **教訓**：**比喻借的是「形狀」，不是「代價」。**
  同一個動作在兩個系統裡的可回復性不同時，語意就不能照搬。
  ⇒ 判準：**問「做錯了要多久才發現、要多少力氣才救得回來」。**
  原版可回收、這裡不可回收 → **改語意**（這裡：刪的是**位置**，內容上移到父層，一件都不少）。
- ⓘ 一個好用的檢查：**能不能問出「我少了什麼」？** 問不出來的損失，就不該讓它默默發生。
- **來源**：[history/118](history/118-刪的是位置不是知識-而opacity-0仍佔寬度.md)；spec 054 FR-004。

### `opacity-0` 不改變版面——在寬度吃緊的地方用它隱藏，等於把空間讓給看不見的東西

- **理論說**：hover 才顯示的動作按鈕，用 `opacity-0 → group-hover:opacity-100` 就好。
- **實際發生**：280px 的樹面板裡，四個透明按鈕**照樣佔著寬度**，
  ⚠️ 深層節點的名字被擠到零寬，而 `truncate` 讓它**靜靜消失**——
  畫面上只剩一個資料夾圖示，不報錯、不換行、不溢出。**截圖才發現。**
- **教訓**：**`opacity` / `visibility` 不改變版面；`display` / 寬度才改變。**
  ＋ 更一般的：**`truncate` 會把版面錯誤變成沉默的**——內容不會溢出來告訴你哪裡不夠，
  所以「東西擠不下」這件事在有 truncate 的地方**永遠不會自己現形**。
  ⇒ 判準：**在寬度會吃緊的容器裡，隱藏要用「不佔位」的方式**（或乾脆換成圖示）。
- ⓘ 這也是「為什麼要看截圖」的一個具體理由：這類缺陷不會出現在任何測試或錯誤訊息裡。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/118](history/118-刪的是位置不是知識-而opacity-0仍佔寬度.md)；
  `DomainsPage.tsx` 的 `renderNode`。

### 隱藏一件東西時，要問「它是不是也從**決策路徑**上消失了」——不只是從畫面上

- **理論說**：封存過的知識在清單頁過濾掉，就算退場了。
- **實際發生**：它仍留在餵給聊天與 RAG 的語料裡 ⇒ ⚠️ **繼續影響每一個回答**，
  而且**沒有任何跡象**——回答依然流暢、來源依然標得出來，使用者以為它已經退場。
  實測：活的吸引子語料要從 549 掉到 496，才是真的擋住了。
- **教訓**：**「不顯示」和「不參與」是兩件事，而使用者只驗證得到前者。**
  凡是「隱藏／停用／封存／軟刪除」，動手前先列出**所有會讀到它的地方**，
  ⇒ 判準：**問「還有誰會讀它？」**——清單、計數、檢索、快取、嵌入、匯出、統計。
  少過濾一處就是沉默失敗，而那一處會繼續把它當成活的。
- ⓘ 有效的作法是一支 **sweep 測試**：封一件，斷言它**不在任何一份活的清單裡**。
  少過濾一處就會紅——這比逐一記得每個讀取點可靠。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/119](history/119-封存-結束不等於湮滅.md)；`tests/unit/test_archive.py`。

### 一個「安全」的保證，如果使用者驗證不到，它在他的決策裡就不存在

- **理論說**：封存不會刪掉東西，所以使用者可以放心按。
- **實際發生**：沒有「已封存」那一格的話，封存在他眼裡**跟刪除沒有差別**
  ——他驗證不到東西還在，就不會信任它，也就不會用。
- **教訓**：**保證要可驗證，否則它只存在於實作者的腦裡。**
  ⇒ 判準：**使用者要按什麼、看到什麼，才能自己確認這個保證成立？**
  答不出來 → 那個「安全」還沒做完，可視化不是附加功能，是保證的一部分。
- ⓘ 同一條也決定了要記什麼：`archived_at`（什麼時候）、`archived_root`（被誰帶走的）
  ——沒有它們，「這是什麼時候、為什麼不見的」問不出來。
- **來源**：[history/119](history/119-封存-結束不等於湮滅.md)；spec 055 FR-002。

### 改行為時，把所有承諾過舊行為的字一起找出來——不一致的文案比舊行為更糟

- **理論說**：把刪除改成封存，行為變好了，文案之後再說。
- **實際發生**：五處還寫著「刪除」「**不可復原**」。⚠️ **那句話現在是假的**
  ——使用者會因此不敢按，或按了以為東西沒了。行為變安全，體感反而更危險。
- **教訓**：**介面的承諾是行為的一部分，改了一半等於沒改。**
  ⇒ 判準：**grep 舊行為的關鍵詞**（不可復原／永久／刪除／清空），一處都不留。
  一個描述已經不成立的介面，比一個誠實描述舊行為的介面更糟。
- **來源**：[history/119](history/119-封存-結束不等於湮滅.md)。

### 要防止誤觸，改路徑比加確認有效——確認是同一條路上多一道問句

- **理論說**：危險動作加一個「確定嗎？」就好。
- **實際發生**：使用者要「第二次的死」（真的抹除）。做法不是加更嚇人的確認，
  而是**讓抹除只作用在遺骸上**——活的東西**沒有任何單一動作**能一次消失，
  你一定要先封存、再回頭抹除。
- **教訓**：**確認視窗是同一條路上多一道問句，而人會學會無視它；
  兩段式是把危險動作移到另一個位置，誤觸在物理上就到不了。**
  ⇒ 判準：**問「最快的誤觸路徑有幾步」**——加確認仍是一步（點、確定），
  兩段式是「先做完另一件事，再回來」。
- ⓘ 附帶好處：中間那個狀態（遺骸）本身就有價值——它讓「我少了什麼」問得出來。
- **來源**：[history/120](history/120-兩次死亡-而正確的拒絕不該長得像故障.md)；spec 056 FR-001。

### 一個「不能做」如果長得像「壞掉了」，那它就還沒做完

- **理論說**：`restore` 對已抹除的東西丟 `ValueError`，行為正確，測試也綠。
- **實際發生**：真的打一次 HTTP ⇒ **500 Internal Server Error**。
  行為是對的，但使用者看到的是「伺服器錯誤」——他會以為系統壞了、會重試、會回報 bug。
  ⚠️ **單元測試看不到這個**：`assertRaises` 會通過，因為它測的是拋出，不是**呈現**。
- **教訓**：**拒絕也是一種回答，它要說得出理由。**
  4xx ＋ 一句人話（「這件東西已經被抹除了，救不回來」），不是 5xx。
  ⇒ 判準：**每一個 `raise`，問它在使用者那一端長什麼樣。**
  接不住的例外會把**設計好的邊界**變成**看起來的故障**。
- **來源**：[history/120](history/120-兩次死亡-而正確的拒絕不該長得像故障.md)；實跑才發現。

### 同一個防護做兩層時，要問「哪一層是真的擋住的」——多做的那層不是免費的

- **理論說**：封存時順手把 `entry_embeddings` 也清掉，「否則檢索仍會命中」，多一層更安全。
- **實際發生**：⚠️ **那個理由是錯的**——檢索一律先過 `list_corpus_entries`，
  而那裡已經濾掉封存的。清向量是多餘的，而且**弄壞了復原**：
  復原後向量沒了、要重算一次 API，**且不會報錯**。
- **教訓**：**「反正多一層更安全」會蓋掉「這一層有它自己的副作用」。**
  ⇒ 判準：**先確定哪一層是真的擋住的，再問其他層買到了什麼、代價是什麼。**
  買不到東西的那層要拿掉——它不是冗餘，是一個沒人在看的副作用來源。
- ⓘ 這次正確的時機是**抹除**（內容都不在了，向量留著就是幽靈），不是封存。
- **母概念**：[沉默的失敗不會自己現形](concepts/沉默的失敗不會自己現形.md)
- **來源**：[history/120](history/120-兩次死亡-而正確的拒絕不該長得像故障.md)；spec 056 FR-008。

### 永久層指向短暫層，那個短暫層就再也短暫不了

- **理論說**：`draft/` 是短期記憶，完成後放手；`history/` 是永久的因果軌跡。兩層分工清楚。
- **實際發生**：35 份 draft 裡 26 份已完成，卻一份都清不掉。量了才看到原因：
  ⚠️ **`history/` 有 59 處指向 `draft/`**（`concepts` 10、`episodes` 6）——
  永久的東西指著會退場的東西，於是**被引用過的 draft 就再也刪不掉**，
  它從「短期記憶」變成「已完成設計的檔案櫃」。
- ⚠️ **我一度把它當成「不能刪」的理由**（「刪掉會產生 31 條死連結」）——那是合理化。
  死連結是**要修的東西**，不是保留的理由；那句話真正的意思只是「刪要連著改指」。
- **教訓**：**兩層的生命週期不同時，指標只能從短的指向長的。**
  反過來指，短的那層就被釘死了，而且**不會有任何錯誤**——它只是慢慢長大。
  ⇒ 判準：**寫下一個指標之前，問「這兩端誰活得比較久」。** 指向活得短的那個，就是在延長它。
- ⓘ 修法不是回頭改記錄（那是竄改），是改規則：往後寫 `history/` 時，
  設計源指向 **`episodes/`**（永久的現場）或把理由直接寫進本文，**不指 `draft/`**；
  而那份現場要在**寫 history 的同時**抽出來——補做貴一個數量級。
- **來源**：2026-08-26 draft 盤點與第一批清理（35 → 27、episodes 4 → 7）；
  [history/README](history/README.md) 末段的結構規則；[draft/README](draft/README.md) 的清理流程。

### 同一個狀態存兩份，就會有一半跟著走、一半留在原地

- **理論說**：頁面裡的「目前選取的領域」用一個 `sel` state，初始值取自 URL 的 `?d=`，很自然。
- **實際發生**：點一個資料夾之後 ⚠️ **側欄進去了、右邊清單沒進去**——
  `sel` 只在**初始化**時讀 URL，之後 URL 變了它不跟。
  兩個消費者讀不同的來源，就各走各的。
- **教訓**：**一個狀態只能有一個真相；「初始值取自 X」等於複製了 X，而複製品不會更新。**
  ⇒ 判準：**問「這個值改變的時候，誰負責通知另一份」。** 答不出具體的一行程式碼 → 那份要砍掉。
  這裡的修法是把 `sel` 整個刪掉，當前位置只認 URL——**少一個 state 就是少一種不同步**。
- ⓘ 它壞得很明顯（畫面自相矛盾），但**只有真的點下去才會看到**——
  型別檢查、測試、build 全綠。
- **來源**：spec 057 實跑；`DomainsPage.tsx`。

### 在 `pointerdown` 改狀態，會跟同一個元素上的點擊互相抵消

- **理論說**：拖曳要在 `pointerdown` 就決定「拖哪一批」，順手把它設成選取狀態最直覺。
- **實際發生**：⚠️ **勾選框完全沒反應**。`pointerdown` 先 `setPicked({這一個})`，
  接著勾選框的 `onChange` 又 `toggle` 掉它——**兩個都執行了，淨結果是沒事發生**。
- **教訓**：**`pointerdown` 是「可能要開始做某件事」，不是「做了」。**
  在它裡面改狀態，等於替使用者決定了他其實還沒決定的事，
  而同一個元素上的 click / change 會接著推翻它。
  ⇒ 判準：**這個狀態改變，在使用者放開手指之前有必要嗎？** 沒有 → 移到門檻之後
  （這裡：真的移動超過 6px 才把選取設起來）。
- **來源**：spec 057 實跑；`DomainsPage.tsx` 的 `beginDrag`。

### 只有一種輸入裝置到得了的路徑，在另一種裝置上是「功能不存在」——而它不報錯

- **理論說**：hover 出現的勾選框、右鍵選單、`hidden md:block` 的側欄，都是常見的桌機作法。
- **實際發生**：⚠️ 這三個加起來，讓手機上那一頁**只能看不能用**——
  選不到任何東西、到不了改名／封存、整棵樹不存在。而它**一個錯都不報**：
  頁面正常渲染、測試全綠、`npm run build` 綠。
  ⓘ **這是同一族的第二次**（第一次是 HTML5 drag-and-drop 在觸控上完全不觸發）。
- **教訓**：**`hover` / 右鍵 / `md:` 斷點都是「只有某類裝置有」的能力，
  用它們當某個動作的唯一入口，等於在另一類裝置上刪掉那個動作。**
  ⇒ 判準：**逐一列出每個動作，問「用手指怎麼做到」。** 答不出來就是那個裝置上沒有這個功能。
- ⓘ 而且**兩邊的慣例不同，不能用同一套硬套**：手機是 iOS Files／Drive
  （點＝進入、長按＝選單、標題列「選取」）；桌機是 Finder（單擊選、雙擊開、右鍵選單）。
  「貼近習慣」的意思是**抄對那個平台的**，不是自創一套兩邊都半生不熟的。
- **來源**：spec 058；`lib/longpress.ts`；[history/115](history/115-在空集合上出貨-而測試是我自己餵的初始狀態.md) 的同族第一次。

### 工具回報「成功」不等於它做到了——量結果，不要量回傳值

- **理論說**：`resize_window` 回 `Successfully resized window to 400x800`，那視窗就是 400 寬了。
- **實際發生**：⚠️ `window.innerWidth` 仍是 **1470**。回傳值是「我送出了這個指令」，
  不是「這件事發生了」。如果我信了它，就會在**桌機寬度**上宣稱驗過了行動版。
- **教訓**：**驗收要量**那件事本身的可觀測後果**，不是量你呼叫的那個 API 的回傳值。**
  ⇒ 判準：**問「如果它其實沒做到，我這個檢查會不會照樣通過」。** 會 → 換一個量法。
  （這裡改用 390px 的 iframe 去量真的 CSS 斷點行為。）
- ⓘ 同一族的第三個實例：前兩個是 `gh run list --limit 1` 回上一次的 run 說「success」、
  以及 pod ready ＋ digest 對但 migration 其實還沒跑。
- **來源**：spec 058 實跑。

### 觸控上「捲動」與「操作」開頭一模一樣——綁在 `pointerdown` 的東西都會被捲動觸發

- **理論說**：拖曳綁 `pointerdown`、點選靠 `click`，各走各的，很清楚。
- **實際發生**：使用者一句「滑動就會被視為點選」。查下去發現**第二個更嚴重的**：
  捲動時手指移動 > 門檻 ⇒ 拖曳 `armed`，⚠️ **手指停在資料夾列上放開就真的把知識搬過去**
  ——而資料夾排在清單最上面，畫面只閃過一句「搬好 1 件」。
- **教訓**：**在觸控上，`pointerdown` 不代表使用者要操作，多數時候他只是要捲。**
  ⇒ 判準：**問「使用者只是想捲畫面的話，這段程式會做什麼」。**
  答案不是「什麼都不做」→ 那個行為會被捲動誤觸，而使用者不會知道自己觸發了它。
  ＋ 「是不是點選」要看**按下與放開的距離**，不能只靠瀏覽器送不送 `click`
  ——捲動後的抑制**不是每次都發生**。
- ⓘ 修法選了**關掉觸控拖曳**而不是調門檻：門檻只是把誤觸變少，
  而這裡誤觸的代價是**悄悄搬走知識**。能力用別的路徑補（長按選單的「搬到…」）。
- **來源**：spec 059；`lib/tap.ts`、`DomainsPage.tsx` 的 `beginDrag`。

### 防誤觸的預設要 fail-open：吃掉一個合法操作，比放行一個誤觸更糟

- **理論說**：判斷不出來是不是點選（例如沒記到按下位置）時，保守一點就別觸發。
- **實際發生**：那會把**鍵盤觸發、程式觸發、輔助工具觸發**的合法點擊一起吃掉，
  ⚠️ 而使用者看到的是「按了沒反應」——他會重按、會以為壞了，**而且沒有錯誤訊息**。
- **教訓**：**攔截器的預設方向要看兩種錯誤的代價，不是看哪個比較保守。**
  ⇒ 判準：**問「攔錯了會怎樣 vs 放行錯了會怎樣」。**
  這裡：攔錯＝功能消失（沉默）；放行錯＝多開一個頁面（可回上一頁）⇒ **fail-open**。
  ⓘ 但同一個判準在**拖放**那邊給出相反答案：放行錯＝悄悄搬走知識 ⇒ 那裡要 fail-closed。
  **同一支功能裡的兩個攔截器，方向可以是相反的。**
- **來源**：spec 059 FR-004；`lib/tap.ts` 的 `isTap(null, …) === true`。

### 同一個手勢在不同平台有不同慣例——「貼近習慣」要問是**誰的**習慣

- **理論說**：長按＝右鍵，做一個選單就對了。
- **實際發生**：使用者要的是「長按**啟動批次功能**」。查下去兩邊真的不一樣：
  **iOS Files** 長按出選單；**Android／Drive／Gmail** 長按**直接進選取模式**。
  兩個都是「正確的慣例」，差別在**你在對誰做**。
- **教訓**：**「貼近使用者習慣」不是一個設計原則，是一個需要指名對象的問句。**
  ⇒ 判準：**問「這個手勢在他天天用的那個 app 裡做什麼」**——而不是問「業界標準是什麼」。
  ⓘ 選了 Android 那套之後，選單並沒有消失：它退回**桌機右鍵**專用，
  而它的四個動作在批次模式的底部列全都有 ⇒ **能力沒少，只是入口換了。**
  換慣例前先數一數能力會不會掉，是這種改動唯一該做的檢查。
- **來源**：spec 059 FR-005；`DomainsPage.tsx` 的 `pressHold`。

### 你自己實作的那條路徑之外，瀏覽器常常還有一條——而兩條會同時跑

- **理論說**：長按要做什麼由我決定，我把 450ms 的計時器改成「進批次模式」就好。
- **實際發生**：使用者回報「長按仍然跳出選單」。⚠️ **瀏覽器在觸控長按時會自己送
  `contextmenu`**（Android Chrome／iOS Safari 都會）——我的批次模式進去了，
  原生那條同時把選單叫出來。**兩條路徑都跑了。**
- **教訓**：**平台對常見手勢已經有預設行為；你「新增」的處理是疊上去，不是取代。**
  ⇒ 判準：**問「這個手勢，瀏覽器原本就會做什麼」**——長按（`contextmenu`）、
  雙擊（選字）、拖曳（`dragstart`）、右滑（上一頁）都有預設行為。
  要獨佔就得明確關掉它，而不是假設自己的處理會贏。
- ⓘ 一個具體的坑：`contextmenu` 事件**不帶 `pointerType`**
  ⇒ 想分辨「這是滑鼠右鍵還是手指長按」，得自己記住上一次 `pointerdown` 的裝置。
- **來源**：spec 059 T014–T015；`DomainsPage.tsx` 的 `lastPointer`。

### 一個模式有兩條進入路徑時，退出鍵一定要讀**推導值**，不能讀其中一條的旗標

- **理論說**：「選取模式」用一個 `selecting` 布林就夠了，按鈕讀它、切換它。
- **實際發生**：批次模式其實有**兩條**進入路徑——長按會設 `selecting`，
  而直接勾選只會動 `picked`。⚠️ 用勾選進去的人，按鈕顯示的是「選取」，
  **按下去反而把他推得更深**，他就退不出來了。
- **教訓**：**「現在處於某個模式嗎」如果有多個成因，它就是一個推導值，不是一個狀態。**
  ⇒ 判準：**列出所有進入這個模式的路徑，問「退出鍵讀的東西，每一條路徑都會設嗎」。**
  有一條不會 → 那個模式要改成 `derived = A || B || …`。
- ⓘ 這是「同一個狀態存兩份」的**第二次**（第一次：`sel` 與 `did` 讓側欄進去了、清單沒進去）。
  差別在上次是兩份**同義**的狀態，這次是兩個**成因**——但症狀一樣：一半跟著走、一半沒有。
- **來源**：spec 060 T001；`DomainsPage.tsx` 的 `inBatch`。

### 一個作廢的按鈕不會只是沒用——它多半還在做**當初那件事**

- **理論說**：spec 040 移除暫存分層後，「💾 存下這段」變多餘了，留著只是佔位。
- **實際發生**：⚠️ 它會把 `tempId` 清成 `null`（那是「已升為永久」的舊語意），
  於是**下一次 autosave 就再開一段新對話**——**按一下等於複製一份**，
  而畫面上只顯示「已存下」。作廢的不是行為，只是那個行為的**理由**。
- **教訓**：**移除一個機制時，它的 UI 入口不會自己失效——它會繼續執行舊語意。**
  ⇒ 判準：**每砍掉一層抽象，就 grep 一次「誰還在呼叫它」**，
  然後逐一問「這個呼叫在新世界裡做了什麼」——答案常常不是「沒事」。
- ⓘ 順帶一條處置紀律：**前端入口拿掉、後端端點留著**。
  端點是人閘門的一部分、有契約測試在守，而刪 API 的代價比留著大。
- **來源**：spec 060 T004；`ChatPage.tsx`、`test_temp_save_web.py:91`。

<!--
  這張表是 /knowie-next 和 /knowie-judge 的「閱讀路由」。
  主文只寫蒸餾後的教訓；完整的因果軌跡放在 history/ 子目錄。
  當某個主題的關鍵字被觸發時，動手前 MUST 先讀對應的 sub-file。
  一開始可以留空；每當你把細節移到子目錄，就回來補一列。
-->

| 觸發關鍵字 | MUST 讀 |
|---|---|
| 沉默失敗 / 沒人會發現 / 儀器說謊 / 對抗測試 / 可觀測性 | [concepts/沉默的失敗不會自己現形.md](concepts/沉默的失敗不會自己現形.md) |
| 架構 / 相依 / 後端 / embedding / LLM / 測試策略 | [history/002-架構選型與務實偏離.md](history/002-架構選型與務實偏離.md) |
| 知識輸出 / 生成文章 / 高證實 / references / 長度難度 | [history/083-知識的輸出-高證實文章生成第一刀.md](history/083-知識的輸出-高證實文章生成第一刀.md) |
| 輸入法 / IME / isComposing / 前端輸入 / Enter 送出 / CJK | [history/083-知識的輸出-高證實文章生成第一刀.md](history/083-知識的輸出-高證實文章生成第一刀.md) |
| 全形標點 / marked / CommonMark / 渲染 / MathJax / 競態 | [history/091-前端渲染兩坑-全形標點與typeset競態.md](history/091-前端渲染兩坑-全形標點與typeset競態.md) |
| 膜 vs 場 / bare / 不接知識庫 / 功能退場 / 沒人用 | [history/090-腦力激盪退場改不接知識庫.md](history/090-腦力激盪退場改不接知識庫.md) |
| 回答太長 / 截斷 / max_tokens / finish_reason / 長度紀律 / 歸因 | [history/092-回答截斷可觀測與長度紀律-兩個假設被實測推翻.md](history/092-回答截斷可觀測與長度紀律-兩個假設被實測推翻.md) |
| 出貨 / 部署 / 推送 / digest 核對 / 收工流程 | [skills/ship-knowfield/SKILL.md](skills/ship-knowfield/SKILL.md) |
| 跑起來看 / 真驗收 / 本機啟動 / port / 前後端 | [skills/run-knowfield/SKILL.md](skills/run-knowfield/SKILL.md) |
| 有沒有人用 / 消費者 / 使用審計 / 殘骸偵測 | [skills/audit-field-usage/SKILL.md](skills/audit-field-usage/SKILL.md) |
| 數字是 0 / 量不到 / 儀器說謊 / 訊號起算時間 / 事實來源 | [history/108-對話由來徽章讀錯欄位-儀器說謊的第三種形態.md](history/108-對話由來徽章讀錯欄位-儀器說謊的第三種形態.md) |
| 加欄 / 既有表 / migration / 雙後端 parity | [history/106-三刀被做成量不到的-加欄地基補上.md](history/106-三刀被做成量不到的-加欄地基補上.md) |
| skill 過期 / 小腦 / 沒有 oracle / 退役子系統的尾巴 | [history/107-退役會教錯路的來源skill-小腦沒有oracle.md](history/107-退役會教錯路的來源skill-小腦沒有oracle.md) |
| 抹除 / 第二次的死 / 兩段式 / 拒絕 vs 故障 / 冗餘防護 | [history/120-兩次死亡-而正確的拒絕不該長得像故障.md](history/120-兩次死亡-而正確的拒絕不該長得像故障.md) |
| 封存 / 遺骸 / 隱藏 vs 不參與 / 可驗證的保證 | [history/119-封存-結束不等於湮滅.md](history/119-封存-結束不等於湮滅.md) |
| 刪除 / 資料夾比喻 / 不可逆 / 版面擠壓 / truncate | [history/118-刪的是位置不是知識-而opacity-0仍佔寬度.md](history/118-刪的是位置不是知識-而opacity-0仍佔寬度.md) |
| 改名 / 詞彙 / 重載 / 應用 vs 文章 / 全域取代 | [history/117-文章改名應用-而重載的詞會沉默地弄壞prompt.md](history/117-文章改名應用-而重載的詞會沉默地弄壞prompt.md) |
| 當前領域 / 站在哪 / 側欄 / 繼承 / 出生歸位 / 立足點 | [history/116-病因在上游-而繼承不是猜.md](history/116-病因在上游-而繼承不是猜.md) |
| 整理 / 批次 / 拖放 / 清冊 / 起始狀態 / 互動次數 | [history/115-在空集合上出貨-而測試是我自己餵的初始狀態.md](history/115-在空集合上出貨-而測試是我自己餵的初始狀態.md) |
| 領域 / 主題 Topic / 樹 / 搬動 / 糾纏 tangle / DAG / RAG scope | [history/113-知識庫長出樹-而第一刀刻意不碰grounding.md](history/113-知識庫長出樹-而第一刀刻意不碰grounding.md)、[history/114-糾纏從裁決降級為問句-而護欄被實測的數字定住.md](history/114-糾纏從裁決降級為問句-而護欄被實測的數字定住.md) |
