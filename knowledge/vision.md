# 願景

<!--
  這份文件描述你的專案要往哪裡走。它會隨著理解的加深而演進。
  在每個里程碑完成後，或是優先順序改變時更新它。
-->

## 問題陳述

AI 領域（工程實作、學術論文、最新趨勢）的資訊爆量且更新極快，人工追蹤成本高。
使用者橫跨工程師／開發者、研究者／學生、產品／決策者，各自需要的資訊深淺不同，
既有的新聞或訂閱服務要嘛太淺（只知道發生了）、要嘛太雜（來不及消化）。

## 核心想法

LearnNews 是**「消化＋溯源」工具**：對材料做**完整消化**幫使用者省時（先看消化結果），
同時每則都保留**一鍵直達原文**的路徑（再視需要看來源）。透過**推**（每日匯整：去重＋
排序＋消化＋來源直達）與**拉**（主題深挖：擴展來源、消化、溯源）兩種模式，讓使用者以
極低成本跟上趨勢。**消化到底（含跨來源 RAG 問答），隨時可回溯來源。**
（轉移鏈：原定位「分診工具、不消化」→ 2026-07-23 回調「消化以省時，溯源不可省」→
2026-07-24 再進一步「消化到底，隨時可回溯」、為 RAG 讓路——見 `principles.md` 原則 4
修正紀錄與 `history/016-原則4再修-消化到底加RAG.md`。）

**差異化定位（2026-07-23 於階段 1 定案）**：與現有個人化論文工具（如 Scholar Inbox）
不同，LearnNews 的差異化在**廣度**（新聞×論文跨源去重，不只論文）與**主權**（使用者
保有過濾控制權），而非個人化推薦本身。

詳細設計理由見 `episodes/2026-07-23-雙模式推拉的設計.md`、`draft/2026-07-23-興趣畫像.md`、
`episodes/2026-07-23-競品與來源研究快照.md`。

## 現狀

全新發想，尚無程式碼。已用 spec-kit 建立專案憲章（`.specify/memory/constitution.md`）、
用 knowie 建立知識庫。下一步進入第一個規格階段。

## 架構

已於階段 1 定案（2026-07-23）：**Python 3.12＋薄 CLI／核心函式庫、SQLite 儲存**。
去重／興趣排序的 embedding 與封頂摘要都藏在**可插拔介面**（`Embedder`／`Summarizer`）後：
預設離線 stub（確定性、零安裝），真實後端走**使用者的 OpenAI 格式 API**
（embedding＋chat，stdlib urllib、零新增相依）。技術脈絡見
`specs/001-daily-triage-digest/plan.md`；真實後端決策見
`history/004-真實後端改用-OpenAI-格式-API.md`（取代 002 的 Claude Haiku/ST 計畫）。

## 路線圖

<!--
  完成里程碑時更新勾選框，並標**完成日期**：`- [x] 完成（YYYY-MM-DD）`。
-->

### 階段 1：需求規格與來源盤點

- [x] 完成（2026-07-23）

<!--
  交付：spec.md（新聞來源、興趣過濾、摘要格式的定義）
  前置條件：無
-->

**成功標準：**
- [x] `spec.md` 完成並經確認（`specs/001-daily-triage-digest/spec.md`）
- [x] 新聞來源清單確定（論文骨幹＋少量精選新聞，X 排除）
- [x] 興趣過濾模型與摘要格式定義完成（2026-07-23 由「讀者角色分層」修正，對齊原則 2）

### 階段 2：每日推播匯整（推）

- [x] 完成（2026-07-23）

<!--
  交付：每日匯整——去重＋排序＋濾成「今天值得點的幾則」，含來源直達
  前置條件：階段 1
  設計理由：episodes/2026-07-23-雙模式推拉的設計.md
  實作：specs/001-daily-triage-digest/（src/learnnews/、49 測試綠燈、HF 真實跑通）
-->

**成功標準：**
- [x] 對選定來源可穩定產出每日匯整
- [x] 跨來源去重生效（同一則消息不重複出現）
- [x] 每則皆可直達有價值的原文
- [x] 摘要止於「一句定位＋一句為何值得看」
- [x] 具備涵蓋抓取、去重、排序、摘要行為的自動化測試（TDD）

### 階段 3：主題拉取深挖（拉）

- [x] 完成（2026-07-23）

<!--
  交付：使用者對某主題有興趣時，擴展來源、溯源直達原文
  前置條件：階段 2
  設計理由：episodes/2026-07-23-雙模式推拉的設計.md、draft/2026-07-23-興趣畫像.md
  實作：specs/002-topic-pull/（src/learnnews/pull/、82 測試綠燈；含 US2 匯整落庫）
  完成記錄：history/008-拉模式完成.md
  註：原「讀者分層投影」已於 2026-07-23 被取代（見 principles 原則 2 修正紀錄）
-->

**成功標準：**
- [x] 對指定主題可擴展並彙整相關來源
- [x] 結果可回溯直達原文，工具不做結論式分析
- [x] 拉取邏輯有測試覆蓋

### 階段 4：知識沉澱 → 個人知識庫（可 RAG 問答）

- [x] **增量 1（MVP）完成（2026-07-24）**：RAG over 流——**CLI `ask` ＋ web `/ask`**（history/023）
  對已落庫匯整可溯源問答。147 測試綠、離線 e2e＋**真跑抽查通過**（SeerGuard 答案逐句對照
  原文皆有據、零杜撰）。真跑另揪出並修好門檻校準 bug（依 embedder 尺度分流，見教訓 4 延伸）。
- [x] **增量 2a 完成（2026-07-24）**：種子 ingest——人冊封第一個「深度吸引子」。
  `ingest <arXiv-id|url> [--explainer]` 抓單篇→消化→嵌入→存為 KB 種子；解說文品質權重。
  spec 006、161 測試綠、真實 arXiv 抓取驗證。**完整 ingest→ask 真跑留使用者（原則 5 冊封）。**
  完成記錄：`history/025-種子ingest完成.md`。
- [x] 增量 2b **完成（2026-07-25）**：根因萃取（試金石＋信心層級＋人冊封）——碰「吸引子本體」。
  → 已獨立成 **階段 10**（完整閉環：萃取＋冊封＋餵回 ask），見下。spec 012、history/040。
- [ ] 增量 3+（未 commit，concept 驅動）：吸引子拓撲／成核／衰減／反濾泡

<!--
  2a 設計理由（in-flight）：draft/2026-07-24-RAG問答.md（種子＋解說文＋根因萃取段）。
  模型：concepts/有吸引子的場.md（種子＝深度吸引子）；原則 5（權重由人冊封）。
  規劃：knowie-next 簡報（2026-07-24）、history/024。拆 2a/2b 理由：根因萃取最危險
  （plausible-BS），與機械的種子 ingest 分開，先鋪材料層再上判斷層。
-->

**增量 2a 成功標準：**
- [x] `ingest <arXiv-id|url>` 把單篇抓進來、消化、嵌入、存為 KB 語料（種子）
- [x] 種子進 KB 後，`ask`（CLI＋web）檢索得到、答案可溯源（沿用增量 1 檢索/門檻）
- [x] **解說文**成獨立來源類，檢索時**品質權重高於快訊**（`cosine×權重`，門檻仍原始 cosine）
- [x] 種子 MUST 掛原文連結（原則 3）；抓取失敗攔成友善繁中、不寫半殘（教訓 3）
- [x] 離線 stub 可測（`http_get` 可注入假來源，零外部呼叫）；147→161 測試不回歸

<!--
  復活緣起：2026-07-24。原「知識沉澱」曾降級（使用者已自我加工、有既有工具）；經「有吸引子
  的場」concept 收斂，形狀變清楚——知識沉澱＝可 RAG 問答的個人 KB（流＋種子兩管道）。
  設計理由（in-flight）：draft/2026-07-24-RAG問答.md ← 雙向連結，做完才反流退場。
  模型：concepts/有吸引子的場.md（增量 1 只鋪「流＋檢索」地板，吸引子/根因是後續增量）。
  規劃：history/019、knowie-next 簡報（2026-07-24）。
-->

**增量 1（MVP）成功標準：**
- [x] `ask "問題"` 對已落庫匯整回答，且**只據語料**、每個論點**掛來源**（原則 3；來源由檢索
  集合程式端生成、不靠模型自律）
- [x] `--today` 限當日、預設跨累積（問今天＋問累積都做）
- [x] **查無相關 → 明說「沒有相關材料」**，不杜撰
- [x] 存匯整時**批次嵌入**落庫；查詢只嵌問題＋cosine 暴力檢索（不上向量庫）
- [x] 外部後端失敗攔成友善訊息（experience 教訓 3），不噴 traceback
- [x] **離線 stub 綠燈**（Hashing＋Stub，零 API）；128→139 測試不回歸
- [x] **真跑抽查忠實**（真實後端）：SeerGuard 答案 5 個論點逐句對照原文皆有據、零杜撰；
  查無說無；順帶揪出並修門檻校準 bug。實作：`specs/005-rag-qa/`、`src/learnnews/rag/`；
  完成記錄：`history/021-RAG-MVP-實作完成.md`

### 階段 5：可讀文章式消化（升級摘要）

- [x] 完成（2026-07-23）

<!--
  交付：把每則摘要從「一句定位」升級為可讀散文＋圖，推拉皆套用
  前置條件：階段 2、3（已完成）
  設計理由：episodes/2026-07-23-可讀文章式消化的設計.md
  實作：specs/003-readable-digest/（src/learnnews/summarize/article.py、media/；103 測試綠燈）
  完成記錄：history/010-可讀文章式消化完成.md
  緣起：原則 4 於 2026-07-23 回調為「消化以省時，溯源不可省」，見 history/009
-->

**成功標準：**
- [x] 每則產出**可讀散文**，完整傳達重要訊息（重點／關鍵數據／適用時機），非「一句定位」封頂
- [x] 每則保留**一鍵直達原文**（原則 3）
- [x] 配圖：原文圖優先；AI 圖明確標「AI 示意・非原文」
- [x] 散文**不捏造**：原文沒有的數據不寫（**已於真實後端抽查通過**，SeerGuard 數據全在原文；見 history/012）
- [x] 推、拉皆套用；`--raw` 純原礦仍保留為選項

### 階段 6：Web 介面

- [x] 完成（2026-07-23）

<!--
  交付：本機 FastAPI web app——看今日匯整（散文＋圖內嵌＋一鍵原文）、輸入主題即時拉、
        管理興趣清單；複用既有核心（digest/pull/interests）。
  技術：FastAPI＋Jinja2＋Tailwind（Play CDN）。相依只在 web 層，核心仍零相依。
  前置條件：階段 2/3/5（已完成）
  設計理由：draft/2026-07-23-部署與介面路線.md（Web 段已完成）
  實作：specs/004-web-ui/（src/learnnews/web/；127 測試綠燈、真跑 uvicorn 驗證）
  完成記錄：history/013-Web-介面完成.md
  執行順序：階段 6（Web，已完成）→ 部署段（容器化→K8s→Helm）為**後續、孵化中、不綁定
  階段號**（見 draft/2026-07-23-部署與介面路線.md）；階段 7/8 由知識庫管理/來源訂閱先插入。
-->

**成功標準：**
- [x] 首頁顯示今日匯整：每則散文＋原文圖**內嵌**＋可點原文連結（原則 3）
- [x] 可輸入主題**即時拉**、回傳散文結果（含 TTL 快取避免狂打 API）
- [x] 可檢視／增／刪興趣主題
- [x] **RWD**：手機／桌面皆正常呈現（Tailwind）
- [x] 全繁中；後端失敗在 web 層攔成友善 503 頁（experience 教訓 3）

### 階段 7：知識庫管理（前端策展／修剪）

- [x] **完成（2026-07-25）**：web `/library` 管理**種子**——瀏覽／刪除／重分類（解說文↔一般）。
  ＝原則 5 的「另一半」（人冊封不只加，也退/改）＝場的人工修剪。spec 007、177 測試綠。
  完成記錄：`history/030-知識庫管理完成.md`。

<!--
  設計理由：原 draft/2026-07-25-知識庫管理.md 已反流退場（roadmap 完成）→ 決策場景見 history/030、
  概念投影見 concepts/有吸引子的場.md（管理＝人工修剪場、退吸引子=手動衰減）。
  緣起：使用者問「前端能不能有管理功能」，點破「工具只有進水口、缺修剪」。規劃：history/029。
  範圍：只碰種子（每日流唯讀）；來源管理接階段 8、共用 /library 模式。
  （draft 退場時薄殘點「批次操作/搜尋」讓其 decay——要時會重新冒出。）
-->

**成功標準：**
- [x] `/library` 列出種子（標題／來源類／日期／原文連結）
- [x] **刪除**種子 → 連 `entry_embeddings` 一起清（`ask` 不再檢索到、無孤兒向量，教訓 8）
- [x] **重分類**（解說文↔一般）→ 改 `source_class`，檢索權重即時跟上
- [x] 只碰**種子容器**、每日流條目唯讀（repo 層結構保證，非靠 UI）
- [x] 照 `/interests` web CRUD 模式；離線 stub 可測；166→177 測試不回歸

### 階段 8：來源訂閱（自助加/管理來源）

- [x] **完成（2026-07-25）**：web `/sources`——貼 RSS/站台 URL 自助加來源（RSS 自動探測＋
  加前驗證有料）＋列出/停用/刪除。＝第三種追蹤粒度（站台/作者＝持續水源，原則 5 人冊封）。
  spec 008、190 測試綠、抓取管線零改。完成記錄：`history/032-來源訂閱完成.md`。

<!--
  設計理由（in-flight）：draft/2026-07-25-來源訂閱.md（＋知識庫管理 draft 的來源管理面合流）。
  模型：concepts/有吸引子的場.md（訂閱＝冊封持續水源、流的進水口）。
  緣起：使用者問「某站/部落格/作者值得關注怎麼加」「前端能不能自助」。規劃：knowie-next（2026-07-25）、history/031。
  現成關鍵：build_adapters(sources) 已吃 DB sources → 加來源進表即被 digest 抓，抓取管線零改。
  範圍：先做 RSS（有 feed 的站）；無 RSS 站的 email-ingestion、作者跨平台、每來源品質加權 → 後續。
-->

**成功標準：**
- [x] `/sources` 列出來源，可**停用/啟用/刪除**
- [x] 貼 **RSS 或站台首頁 URL** → 自動探測 feed → **實測有料才加**（死 feed/無 RSS → 友善提示不加壞）
- [x] 加的來源**下次 digest 自動帶入**（build_adapters 已吃 DB sources，零改管線；測試驗 adapter 被建）
- [x] RSS 探測/驗證用可注入 `fetch` → **離線 stub 可測**（教訓 1）；抓取失敗攔友善繁中（教訓 3）
- [x] 復用 `sources` 表（無新 schema，教訓 8）；照 `/interests`/`/library` CRUD；177→190 測試不回歸

### 階段 8 增量：場驅動來源推薦（WebSearch 找進水口＋場驅動排序＋人訂閱）

> 護城河支柱二／北極星習慣 #1（信任/策展層）。手動加來源太慢、名冊太少、視野太小——要工具
> **主動撒網提候選來源、你挑著訂**（＝發現**進水口本身**，不只發現水）。設計源＝
> `draft/2026-07-25-來源訂閱.md`（「主動推薦來源」段，使用者 2026-07-26 定案「排序場驅動優先」）。

- [x] **已完成（2026-07-26）**：一個 opt-in 動作「幫我找新來源」→ `WebSearch` 專查 roundup（一般搜尋、
  非 news）→ 抽候選網域 → **複用 spec 008** `discover_feed`／`validate_feed`（擋死/幻覺 feed）→ **場驅動
  排序** → 推薦清單 → **人按訂閱才進名冊**（複用既有 `/sources/add`）。spec 020、history/055；307 測試綠。
  **真後端（Tavily）驗過**：撒網＋真抓開放網路 feed，回 3 個有活 feed 可訂（bair.berkeley.edu、
  research.google、aiconnections.substack.com）、10 個標「無 RSS」（誠實天花板、非報錯）。

<!--
  規劃：knowie-next（2026-07-26）。近乎全複用：WebSearch（spec 009/016 make_web_search）＋
  discover_feed/validate_feed/subscribe（spec 008 sources/subscribe.py）＋/sources 路由＋subscribe_factory。
  場驅動排序＝候選與你冊封的種子/根因嵌入相近（ensure_embeddings/cosine，spec 005/018 複用）。
  技能複用：knowledge/skills/evaluate-and-add-source 的評估邏輯接在「驗證」步（別重寫）。
  範圍：opt-in 一動作；out：自動訂閱（違原則 5）、每次匯整自動跑（花額度）、無 RSS 的好站（誠實天花板）、
  每來源品質加權（來源訂閱 draft 另一孵化段）。
  母概念：concepts/有吸引子的場（訂閱＝冊封持續水源）；護城河：draft 轉向場的護城河（場驅動＝用下游
  優化上游，Google 做不到，設計 A 近親）。
-->

**成功標準（可驗）：**
- [x] 一個 opt-in 動作回**候選來源清單**，每項含網域＋**活 feed**＋推薦理由
- [x] 排序**場驅動優先**：你冊封材料出自它 ＞ 有活 feed ＞ 跨清單重複（`(field_score, has_feed, list_hits)`）
- [x] **驗證先行**：死/幻覺 feed **不進**清單（複用 spec 008 `validate_feed`）；無 RSS 標示非報錯（誠實天花板）
- [x] **人按訂閱才進名冊**（不自動，原則 5）；**opt-in／按需**（非匯整自動跑，有守衛測）
- [x] WebSearch／`http_get` 可注入 → **離線 stub 可測**（教訓 1）；搜尋/抓 feed 失敗友善繁中（教訓 3）
- [x] 複用 `evaluate-and-add-source` skill 的評估邏輯於驗證步；測試 298→307 不回歸
- [x] **out of scope**：自動訂閱、每次匯整自動跑、無 RSS 的好站（訂閱）、每來源品質加權（未做）

### 階段 9：web 搜尋（開放網路進水口）

- [x] **已完成（2026-07-25）**：web `/search`——打 query → 可插拔搜尋後端回結果 → 每則可
  **「收進」冊封成種子**（複用 spec 006 ingest）。搜尋結果預設短暫、人冊封才留（原則 5）。
  ＝第三口進水（web search＝臨時撒的網、反濾泡）；沿用 OpenAI 後端款（urllib、可插拔、離線 stub）。
  **真跑通**：對接 Tavily（`api_key` 放 body）真搜回開放網路結果；spec 009、history/034。
  升級：智慧搜尋（整理＋排序）已完成——見下方「增量 b」。探索（optional）仍孵化。

<!--
  設計理由（in-flight）：draft/2026-07-24-趨勢熱詞發現.md（A 搜尋 adapter 段；B 趨勢讀數留後續）。
  模型：concepts/有吸引子的場.md（web search＝臨時大網/反濾泡；搜尋=AI撒網、冊封=人挑）。
  緣起：使用者問「有些東西只能靠當下 web search 找」。規劃：knowie-next（2026-07-25）、history/033。
  範圍：先 web 搜尋→結果→收進成種子；趨勢讀數（B）、拉擴開放網路 → 後續。
-->

**成功標準（可驗）：**
- [x] `/search` 打 query → 可插拔搜尋後端回結果（標題/網址/摘要）
- [x] 每則可**「收進」冊封成種子**（複用 ingest；之後 ask 可檢索到）
- [x] **搜尋結果預設短暫**（不落庫），只有收進的才留（原則 5 人冊封）
- [x] 搜尋後端**可插拔**：離線 stub（假結果、零外部呼叫綠燈，教訓 1）／真實 API（urllib、零 pip 相依）
- [x] 後端失敗攔友善繁中（教訓 3）；測試 190→197 全綠、零回歸（真跑通 Tavily）

### 階段 9 增量 b：智慧搜尋（整理＋排序）

- [x] **已完成（2026-07-25）**：`/search` 不再只倒原始結果——抓 top-N 結果內文、用 RAG
  answerer 合成一段繁中「整理」（逐點 `[n]` 掛結果）、依相關度排序；頁面頂端整理＋下方結果
  （仍可收進）。＝把「消化到底」（原則 4）套到 live 搜尋流＝**RAG over 搜尋結果**。
  spec 010、history/036；209 測試綠。**探索（optional）仍未做**（draft 探索段保留孵化）。

<!--
  設計理由（in-flight）：draft/2026-07-25-智慧搜尋.md（核心＝整理＋排序；探索 optional 未 promote）。
  模型：concepts/有吸引子的場.md（讀場套到 live 流；探索＝反濾泡走 agentic）。
  緣起：使用者對比「你的 Web Search 也會處理和探索」，問 /search 能不能有智慧處理。
  規劃：knowie-next（2026-07-25）、history/035。復用密度極高（fetch_url／answerer／Embedder）。
  範圍：先「處理」（整理＋排序）；探索（multi-angle／agentic）另列 optional 里程碑、成本翻倍。
-->

**成功標準（可驗）：**
- [x] 打 query → 回「整理」段（逐點 `[n]`）＋原始結果清單；整理 `[n]` 可捲到對應結果
- [x] 整理**只根據抓到的內文**（grounded，複用 RAG 防線）；抓不到退回 snippet／標示、完全無關明說不杜撰
- [x] 依相關度排序結果（Embedder＋cosine）
- [x] 結果仍可**「收進」**（原則 5 人挑不變）；整理是閱讀輔助不落庫
- [x] top-N 抓取／answerer 失敗友善繁中（教訓 3）；測試 197→209、零回歸
- [x] **探索 out of scope**（已另立增量 c，見下）

### 階段 9 增量 c：探索（多角度擴展 a）

- [x] **已完成（2026-07-25）**：`/search` 加**「深入探索」opt-in**（預設關）——開啟時 LLM 把 query
  拆成 3–5 子角度 → 各搜一次 → 合併去重 → 對合併池跑**現有整理**（spec 010）。＝反濾泡再進一步
  （撒更廣的網照到單一 query 的盲區）。使用者選 (a) 多角度（AskUserQuestion，2026-07-25）。
  spec 011、history/038；219 測試綠。**(b) agentic 仍 optional／未做**（draft 探索段保留）。

<!--
  設計理由（in-flight）：draft/2026-07-25-智慧搜尋.md（探索段 a）。規劃：history/037。
  模型：concepts/有吸引子的場.md（探索＝反濾泡/驚訝力）。
  成本閘：opt-in 預設關（根公理成本極低）＋子角度上限 5＋只 fetch top-N。單輪有界、可預測。
  範圍：只做 (a) 多角度；(b) agentic 追線索 optional／未 promote、留後續。
-->

**成功標準（可驗）：**
- [x] `/search` 有「深入探索」開關，**預設關**；關閉時行為＝增量 b（單 query）
- [x] 開啟時：LLM 拆 3–5 子角度 → 各搜 → **合併去重**（同 url 不重複）→ 跑現有整理
- [x] 可插拔查詢擴展後端：離線 stub（確定性子角度、零外部呼叫可測，教訓 1）／真實 urllib
- [x] 子角度數**有上限**（≤5）；擴展失敗友善繁中、退回單 query 搜尋（教訓 3）
- [x] 不新增資料表、不改 answerer 介面；測試 209→219、零回歸
- [x] **(b) agentic 明確 out of scope**（未做，draft 保留）

## 階段 10：根因萃取（冊封根因＝吸引子本體）

> 場的四步（進水→**冊封**→讀→修剪）裡「冊封根因」那塊，也是母概念的本體。
> 設計源＝`concepts/有吸引子的場.md`（根因萃取紀律 :55、試金石 :63、拆開的 optimizer :109）。

- [x] **已完成（2026-07-25）**：對一則種子，AI 試抽「這東西為何 work 的根因」→ 產**候選
  why-node**（標「AI 推斷＋證據」、跑**試金石自我反駁**逐條標 pass/fail＋霧詞旗標）。**人冊封**
  （accept 可編輯／退回）才轉正；**已冊封 why-node 成高權重吸引子、餵回 `ask` 檢索**（閉環）。
  ＝原則 5「知識繞根因組織、權重由人冊封」的實作。使用者選**完整閉環**（AskUserQuestion 2026-07-25）。
  spec 012、history/040；232 測試綠、`/roots` 上線。**場的四步（進水→冊封→讀）合攏。**

<!--
  設計源：concepts/有吸引子的場.md（不是 draft——2b 紀律早寫在 concept）。規劃：knowie-next、history/039。
  拆開的 optimizer：AI 提候選（算梯度）／人冊封（optimizer step，不可逆）。
  folie à deux 解藥：萃取 MUST 對自己 adversarial（試金石自我反駁），不只證實（concept :133）。
  範圍：單則種子萃取＋試金石＋人冊封＋餵回 ask（高權重）。out：一根因掛多載體、成核、自動三角測量。
-->

**成功標準（可驗）：**
- [x] 對一則種子可「萃取根因」→ AI 產候選 why-node：**根因主張＋試金石逐條 pass/fail＋霧詞旗標＋
  證據 url**；標「AI 推斷」（原則 3、教訓 7 落結構，缺證據/試金石就不給冊封）
- [x] 候選預設**只是候選**（信心層級），**人冊封**（可編輯文字）才轉「已冊封」；可退回（刪）
- [x] **已冊封 why-node 成 corpus 高權重條目**、`ask` 檢索得到（權重 > 解說文，「重吸引子」）
- [x] 可插拔萃取後端：離線 stub（確定性、零外部呼叫可測，教訓 1）／真實 chat（urllib、零 pip）
- [x] 萃取失敗友善繁中（教訓 3）；新增 `why_nodes` 表（新表 OK，不動既有表，教訓 8）
- [x] **out of scope**：一根因掛多載體、成核（無主通量建議新吸引子）、跨來源自動三角測量（未做）

## 階段 11：趨勢讀數（讀場的盆地通量＝「幫你發現在紅什麼」）

> 場動力第一塊：不是進水/冊封，是**讀場的趨勢**。收尾最初的使用者請求「幫我發現在紅什麼」
> （2026-07-24）。設計源＝`draft/2026-07-24-趨勢熱詞發現.md`（B 段）。

- [x] **已完成（2026-07-25）**：首頁頂端顯示 6–8 個**熱詞 chips**——從**已落庫的匯整材料**
  用**統計法**萃取當前高頻主題詞，點擊 → `/pull?topic=<熱詞>` 深挖。＝discovery：使用者不必
  事先知道趨勢。趨勢＝**盆地通量**（`concepts/有吸引子的場.md`），與成核同源。
  spec 013、history/043；243 測試綠、首頁上線。**收尾最初的請求「幫我發現在紅什麼」。**

<!--
  設計源：draft/2026-07-24-趨勢熱詞發現.md（B 趨勢讀數；A web 搜尋已 spec 009/010/011 完成）。
  規劃：knowie-next（2026-07-25）、history/042。母概念：趨勢＝盆地通量、成核同源。
  MVP 繞過缺口：全量候選池未落庫（只 top-N），先用已落庫匯整標題統計——零 schema 變更（教訓 8）。
  範圍：先 (b) 統計＋純高頻；(a) LLM 萃取、竄升排序（需存歷史）、成核、全量池落庫 → 後續。
-->

**成功標準（可驗）：**
- [x] 首頁出現 6–8 個熱詞 chips，**源自真實落庫材料**、點擊 → `/pull?topic=` 深挖
- [x] 熱詞用**統計法**（stdlib 詞頻/中文 bigram＋停用詞過濾，可溯源、零成本、離線可測，教訓 1）
- [x] 措辭**描述性**（「今日高頻」非「會爆」）；每個熱詞可回溯背後材料（原則 3/4）
- [x] **不新增資料表**（用已落庫 `digest_entries` 標題，教訓 8）；測試 234→243、零回歸
- [x] **out of scope**：(a) LLM 萃取、竄升排序（需歷史）、成核（建議新吸引子）、全量候選池落庫（未做）

## 階段 12：首頁重新整理（web 完整性）

> **非場動力**——是把「拿最新」的操作成本降到一鍵（根公理），讓 web 介面自足、免回 CLI。

- [x] **已完成（2026-07-25）**：首頁「🔄 重新整理」鈕 → 從啟用來源**重跑分診**（複用 CLI
  `run_digest`／`build_backend_builder`）→ 存新匯整 → 顯最新（熱詞一併重算）。**使用者明確觸發、
  不自動定時**（原則 5）；同步；失敗友善不崩、舊匯整保留（append）；缺漏來源沿用標示（憲章 V）。
  spec 014、history/044；247 測試綠。

<!--
  緣起：使用者「希望首頁有個重新整理功能」→ AskUserQuestion 選「重跑分診、拓最新」。規劃 history/044。
  同步 MVP（DigestBuilder.build 非 generator）；串流即時進度、自動定時 → out of scope。
  復用整條 digest 管線＋save_digest（append）＝零 schema（教訓 8）；可注入 digest_refresh_factory（教訓 1）。
-->

**成功標準（可驗）：**
- [x] 首頁有「重新整理」鈕，按下重跑分診、抓最新、存新匯整、顯最新（含重算熱詞）；免 CLI
- [x] **使用者明確觸發、不自動定時**（原則 5）；鈕旁有成本提示
- [x] 缺漏來源沿用 `missing_sources` 標示（憲章 V）；失敗友善繁中、不噴堆疊、非 500（教訓 3）
- [x] 可注入離線 stub、零外部呼叫可測（教訓 1）；不新增/不改資料表（教訓 8）；舊匯整不刪
- [x] **out of scope**：串流即時進度、自動/定時刷新、UI 選日期/主題、同日去重、CLI（未做）

## 階段 13：live web 活水（開放網路進每日 digest）

> 治使用者核心痛點：**追不到剛紅的 AI 趨勢**（連 Opus 5 都漏）——固定名冊（論文骨幹＋週刊）
> 結構上抓不到即時產品新聞。concept 第 3 層**反濾泡／驚訝力**的具體機制：伸手到策展名冊**之外**。
> 設計源＝`draft/2026-07-24-趨勢熱詞發現.md`（live 活水段）。

- [x] **已完成（2026-07-25）**：`WebSearchAdapter`——digest 除了抓固定名冊，也對「AI 最新」跑
  web 搜尋（複用 spec 009 `WebSearch` 後端）、把結果映成 `Item` 餵進**既有 digest 管線**（去重/
  依興趣排序/消化）。註冊 `access_method="web_search"`；一個**預設停用**的源 `web-ai-trends`，
  使用者在 `/sources` 啟用＋設搜尋金鑰才生效（opt-in、控成本）。spec 015、history/046；257 測試綠。
  **治使用者核心痛點（追不到剛紅、連 Opus 5 都漏）＋①擴名冊止血（history/045）。**

<!--
  緣起：使用者痛點「追不到剛紅 AI 趨勢、連 Opus 5 都漏」。先①擴名冊止血（history/045）、②本階段根治。
  設計源：趨勢 draft live 活水段。規劃 knowie-next、history/045。概念：反濾泡/驚訝力（第 3 層）。
  復用：SourceAdapter.fetch＋WebSearch.search＋整條 digest 管線＋missing_sources＋/sources 管理。
  成本閘：opt-in 預設停用＋需金鑰＋query 數有限。web 進的是「流」非種子（種子只經「收進」，原則 5）。
-->

**成功標準（可驗）：**
- [x] 啟用 `web-ai-trends` 源＋有搜尋金鑰時，digest／refresh **把開放網路剛紅新聞納入匯整**（Opus 5 這類進得來）
- [x] **預設停用**：不啟用／不設金鑰＝零成本、行為與現在一致（opt-in、原則 5）
- [x] web 結果帶網址、匯整每則可回原文（原則 3）；進的是**流非種子**（要留仍靠「收進」，原則 5）
- [x] 搜尋失敗 → 沿用 `missing_sources` 標示、digest 照常產出（教訓 3）
- [x] 離線 stub 可測、零外部呼叫（教訓 1）；不新增資料表（web 源＝sources 表一列，教訓 8）；測試 249→257
- [x] **out of scope**：web 結果自動變種子（絕不）、竄升/成核、per-query LLM 擴展、即時串流（未做）

### 階段 13 增量 b：web 活水 news 模式（只回近期新聞）

- [x] **已完成（2026-07-26）**：實跑階段 13 發現通用查詢＋一般搜尋回 SEO 常青清單文、非剛紅
  新聞。加 **news 模式**：web 活水走搜尋服務的 `topic=news`＋時間範圍（`config.search_news_time_range`
  預設 week），只撈近期新聞；手動 `/search` 維持一般搜尋。向後相容（news/time_range 預設關）。
  spec 016、history/048；267 測試綠。**真跑驗證**：web 活水改回真新聞（SecurityWeek/Axios 取代
  bentoml/instaclustr 那類 SEO 清單文）。同批修了 `_canonical` 混時區 published_at 崩潰（曾讓重整整份失敗）。

## 階段 14：匯整分區（新聞流 vs 基礎知識）

> 使用者洞見「有些來源是很重要的基礎知識、不是新聞類」。這是把母概念 concepts/有吸引子的場 的
> **流（news/趨勢，會衰減）vs 吸引子（基礎/常青）** 在 UI 上顯性化。

- [x] **已完成（2026-07-26）**：首頁把匯整分兩區——**📰 今日新聞**（type=news 源：新聞媒體/HN/Reddit/
  web 活水）＋**📚 基礎知識精選**（type∈{paper,blog}：arXiv/HF/教學/經典部落格）。只改呈現、不動
  匯整產生流程；空區不顯示、舊資料落預設區。HN/Reddit 重分類 blog→news。spec 017、history/049；
  274 測試綠、真跑分區（新聞 9／基礎 6）。使用者選「分區顯示」（AskUserQuestion 2026-07-26）。

<!--
  緣起：使用者「有些來源是基礎知識非新聞」。方案：分區顯示（非「基礎源→種子候選」的方案 A）。
  設計源：concepts/有吸引子的場（流 vs 吸引子）。規劃 history/049。
  最小新增：digest_entries 加 source_id 欄；home 依 sources id→type 分兩區（教訓 8）。
  範圍：只分區顯示；基礎源改種子候選/不進流/改衰減 → 後續（方案 A）。
-->

**成功標準（可驗）：**
- [x] 首頁分「今日新聞」＋「基礎知識精選」兩區；基礎源進基礎區、新聞源進新聞區、皆可回原文
- [x] HN/Reddit 歸新聞流（type blog→news）；空區不顯示；舊匯整條目落預設區、不崩
- [x] `digest_entries` 加 `source_id` 欄（_migrate 冪等、不改既有欄，教訓 8）；不改匯整產生流程
- [x] 測試 268→274、零回歸
- [x] **out of scope**：基礎源改種子候選/不進流、改衰減邏輯、每區調參、UI 改 type、CLI 分區（未做）

## 階段 15：場對新材料做工（理解＝forward pass over your field）

> 護城河的核心：**Google 架構上做不到**——它沒有「你的場」。新材料進來，工具不做通用摘要，
> 而是**接到你冊封的根因/種子**（延伸/牴觸/成核）。設計源＝`concepts/有吸引子的場.md`
> （理解＝前向傳遞 :97、梯度＝成核/證偽/驚訝 :116）＋`draft/2026-07-26-轉向場的護城河.md`（設計 A）
> ＋`draft/2026-07-26-跟得上趨勢者的知識整理.md`（北極星習慣 2/3/5）。

- [x] **已完成（2026-07-26）**：對一則材料（MVP＝`/library` 種子「🧭 關聯到我的場」）→ 嵌入找最近的
  **已冊封根因/種子** → LLM **grounded** 判關係：**延伸**該根因／**牴觸**該根因（殺手級——Google 不會
  告訴你「這跟你以為的相反」）／都很遠 → **成核候選**（新地基？建議萃取根因）。顯示＋連到該 why-node。
  **AI 提關係、人決定；場不自動改**（原則 5）。spec 018、history/051；286 測試綠、`/field/relate` 上線。

<!--
  設計源：concepts/有吸引子的場（forward pass/拆開的 optimizer/成核-證偽-驚訝）＋draft 轉向場的護城河
  （設計 A）＋跟得上趨勢者（北極星）。規劃 knowie-next、history/050。
  復用：已冊封 why-node（source_class=root）＋種子＋ensure_embeddings/cosine＋make_answerer（grounded）。
  範圍：MVP＝單則材料按需關聯；out：自動標註每則、批次成核聚類、自動改場（違原則 5，同否決自動冊封）。
  這是護城河：Google 無「你的場」故做不到；貼北極星「把新東西接到少數核心理解」。
-->

**成功標準（可驗）：**
- [x] 對一則材料可「關聯到我的場」→ 回**延伸/牴觸 你的某根因**，或**成核候選**（離所有吸引子都遠）
- [x] 關係 **grounded**：只依材料＋那條根因主張，可回溯核對；不杜撰（原則 3、教訓 7）
- [x] **牴觸偵測**：材料與某冊封根因相反時明確標出（北極星「連點/三角」）
- [x] **場不自動改**：AI 提關係候選、人決定要不要據此動場（原則 5，同否決自動冊封）
- [x] 可插拔離線 stub（judge/embedder）、零外部呼叫可測（教訓 1）；LLM 失敗友善（教訓 3）；測試 274→286
- [x] **out of scope**：自動標註每則匯整、批次成核聚類、自動改場、牴觸自動退根因、多跳（未做）

### 階段 15 增量 b：forward-pass 接每日流（匯整條目也能「關聯到我的場」）

> 讀 delta 最該作用在**剛炸的新聞**上——階段 15 只在種子（已存的），把引擎接到**每日流**才真正
> 兌現北極星習慣 2/3/5。設計源＝`draft/2026-07-26-跟得上趨勢者的知識整理.md`（缺件 #1）。

- [x] **已完成（2026-07-26）**：首頁每則**匯整條目**（新聞＋基礎兩區）加「🧭 關聯到我的場」→
  複用階段 15 的 `FieldRelate`（延伸/牴觸/成核、grounded、場不自動改）。缺口只補了暴露條目 id＋
  `/field/relate` 泛化吃流的條目（非只種子）。spec 019、history/053；298 測試綠，真後端在每日流上驗過。

<!--
  規劃：knowie-next（2026-07-26）、history/052→053。復用 spec 018 FieldRelate/結果頁全部。
  實作：get_last_digest 帶 digest_entries.id；repository.get_entry_material(id) 取任一條目材料（種子或流
  同住 digest_entries、一路徑服務兩者）；/field/relate 用它取代 list_seeds、exclude_url 排除自己；
  PageEntry+entry_id（entry_to_page 用 getattr，PullEntry 無→None）；_entry.html {% if e.entry_id %} 關聯鈕，
  digest.html 兩區共用故一次生效、pull 頁自然無鈕。零新相依/零新表/不改引擎。
  範圍：按需按鈕（維持深淺分明、不自動標每則）；out：自動標註每則、批次成核、多跳。
-->

**成功標準（可驗）：**
- [x] 首頁**匯整條目**可「關聯到我的場」→ 回延伸/牴觸/成核（同種子版）、連到根因、可回核對
- [x] `/field/relate` 吃**流的條目**（用 `digest_entries.id` 取材料）；排除自己（`exclude_url`）
- [x] 場不自動改（原則 5，relate 不寫庫）；grounded（原則 3/教訓 7，真後端誠實回無關聯已驗）；離線可測（教訓 1）；失敗友善（教訓 3）
- [x] **維持按需**（不自動標每則，深淺分明；首頁載入零關聯呼叫，有守衛測）；測試 286→298 不回歸
- [x] **out of scope**：自動標註每則、批次成核、多跳（未做）

## 階段 16：反逢迎的當下副手 — 時刻 A「值不值得 follow」（★ 產品轉向）

> **這是路線圖的轉向點。** 前 15 階段蓋了「你要去照顧的知識庫／目的地」，但使用者**從沒自己開過它**
> （見 experience「提案-批准 ≠ 打到需求」）。北極星從「知識庫你照顧」改成「**當下能問、不順著你走的
> 副手**」。設計源＝`draft/2026-07-28-反逢迎的當下副手.md`；賭注 A（一份誠實的「值不值得＋真實心得＋
> 怎麼用」到底有沒有幫到你）**已用手動探針驗過＝有用**（2026-07-28，對 Claude Opus 5 那則）。

- [x] **已完成第一塊（2026-07-28）**：`/worth` 丟一個新 AI 東西（名字／內文／網址）→ 撒網獵取
  **真實用戶心得**→ 回**反逢迎綜合**（官方/獨立/用戶分開、明說炒作、有引用、grounded）。spec 021、
  history/057；322 測試綠。**真後端（Tavily+LLM）驗過**（Claude Opus 5）：三層來源＋明說炒作/缺點/難搞
  ＋13 引用＋自己抓到證據命名混亂並說「沒腦補」，複刻手動探針。**真驗收「一週自己伸手用」仍待觀察。**

<!--
  規劃：knowie-next（2026-07-28）＋手動探針（賭注 A 過）。手動探針＝參考實作：
  fetch(or 收內容)→ 獵心得多角度 query → 反逢迎綜合。
  複用：make_web_search（Tavily）、make_answerer、search/smart.py＋expand.py（查詢擴展骨架）、
  Tailwind RWD。缺口＝兩段 prompt（獵心得 query＋反逢迎綜合）＋收內容口＋手機可達。
  探針暴露的兩個真實風險（使用者親點）：① 伺服器抓常被擋（WebFetch 403→curl；牆內更抓不到）→
  收內容口（客戶端送內文）；② query 品質決定成敗（要獵心得/批評/怎麼用，不是查通用名）。
-->

**成功標準（可驗）：**
- [x] 丟一個新 AI 東西 → 回**反逢迎綜合**：官方說法／獨立評測／真實用戶心得**分開擺**，明說「炒作/不值得」，有引用
- [x] **收內容口**：接受客戶端提供的內文（牆內也收得到）；伺服器抓＝best-effort、擋掉不崩（subject 解析序 name＞content首行＞url標題＞url）
- [x] **獵心得 query**：後端自動生成多角度查詢（心得/評價、vs 對手、值不值得/缺點、怎麼用），非查通用名（`worthit_queries` 確定性模板）
- [x] **手機搆得到**：頁面 RWD＋收內容口；tunnel（cloudflared）＝ops、非本 spec 功能；**out：容器化／K8s／Helm**（`部署與介面路線` draft 已擱置）
- [x] WebSearch／抓取／綜合可注入 → 離線 stub 可測（教訓 1）；失敗/空友善繁中（教訓 3）；全繁中（憲章 II）；核心零新相依（憲章 IV）
- [ ] **真驗收（超越測試綠）**：接下來一週使用者**自己伸手用過幾次**（experience 教訓：批准 ≠ 打到需求）——**待觀察**
- [x] **out of scope**：場驅動關聯（fast-follow）、收進捕捉 extension（設計 B 後續）、moment B/C、K8s/Helm（未做）

## 階段 17：跟你的場聊天（moment B/C ＝ 迴圈引擎）

> **排序更正（2026-07-29，使用者點破）**：對話是**動機引擎**，/worth 等進料口沒有「想跟場聊」這個
> 動機就不會被餵——所以「/worth 一週會不會用」在沒有對話下**是假陰性**。**先有對話，餵養才有理由。**
> 這 re-route 掉「先等 /worth 訊號再蓋對話」。且對話賭注**已被本次示範驗過**（使用者：「像我剛剛那樣，
> 我才會因為想聊把東西丟進來」）。設計/行為規格＝`draft/2026-07-28-反逢迎的當下副手.md`「跟你的場聊天」段
> （2026-07-29 現場示範＝可執行規格）。

- [x] **已完成（2026-07-29）**：`/chat` 多輪對話，AI **從你冊封的根因往下推**（注入 anointed roots）、
  **反逢迎的膜**（標 grounded/猜、分 derived/empirical/applied 且低層不借高層權威、攔過度抽象/逢迎、
  建設性）、**按需找真研究佐證**、**提冊封候選、人按才寫進場**（原則 5）。spec 022、history/059；336 綠。
  **真後端驗過（第一風險）**：對真實場問 Flow matching → 自動版複刻手動品質（援引 #14/#13 根因往下推、
  通篇標 grounded、把類比標為 applied 不冒充 derived）。**真驗收「複刻到讓你想餵場/動機迴圈」長期待觀察。**

<!--
  規劃：knowie-next（2026-07-29）＋本次現場示範（探針過）。複用：list_field_attractors（讀場）、
  make_web_search（佐證）、chat 後端（_post）、add_why_node/anoint（人閘門寫回）。
  第一風險＝自動版能否複刻手動品質（價值全在 membrane system prompt，如 /worth 的兩段 prompt）。
  桌面深工優先（非手機一瞥）→ 降低部署前置。多輪（不同於 /worth、/ask 的一次性）。
  範圍：對話＋讀場＋膜 prompt＋按需佐證＋anoint-from-chat（人按）。
  out：自動 tool-use 每輪自搜（佐證按需）、自動 capture、手機/tunnel 急迫、軌跡量測儀表板。
-->

**成功標準（可驗）：**
- [x] 多輪對話**從你冊封的根因往下推**（注入你的 anointed roots 當場脈絡），非通用先驗
- [x] **反逢迎的膜**：標 grounded/猜；分 derived/empirical/applied 且**低層不借高層權威**；攔過度抽象/逢迎；建設性
- [x] 提冊封候選（`/chat/distill` 蒸餾）；**人按「冊封」才 `add_why_node`+`anoint` 進場**（`/chat/anoint` 為唯一寫 bedrock 路徑，原則 5，永不自動；有守衛測）
- [x] **按需找佐證**：對一個主張跑 web 搜尋 → 附真引用（`/chat/cite`，grounded，原則 3/教訓 7）；不自動每輪搜
- [x] 對話/搜尋/寫回可注入 → 離線 stub 可測（教訓 1）；失敗/場空友善繁中（教訓 3）；全繁中；核心零新相依
- [~] **真驗收（超越測試綠）**：自動版**複刻手動品質到讓你想餵場**（動機迴圈）。**早期強烈正向訊號
  （2026-07-29）**：使用者回報「**有根據聊＋反逢迎的膜**幫助大、**會一直不斷想使用**、還能助**研究可能性
  的討論**（moment B）」——這是整場堅持的「真實使用才是驗收」訊號本人，且指認**價值在膜、非花招**，
  回頭驗證了產品轉向（→反逢迎的當下副手）。**仍非結案**：真證據是軌跡（幾週後是否續用、場是否越聊
  越豐富、是否真冊封）——標「強烈正向、待軌跡」。測試 322→336 不回歸
- [x] **out of scope**：自動每輪自搜、自動 capture、手機/tunnel、軌跡儀表板、moment A 改動（未做）

## 階段 18：對話的「由來」存檔（tool 的 episodes 層）

> 冊封留下**結論**（根因＋階梯），卻丟了**現場**——半年後看根因「當初為何信這個」，由來不見了。這層
> 讓值得回想的對話能存下、且**連回它生出的根因**。＝tool 長出 knowie 的 episodes（三層齊：根因＝
> principles／對話＝draft／存下的對話＝episodes）。設計源＝`draft/2026-07-29-對話的由來存檔.md`。

- [x] **已完成（2026-07-29）**：兩個存檔點——① **冊封時**「連同這段對話一起存成由來」（連到該根因）
  ② 獨立「**存這段對話**」。**存整段＋自動生一句『由來』標題**。`/roots` 根因下「← 由來」連得回、
  `/conversations` 清單/單篇。spec 023、history/061；362 綠、真後端驗過（標題「Transformer殘差為何用加法」）。

<!--
  規劃：knowie-next/capture（2026-07-29）、history/060。使用者定案：兩存檔點都要、存整段＋自動標題。
  一條硬原則：存下的對話仍是沙盒層——唯讀參考、**絕不注入回對話當 grounded 場**（否則幻想污染回來，
  違 principle 6）；只有冊封的根因是地基、對話是其由來/附錄。
  實作：需新表 conversations（訊息 JSON＋自動標題＋可選 linked_why_node_id）——第一個落庫的對話產物。
  查閱主路徑＝根因→由來對話（/roots 加連結）＋輕量「存下的對話」清單。
  out：自動存全部（囤積）、注入回對話、全文搜尋、跨對話關聯。
-->

**成功標準（可驗）：**
- [x] **冊封時**可「連同這段對話一起存成由來」→ 該根因（`/roots`）能連回那段對話（真後端驗過）
- [x] **獨立**可「存這段對話」（未冊封的探索也能留）；**存整段**、自動生一句『由來』標題
- [x] **唯讀參考、不入地基**：存下的對話**不入** `build_field_system_prompt`（場脈絡只來自冊封根因；核心守衛測：存 SECRET_FANTASY 後新對話 system 不含它）（原則 6）
- [x] **人閘門、選擇性、預設流走**（原則 5；人按才存、不自動存全部）
- [x] 存/讀/標題可注入 → 離線可測（教訓 1）；標題失敗退回不崩＋刪根因不孤兒（教訓 3）；全繁中；測試 348→362 不回歸
- [x] **out of scope**：自動存全部、注入回對話、對話全文搜尋、跨對話關聯（未做）

## 階段 19：匯出給 NotebookLM（複製 Markdown＋複製佐證網址）

> 用了 `/chat` 後使用者問「**不會跟 NotebookLM 重疊嗎？**」→ 定調**不是競爭是接力**：這工具做**膜／蒸餾**
> （護城河），NotebookLM 做**打磨輸出**（audio/study guide）；**不重蓋 audio，匯出讓它接力**。要匯出
> **兩種料**：**蒸餾內容**（貼文字來源＝真價值，NotebookLM 抓不到）＋**佐證網址**（當 URL 來源，它自己抓、
> 但會踩 403，當附帶）。設計源＝`draft/2026-07-29-匯出給NotebookLM.md`。

- [x] **已完成（2026-07-29）**：`/chat`、`/conversations/{id}`、`/roots` 各加**📋 複製 Markdown**＋
  **🔗 複製佐證網址**兩顆鈕。純 formatter 模組 `export/notebooklm.py`（零相依 4 函式）＋3 個 `text/plain`
  端點（`as=md|urls`）＋共用 `copyExport`/toast。**來源逐訊息塊**（避 `[n]` 撞號，非全域清單）。
  spec 024、history/063；368→393 綠。**零外部相依**故離線測即完整驗證（無真後端待驗）。

<!--
  規劃：knowie-next/capture（2026-07-29）。使用者定案：「複製 Markdown 和複製佐證網址都要，才能都丟給
  NotebookLM」。最快＝純前端複製鈕、零後端（資料已在頁面）；可測核心＝純函式 formatter（離線可測、
  失敗不崩）。誠實判準（膜）：只給網址＝降成書籤匯出器、丟了蒸餾——內容為主、網址為輔。
  out：下載 .md 檔、LLM 洗「蒸餾 brief」（fast-follow）、直推 NotebookLM API、對話全文搜尋。
-->

**成功標準（可驗）：**
- [x] `/chat`、`/conversations/{id}`、`/roots` 皆可**一鍵複製 Markdown**（乾淨、含引用與來源，貼 NotebookLM 文字來源）
- [x] 皆可**一鍵複製佐證網址**（被引用來源、去重每行一個，貼 NotebookLM URL 來源）
- [x] **可測純 formatter**：對話→Markdown、對話→網址清單為純函式，離線可測、失敗不崩（教訓 1/3）
- [x] **純匯出、唯讀**：不改場、不注入回對話（守衛測：匯出後 `build_field_system_prompt` 不含發想內容，principle 6）；全繁中；核心零相依
- [x] 測試 368→393（＋25）不回歸
- [x] **out of scope**：下載 `.md` 檔、LLM「蒸餾 brief」、直推 NotebookLM API、對話全文搜尋（未做）

## 階段 20：對話收料的漏——去重＋收尾缺口（真實使用審計照出）

> 使用者請我審他自己的場（19 段對話／24 根因），照出 spec 023 兩個漏：**「引擎好 ≠ 收料好」**——深長
> 推導煉出 15 條根因（引擎好），但**收料**跟不上：① `冊封時連同存`把同一段對話**複製了 15 份**（打臉階段
> 18「不囤積」）；② 收到第 32 句就停、卻聊到第 70 句，**最原創的四元樹/影片串流落點沒被蒸餾**。
> 設計源＝`draft/2026-07-29-對話收料的漏.md`。

- [x] **已完成（2026-07-29）**：**修 #1 去重**——`save_conversation` 依內容指紋冪等＋連結改存
  `why_nodes.conversation_id`（多對一）、provenance 改讀 why_node 側；anoint 前端零改自動生效。
  **修 #2 缺口提醒**——純函式 `distill_gap`＋chat 頁 client localStorage 記上次收位置、只提醒不自動冊封。
  spec 025、history/064；393→414 綠。真實 db 驗：migrate 冪等回填 16 連結、既有由來不斷。

<!--
  規劃：knowie-capture（2026-07-29）、真實使用審計。使用者定案：先捕捉、再 speckit 修 #1+#2。
  #3（標題凍住/無章節切分）待議、暫不做。硬約束：判準純可測（教訓 1）；不自動冊封（人閘門，原則 5）；
  去重不得誤刪既有存檔（唯讀取向、只改「新存時連既有」）。
-->

**成功標準（可驗）：**
- [x] **修 #1**：同一段對話冊封 N 條根因 → 只留**一份**存檔、N 條根因皆連到它（測證：連 3 anoint→1 份、3 條映同 cid；異段不誤併）
- [x] **修 #2**：長對話 + 根因停在前段 → 出現「尾段未收」提醒＋區間；短或剛收滿 → 不提醒（`distill_gap` 純函式＋web 測）
- [x] 判準純函式、離線可測（教訓 1）；不自動冊封（人閘門，原則 5，守衛測）；去重不誤刪、不改場（原則 6）
- [x] 全繁中；核心零相依；測試 393→414 不回歸
- [x] **out of scope**：#3 章節切分／落點重命名（待議）、既有 15 份複本清理（→ 階段 21）、跨對話關聯、對話全文搜尋（未做）

## 階段 21：既有重複對話清理（一次性、非破壞、人確認）

> 階段 20 只修「以後不再複製」；`learnnews.db` 仍有既存複本（15 份同 32 句 Flow Matching＋其他）。這層把
> **同指紋（內容完全相同）的複本併成一份**、把指向舊複本的根因**重指到留存那份**、刪其餘。設計源＝
> `draft/2026-07-29-對話收料的漏.md`（未竟項）。

- [x] **已完成（2026-07-30）**：`/conversations`「🧹 清理重複對話」——GET 預覽（N 組/M 份多餘/K 根因重指、唯讀）
  ＋POST 人確認執行。純函式 `plan_dedupe`（分組/選留存 max id/重指，複用 `conversation_fingerprint`）＋
  repo `dedupe_plan`/`apply_dedupe`。spec 026、history/065；414→423 綠。真實 db dry-run 驗：2 組/15 份多餘/
  14 重指、19→4（異長度版 #18/#19 保住）、唯讀未動。

<!--
  規劃：使用者 2026-07-30 定案排入。硬約束：只併 conversation_fingerprint 相同者（非破壞、不同內容絕不併）；
  dry-run 預覽＋人確認（原則 5，刪除是不可逆）；重指根因連結、不改根因主張；不動指紋不同者（如 #18/#19 65/70 句）。
  純核心：分組/選留存/重指計畫為純函式、離線可測；執行層才真刪。已備份 learnnews.db。
-->

**成功標準（可驗）：**
- [x] **dry-run 預覽**：回報「N 組重複、M 份多餘、K 條根因將重指」，**不動任何資料**（人閘門守衛測）
- [x] **執行（人確認後）**：每組同指紋只留一份（最新）、指向舊份的根因全重指到留存份、刪其餘份
- [x] **非破壞**：只併**同指紋**；指紋不同者（#18/#19 等）**不動**；不改根因主張、不誤刪（守衛測）
- [x] 分組/選留存/重指計畫為**純函式**、離線可測；清理後每條根因仍連得到（留存份、不孤兒）
- [x] 全繁中；核心零相依；測試 414→423 不回歸
- [x] **out of scope**：#3 章節切分／落點重命名（→ 階段 22）、跨對話關聯、全文搜尋、自動定期清理（未做）

## 階段 22：對話的可找回性——落點重命名＋章節切分（#3）

> 真實使用審計的第三個漏：長對話**標題凍在第一句**（19 份全叫「Flow Matching」），最原創的落點（四元樹/
> 影片串流）被埋、**半年後找不回**。使用者 2026-07-30 定案做**全 #3**。設計源＝`draft/2026-07-29-對話收料的漏.md`（#3）。

- [ ] **待實作（MVP＝重命名）**：**US1 重命名**——標題改由**落點/全貌**生（非第一句）＋**手動改名**＋既有可重命名；
  **US2 章節切分**——把一段對話切成有小標的章節（LLM 可注入、離線 stub 可測；**算好即用、不落庫**）、可跳讀；
  **US3 每章動作**——單獨匯出（複用 spec 024＋range）＋「整理這章成重點」（複用 distill on 章節切片→冊封）。

<!--
  規劃：使用者 2026-07-30 選「全 #3」。膜提醒：章節切分較投機，守原則 6 過度擬合檢查——LLM 可注入、
  離線可測、on-demand 不落庫、算好即用。優先序：US1 重命名(P1，真解找不回)＞US2 切分(P2)＞US3 每章動作(P3)。
  複用：FieldChat.title(改提示)、spec 024 匯出、distill_factory；教訓 1 離線 stub、教訓 3 失敗退回。
-->

**成功標準（可驗）：**
- [ ] **US1 重命名**：新存對話標題反映**落點/全貌**（非第一句）；`/conversations` 可**手動改名**；既有對話可重生標題。標題生成可注入 stub、失敗退回不崩
- [ ] **US2 章節切分**：一段對話可切成**有小標＋範圍＋一句摘要**的章節、可跳讀；切分器可注入（離線可測）、**不落庫**（on-demand）
- [ ] **US3 每章動作**：每章可**單獨匯出 Markdown/網址**（複用 spec 024）＋「整理這章成重點」（複用 distill→冊封，人閘門）
- [ ] 全繁中；核心零相依；不自動改名/不自動切分/不自動冊封（原則 5）；測試 423→ 不回歸
- [ ] **out of scope**：自動存時就切分、跨對話關聯、對話全文搜尋、章節落庫/版本、CLI

## 關鍵延伸（主題觸發必讀）

<!--
  這張表是 /knowie-next 和 /knowie-judge 的「閱讀路由」。
  主文只寫願景骨幹；詳細按概念放進 concepts/。
-->

| 觸發關鍵字 | MUST 讀 |
|---|---|
| [關鍵字 / 主題] | `concepts/[檔名].md` |
